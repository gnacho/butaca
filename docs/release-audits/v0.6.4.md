# Release audit — v0.6.4

The evidence behind [the v0.6.4 release note](https://github.com/GLinnik21/plx-native/blob/v0.6.4/docs/release-notes/v0.6.4.md). Written for a webosbrew reviewer, a contributor auditing an old release, and us in a year. The section at the bottom is generated from the artifact that was published; everything above it was written by a person before the release ran. Cut from `release/v0.6`, like v0.6.3 before it; the generated block is filled in and committed to that branch, not to the tag — read this document at `blob/release/v0.6/docs/release-audits/v0.6.4.md` if the generated section matters to you.

## What this release contains

- **Issue #76 persistence recovery.** Host tests cover fresh reauthentication over a recognized secure envelope whose key service is unavailable, plus the separate client-ID-only primary/plaintext shadow case. Fresh reauthentication takes the explicit owner-only recovery path at the selected candidate and sweeps known lower copies; a selected unknown-format envelope remains blocked, while unused unknown-format copies are preserved. Real keymanager3 behavior remains unconfirmed.
- **`session::save` reports what it actually did, not a bare bool.** `PersistOutcome` (`persisted_plaintext` / `persisted_sealed` / `preserved_existing_secure` with a reason of `not_proven`, `refused_marker_no_fresh_sign_in` or `seal_failed` / `blocked_unknown_envelope` / `write_failed` / `serialization_failed`) replaces the bool `save` used to return. Auth uses `.persisted()` to distinguish a saved session from a run-only session requiring a warning. One `session: persist outcome=<wire>[ reason=<wire>]` log line per save, whichever branch ran, distinguishes deliberate preservation from a genuine write failure.
- **`read_locked` records why each candidate path was rejected, not only that the launch as a whole failed.** `ReadRejection` includes `unparsable` for readable bytes with no recognized shape, and each `CandidateRead` is tagged by its position in the candidate search order (`developer` / `internal` / `app_dir` / `runtime` / `other`), never by exposing a path. The result is a bounded comma-joined wire line and one load log; no trust-policy change is intended.
- **Storage diagnostics are available without successful sign-in or persistent telemetry consent.** The automatic `SignInNotPersisted` report retains the consent-gated defer/replay path. Explicit one-off reports additionally carry owned cold-read errors and eligibility facts, fresh-save errors/readback, and typed per-candidate write attempts with numeric OS errors or durability warnings. Cold evidence is not replaced by a later successful save. Details uses the shared `Popover`/`TableView` and a Send report confirmation; manual requests have a distinct diagnostic source/fingerprint rather than inventing a failed Plex connection. The report's ephemeral event ID and delivery status are shown without creating a persistent user identifier.
- **An unwritable report queue is not the only delivery path.** One-off reports use the ordinary spool when possible and otherwise attempt a bounded background send of the same record. The UI distinguishes queue acceptance from direct sending, successful server response and failure; standing Errors/Usage routing remains unchanged. Notice revision is now 5, while both standing scopes remain 6. Each explicit report asks through its own updated confirmation; no account-wide re-prompt is introduced.
- **Failed fresh saves hold the auth handoff for an explicit decision.** Discovery and final-save warnings have attempt/generation keys; Details can report before a later save replaces that evidence. Continue finalizes from the latest coherent controller state without another write. Host regressions cover real write refusal, stale acknowledgement/report keys, and coherent handoff after reconciliation; native synthetic captures cover presentation, Details, confirmation and Continue.
- **The report and screen wiring has host and receiver evidence.** Save outcomes, one-off fields, and the Details read-out have production call-site tests. Candidate summaries now travel on load-owned detached reports through `telemetry::storage::report_error_with_candidate_reads`, so a probe report cannot steal the session-read summary. A native one-off reached development Sentry with cold storage errors and hardware/webOS context while its selected spool path was deliberately unwritable and standing consent remained off.

## Device test evidence

| | |
|---|---|
| television | dev set: LG 49SM9000PLA, board `m16p3`, SoC `M19_DVB`, platform release 4.10.2 — no real keymanager3; debug fake used for storage recovery |
| server | user completed fresh QR authorization during the recovery test; the following process restored that saved session without QR; no media playback |
| suite | `make check`: default-feature and hostsim unit suites, lint, and ancillary checks passed in local release preparation |
| shipping configuration | `CARGO_INCREMENTAL=0 cargo +nightly check --lib --no-default-features` and the stable ARM package build passed |
| suite | `./tests/run.py` — not run; nothing on the playback path changed |
| by hand | native debug-TV session passed startup, Details pointer/D-pad/BACK/new-code interactions, real QR recovery from a recognized sealed session with fake-keymanager `stall`, fresh disk readback `match`, and a new process restoring the plaintext session without QR while `stall` remained armed |

**What the runs do not settle.** Host tests and a real QR/application-process-boundary test on the TV establish recovery with the debug fake service, not real keymanager3 behavior or persistence across a TV power cycle. A separate synthetic sign-in error reached development Sentry through the real one-off button with `storage_candidate_reads=developer:plaintext`; fresh-save outcome delivery and the production endpoint were not tested by that report. See the [reassessment ledger](../measurements/issue-76-reassessment-2026-09-11.md) for the event and process evidence. Media playback was not part of this release verification.

## External compatibility reports known at release time

None new since v0.6.3. [Issue #76](https://github.com/GLinnik21/plx-native/issues/76) still carries the same six reporters (platform releases 5.6.2, 9.2.2, 10.3.1 and three at 11.2.0); none has yet run a build containing this fix.

## Compatibility tiers, and what moved

No new firmware support is claimed. The loader compatibility boundary is unchanged. This preparation checked startup and sign-in UI on the dev set; earlier playback results remain prior-version evidence.

## Known issues at release, with provenance

- **Whether a real `keymanager3` recovers the fixed loop the same way the fake one does is unconfirmed.** The dev fake reproduced a matching symptom and host tests covered the recovery; no television with a real key service has run this exact fix.
- **Receiver coverage is partial.** Development Sentry received native event `4175abcfc8948826378a90b3302e1650` through the direct unwritable-spool fallback, including cold candidate/error/eligibility and hardware/webOS facts. Fresh-save arrays were empty in that cold-read experiment; their serialization and real failed-write snapshot coverage is host-side. This build's production endpoint was not exercised. Neither receiver receipts nor PostHog analytics establish real-keymanager3 recovery.
- Every known issue in [v0.6.3's audit](https://github.com/GLinnik21/plx-native/blob/release/v0.6/docs/release-audits/v0.6.3.md#known-issues-at-release-with-provenance) not superseded above is unchanged, including the one-off libmali SIGSEGV recorded there.

## Deliberately not done

- **Forward-port to `main`** is deferred until the trunk's UI refactor lands, unchanged from v0.6.3's decision.

## Release configuration

| | |
|---|---|
| flavour | `com.beb.plxnative` (stable) — the id users install; `FLAVOR: stable` is pinned in the release workflow's build job |
| cargo features | `RELEASE=1`, which drops `devtools` and `devtriggers` (and with them the fake key service) |
| built by | GitHub Actions, from the tag, at a path identical on every runner |
| toolchain | webOS NDK, Rust nightly pinned by date in the `RUST_NIGHTLY` repository variable — unchanged from v0.6.3 |

No new payload file, no new outbound host, no new file written outside the app's own directories beyond the ones v0.6.3 already introduced.

The approved UI follow-up was checked on the simulator and native debug television. The current
source passed `make FLAVOR=stable RELEASE=1 ipk` and a separate `python3 ci/check-package.py`.
That local package is validation output, not a published CI artifact, and was not installed over
the household's stable app. Publication remains pending. This authored audit includes no local
artifact hash.

<!-- BEGIN GENERATED — ci/gen-release-audit.py. Do not edit by hand. -->

*Every value below was read out of `com.beb.plxnative_0.6.4_arm.ipk` by `ci/gen-release-audit.py`. Nothing in this block was typed by a person.*

### Identity and provenance

| field | value |
|---|---|
| tag | `v0.6.4` |
| commit | `391c74d0884e0fa9aa80b2e1aa64f08575507ffa` |
| workflow run | https://github.com/GLinnik21/plx-native/actions/runs/34599339828 |
| asset uploader | `github-actions[bot]` |
| built by | GitHub Actions (`.github/workflows/release.yml`, job `build + verify`) |

### The package

| field | value |
|---|---|
| filename | `com.beb.plxnative_0.6.4_arm.ipk` |
| app id | `com.beb.plxnative` |
| version | `0.6.4` |
| type | `native` |
| sha256 | `1d20d2b297d984e65b7186fd1cb932c9db7bcc045eee4173aa32f7b179101308` |
| download size | 16 829 512 bytes |
| installed size | 31 520 KiB (control `Installed-Size`) |
| ar members | `debian-binary` (4 B), `control.tar.gz` (384 B), `data.tar.gz` (16 828 935 B) |

**Hash agreement:** all 3 copies agree.

| source | sha256 |
|---|---|
| the artifact's own sha256 | `1d20d2b297d984e65b7186fd1cb932c9db7bcc045eee4173aa32f7b179101308` |
| `com.beb.plxnative.manifest.json` → `ipkHash.sha256` | `1d20d2b297d984e65b7186fd1cb932c9db7bcc045eee4173aa32f7b179101308` |
| `ipk.sha256` | `1d20d2b297d984e65b7186fd1cb932c9db7bcc045eee4173aa32f7b179101308` |

`ipk.sha256` names `com.beb.plxnative_0.6.4_arm.ipk`, so `shasum -a 256 -c ipk.sha256` works in the directory a user downloads into.

The Homebrew Channel manifest declares `ipkSize` 16 829 512 bytes and `installedSize` 31 520 KiB; the size it declares matches the artifact. `rootRequired` is `false`.

### Control file

| field | value |
|---|---|
| `Architecture` | arm |
| `Description` | PlxNative — unofficial native Plex client for LG webOS (not affiliated with Plex) |
| `Homepage` | https://github.com/GLinnik21/plx-native |
| `Installed-Size` | 31520 |
| `License` | MIT |
| `Maintainer` | Gleb Linnik <support@plxnative.com> |
| `Package` | com.beb.plxnative |
| `Priority` | optional |
| `Section` | misc |
| `Version` | 0.6.4 |
| `webOS-Package-Format-Version` | 2 |
| `webOS-Packager-Version` | plxnative-mkipk/1 |

### Build configuration

| field | value |
|---|---|
| flavour | `com.beb.plxnative` — the stable id, which is what users install |
| cargo features | `features:--no-default-features+symbols+tel:78a04a1fd0f5` |
| dev-trigger surface | absent — `plxnative-noidle`×0, `plxnative-remote`×0, `plxnative-capture`×0, `plxnative-drawmask`×0, `plxnative-heroground`×0 |
| telemetry endpoints | Sentry `https://30574b7659ec4fcdc39d6b3072deace8@o4511994943700992.ingest.de.sentry.io/4511994955890768` (EU); PostHog `phc_kVAmK72TzZXH4VwLkcJuJs5tqAgWh97GRqaSpR9cKn3M` — write-only ingest credentials, publishable by design |

The witnesses are literals from `dev.rs`'s `DIAG` array, which is `#[cfg(feature = "devtriggers")]`; two of them name the surfaces a reviewer reading this repository's source would rightly ask about — the world-writable `plxnative-remote` FIFO that can drive the UI, and the unauthenticated TCP capture listener. Zero of all five is what `RELEASE=1` looks like in the bytes, and it is a property of this package rather than of the command line that produced it. Each witness has been measured from both sides (0 here, 1-2 in a dev build), because a witness that cannot fail is not evidence.

### Package surface

| field | value |
|---|---|
| declared permissions | none — `appinfo.json` declares no `requiredPermissions` |
| `requiredMemory` | 160 MB |
| `id` prefix | not one of LG's reserved prefixes |
| localized descriptors | 12 locales |

**Host-shaped strings in the shipped binary.** A floor, not a proof: a string here may be a log message rather than a destination, and a host assembled at runtime would not appear at all.

```
1.0.net
answeringplex.tv
eu.i.posthog.com
github.com
index.crates.io
ns.adobe.com
o4511994943700992.ingest.de.sentry.io
plex.tv
plxnative.com
so.63libavformat-plx.63.dylibplex.tv
www.plex.tv
www.w3.org
yetplex.tv
```

**Absolute paths outside the app's own directory, same caveat.**

```
/dev/null
/dev/random
/dev/rtkmem
/dev/rtkmem/etc/prefs/properties/machine
/dev/urandom
/etc/os-release
/etc/umediaserver/device_codec_capability_config.json
/etc/xdg
/media/cryptofsunknowndevmodeapplications
/media/developer
/media/developer/
/media/developer/apps/usr/palm/applications/com.beb.plxnative
/media/internal/.
/proc/self/auxv
/proc/self/cgroupcpu.cfs_period_us/sys/fs/cgroupcgroup.controllers
/proc/self/exe
/proc/self/fdfatal
/proc/self/maps
/proc/self/mountinfocgroup/
/proc/sys/kernel/random/uuidplayback
/tmp/%s
/tmp/plxnative-dvnonode
/tmp/plxnative-profile
/tmp/plxnative-url
/var/run/nyx/device_info.json
/var/run/nyx/os_info.json
```

### Reproducibility evidence

No payload file carries a build machine's directory layout (all 35 files scanned; the NDK's own `--cross-prefix`, which FFmpeg records and which is identical on every runner, is the one allowed exception). That is what makes two builds of this commit on one machine byte-identical. It is not yet a cross-machine claim.

### Payload inventory

| path | bytes | mode | owner | sha256 (first 16) |
|---|---|---|---|---|
| `usr/palm/applications/com.beb.plxnative/OFL.txt` | 4 377 | `0644` | root:root | `5b9321a4298cfeb6` |
| `usr/palm/applications/com.beb.plxnative/THIRD-PARTY-NOTICES.md` | 24 025 | `0644` | root:root | `f5328c1cb3bee9e2` |
| `usr/palm/applications/com.beb.plxnative/appfont-bold.ttf` | 408 976 | `0644` | root:root | `b30d8082184474b4` |
| `usr/palm/applications/com.beb.plxnative/appfont-cjk.ttf` | 21 185 868 | `0644` | root:root | `eeb1b524ac15a8bb` |
| `usr/palm/applications/com.beb.plxnative/appfont.ttf` | 407 448 | `0644` | root:root | `4ef24f0bac98d3a6` |
| `usr/palm/applications/com.beb.plxnative/appinfo.json` | 452 | `0644` | root:root | `34ec53c1cda6ae2b` |
| `usr/palm/applications/com.beb.plxnative/icon.png` | 3 972 | `0644` | root:root | `d70bcff921afb66f` |
| `usr/palm/applications/com.beb.plxnative/largeIcon.png` | 9 716 | `0644` | root:root | `dc169758d9bc7fa9` |
| `usr/palm/applications/com.beb.plxnative/libavcodec-plx.so.63` | 441 112 | `0644` | root:root | `ec14fafb73febeea` |
| `usr/palm/applications/com.beb.plxnative/libavformat-plx.so.63` | 445 272 | `0644` | root:root | `cbf5763941a3f98c` |
| `usr/palm/applications/com.beb.plxnative/libavutil-plx.so.61` | 452 796 | `0644` | root:root | `813fcbe68d041ee7` |
| `usr/palm/applications/com.beb.plxnative/licenses/Apache-2.0.txt` | 10 280 | `0644` | root:root | `074e6e32c86a4c0e` |
| `usr/palm/applications/com.beb.plxnative/licenses/LGPL-2.1.txt` | 26 419 | `0644` | root:root | `20e50fe7aae3e563` |
| `usr/palm/applications/com.beb.plxnative/licenses/LICENSE` | 1 068 | `0644` | root:root | `3b297a24aa928a85` |
| `usr/palm/applications/com.beb.plxnative/licenses/LLVM-exception.txt` | 919 | `0644` | root:root | `e34c58338bd89d43` |
| `usr/palm/applications/com.beb.plxnative/licenses/MIT.txt` | 1 078 | `0644` | root:root | `b05785f9f18e6716` |
| `usr/palm/applications/com.beb.plxnative/licenses/TRADEMARKS.md` | 2 722 | `0644` | root:root | `9126d07ee3b66ac2` |
| `usr/palm/applications/com.beb.plxnative/licenses/Unicode-3.0.txt` | 1 995 | `0644` | root:root | `f7db81051789b729` |
| `usr/palm/applications/com.beb.plxnative/licenses/Zlib.txt` | 838 | `0644` | root:root | `bfb1112d49db5b1d` |
| `usr/palm/applications/com.beb.plxnative/plxnative` | 8 247 904 | `0755` | root:root | `d5527ea23cafc5fa` |
| `usr/palm/applications/com.beb.plxnative/resources/de/appinfo.json` | 121 | `0644` | root:root | `b99d7d0a4dcec269` |
| `usr/palm/applications/com.beb.plxnative/resources/es/appinfo.json` | 108 | `0644` | root:root | `7a1a661a8fb32705` |
| `usr/palm/applications/com.beb.plxnative/resources/fr/appinfo.json` | 104 | `0644` | root:root | `99cdb882afc49208` |
| `usr/palm/applications/com.beb.plxnative/resources/it/appinfo.json` | 106 | `0644` | root:root | `740304fde57c5f1f` |
| `usr/palm/applications/com.beb.plxnative/resources/ja/appinfo.json` | 140 | `0644` | root:root | `0d37e380dfb76ab4` |
| `usr/palm/applications/com.beb.plxnative/resources/ko/appinfo.json` | 134 | `0644` | root:root | `a798bbe139304277` |
| `usr/palm/applications/com.beb.plxnative/resources/nl/appinfo.json` | 107 | `0644` | root:root | `33e3f2e2a2e64dff` |
| `usr/palm/applications/com.beb.plxnative/resources/pl/appinfo.json` | 106 | `0644` | root:root | `9f73431888bf6759` |
| `usr/palm/applications/com.beb.plxnative/resources/pt/appinfo.json` | 112 | `0644` | root:root | `fc2cb8567bac914f` |
| `usr/palm/applications/com.beb.plxnative/resources/ru/appinfo.json` | 140 | `0644` | root:root | `97d9f269c96c5b38` |
| `usr/palm/applications/com.beb.plxnative/resources/sv/appinfo.json` | 107 | `0644` | root:root | `690f9ffe3b72c9e7` |
| `usr/palm/applications/com.beb.plxnative/resources/tr/appinfo.json` | 117 | `0644` | root:root | `990aabb413e146e5` |
| `usr/palm/applications/com.beb.plxnative/sentry-crash` | 514 776 | `0755` | root:root | `0e1617485cee8890` |
| `usr/palm/applications/com.beb.plxnative/splash.png` | 81 929 | `0644` | root:root | `b07abcf1b71e7b4d` |
| `usr/palm/packages/com.beb.plxnative/packageinfo.json` | 173 | `0644` | root:root | `11d1fa0c87fdc4fc` |

### Linkage

`plxnative` is ELF32 / ARM, with **15 `DT_NEEDED` entries**:

```
libSDL2-2.0.so.0
libSDL2_ttf-2.0.so.0
libGLESv2.so.2
libluna-service2.so.3
libglib-2.0.so.0
libwayland-client.so.0
libplayerAPIs.so.1
libpf-1.0.so.1
libdl.so.2
librt.so.1
libpthread.so.0
libm.so.6
libgcc_s.so.1
libc.so.6
ld-linux.so.3
```

`ci/check-elf.sh` asserts this list against `ci/expected-dt-needed.txt` on every build: they agree. Two library families are deliberately absent because their SONAME moves between firmwares and a `DT_NEEDED` entry cannot express "either of these" — libcurl and libAcbAPI are `dlopen`ed by candidate list instead (`rust-modules/src/dynlib.rs`, `src/starfish.c`).

Shared libraries shipped **beside** the binary and opened by absolute path out of the app's own directory, so they can neither shadow nor be shadowed by the television's own FFmpeg:

| file | identity |
|---|---|
| `libavcodec-plx.so.63` | SONAME `libavcodec-plx.so.63`, 441 112 bytes, sha256 `ec14fafb73febeea` |
| `libavformat-plx.so.63` | SONAME `libavformat-plx.so.63`, 445 272 bytes, sha256 `cbf5763941a3f98c` |
| `libavutil-plx.so.61` | SONAME `libavutil-plx.so.61`, 452 796 bytes, sha256 `813fcbe68d041ee7` |

### FFmpeg and the LGPL position

| field | value |
|---|---|
| corresponding source | `ffmpeg-9.0.tar.xz` attached to this release, sha256 `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| build script | `build-ffmpeg.sh` attached, sha256 `7b49d924ae7c673f` |
| upstream tarball pinned in the script | `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| copyleft-widening flags | none of `--enable-gpl`, `--enable-nonfree`, `--enable-version3` (comments excluded from the scan) |
| licence texts in the payload | `Apache-2.0.txt`, `LGPL-2.1.txt`, `LICENSE`, `LLVM-exception.txt`, `MIT.txt`, `TRADEMARKS.md`, `Unicode-3.0.txt`, `Zlib.txt` |
| `THIRD-PARTY-NOTICES.md` in the payload | yes |
| `LICENSE` in the payload | yes |
| `TRADEMARKS.md` in the payload | yes |

The configure invocation FFmpeg records inside `libavutil`, verbatim — this is the primary evidence for both the licence position and the reproducibility scan above:

```
--prefix=/plx --enable-cross-compile --cross-prefix=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/bin/arm-webos-linux-gnueabi- --host-cc=cc --arch=arm --cpu=cortex-a9 --target-os=linux --sysroot=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/arm-webos-linux-gnueabi/sysroot --build-suffix=-plx --enable-shared --disable-static --enable-pic --extra-cflags='-Dstatic_assert=_Static_assert' --disable-programs --disable-doc --disable-avdevice --disable-avfilter --disable-network --disable-swresample --disable-debug --enable-small --disable-everything --disable-autodetect --enable-demuxer='matroska,mov,mpegts,h264,hevc' --enable-parser='h264,hevc,aac,ac3,dvdsub,dvbsub' --enable-bsf='h264_mp4toannexb,hevc_mp4toannexb,extract_extradata' --enable-decoder='pgssub,dvdsub,dvbsub,ass,srt,subrip,text,mov_text,webvtt' --enable-protocol=file --disable-swscale
```

### Verification gates

| gate | verdict |
|---|---|
| `tag / version agreement` | success |
| `redistributable assets` (LICENSE, notices, font family) | success |
| `build + verify` | success |
| `ci/check-elf.sh` — ELF identity, CP15 barriers, DT_NEEDED | pass |
| `ci/check-package.py` — versions, ar layout, descriptors, payload, notes | pass |
| the Homebrew Channel manifest's sha256 matches the artifact | pass |
| LGPL corresponding source attached (ffmpeg-9.0.tar.xz + build-ffmpeg.sh) | pass |

### Static firmware compatibility

This grades whether the process STARTS: it resolves the binary's libraries and undefined symbols against webosbrew's firmware inventories. It says nothing about whether video plays. Playback evidence is in the authored half above.

```
## Package com.beb.plxnative

### App com.beb.plxnative

|           | 4.4.2 | 4.10.0 | 5.3.1 | 6.4.0 | 7.4.0 | 8.3.0 | 9.2.0 | 10.2.0 | 11.2.0 |
|-----------|-------|--------|-------|-------|-------|-------|-------|--------|--------|
| plxnative | :ok:  | :ok:   | :ok:  | :ok:  | :ok:  | :ok:  | :ok:  | :ok:   | :ok:   |


#### plxnative

All OK
```

### What the public can download

`ci/verify-published.sh` re-derives every claim from the **published** assets after the release exists — the hash in every place it appears, `shasum -c` working where a user stands, CI rather than a person as the uploader, and no payload file carrying a build machine's paths.

```
== what the public downloads — v0.6.4 ==
  ok    the .ipk is published
  ok    shasum -c works where the two assets land side by side
  ok    checksum file matches the artifact
  ok    manifest hash and size match the artifact
  ok    the release note quotes this hash
  ok    assets uploaded by CI
  ok    no payload file carries a build-machine path

7 passed, 0 failed, 0 to look at
```

<!-- END GENERATED -->
