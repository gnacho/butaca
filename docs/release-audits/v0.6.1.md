# Release audit — v0.6.1

The evidence behind [the v0.6.1 release note](https://github.com/GLinnik21/plx-native/blob/v0.6.1/docs/release-notes/v0.6.1.md). Written for a webosbrew reviewer, a contributor auditing an old release, and us in a year. The section at the bottom is generated from the artifact that was published; everything above it was written by a person before the release ran. The generated block is filled in by `.github/workflows/release.yml`'s own last step, AFTER the `v0.6.1` tag is cut, and committed to this maintenance line's own branch (`release/v0.6`) rather than to the tag — so the tagged tree can never carry it. Read this document at `blob/release/v0.6/docs/release-audits/v0.6.1.md`, not at `blob/v0.6.1/…`, if the generated section matters to you.

## What this release contains

Two independent fixes, from two different reports.

- **Issue #74 — a playback crash at the start of any video**, on an LG 43UM7400 (2019, Realtek `k5lp` chassis). Covered end to end below (Device test evidence, External compatibility reports, Known issues).
- **Issue #75 — a sign-in screen that gave no reason when it could not reach plex.tv**, reported on a 2024 LG OLED with no log and no telemetry. `net.rs` now records the outcome of the last call to the plex.tv host; `auth.rs` turns that into a bounded, identifier-free sentence (`link_detail`) covering consecutive unanswered polls, elapsed time and the last outcome; `ui/login.rs` shows that sentence and a permanent build/webOS-line/set footer on every sign-in read-out. A failed or stuck sign-in can now be reported: automatically, as a handled `SignInError` Sentry event, when crash-report consent is already on; otherwise through a one-off "Send report" decision alert (`queue::Category::OneOff`) that sends a single identifier-free event, can be declined, and is exempt from the account-wide consent switches by design. `diag` defers the sign-in analytics events while consent is unanswered and replays or drops them once it is answered. `PRIVACY.md`'s `POLICY_VERSION` moved to 5 for this addition, so every existing 0.6.x install is asked the reporting question again on first launch of 0.6.1. See Device test evidence and External compatibility reports below for what has and has not been verified.

## Device test evidence

| | |
|---|---|
| television | dev set: LG 49SM9000PLA, board `m16p3`, platform release 4.10.2 — not the reporter's `k5lp`/`k3lp` board; see below for what a run on it can and cannot confirm |
| server | server-tier cases against a real PMS on the LAN; synthetic pipeline-tier cases need no server |
| suite | `cargo +nightly test --lib` (default features) — 2110 passed, 0 failed |
| suite | `cargo +nightly test --lib --features hostsim` — 2139 passed, 0 failed |
| suite | `./tests/run.py` (synthetic pipeline tier, no Plex) — `pipe_h264_ac3_1080p`, `pipe_abr_steady_modest_link`, `pipe_seek_inplace` — 3 cases, PASS, 2026-09-10 |
| suite | `./tests/run.py --server` — `transcode_av1_no_dp_audio`, `seek_transcode` — 2 cases, PASS, 2026-09-10 |
| perf | `presented_rate` graded on every case above and passed (24.0-24.1 fps mean); no `--fps` regression scenes run this cycle |
| by hand | none beyond the harness runs above |
| sign-in (#75) | host suite (both feature sets, `--no-default-features` compiles), the desktop simulator, and one dev-set look on 2026-09-10 with `plxnative-signinfail=error`: the failed read-out with the new reachability line and footer, and the "Send report" alert over it, drawn cleanly by LG's rasterizer — pass (a screen look, not a reproduction of the report's failure) |

**What was verified, and what was not, stated plainly rather than blurred together.** The host suite for the final tree ran clean: `make check` — 2110 default-feature tests and 2139 hostsim tests, 0 failed — plus `CARGO_INCREMENTAL=0 cargo +nightly check --no-default-features`, clean. P1a's new in-flight-dispatch regression test was watched RED against the unfixed v0.6.0 gate first (`left: 51, right: 0` — 51 seam dispatches landed while Load was still in flight, the first of them the `isLoadCompleted` poll) and GREEN after the fix, per this project's reproduce-before-fixing rule. A fw-compat review found `tools/fwcompat.py` OK on every release from 4.4.2 through 11.2.0, and `ci/check-elf.sh` clean with `DT_NEEDED` unchanged at 15 entries. `make FLAVOR=stable RELEASE=1 ipk` followed by `python3 ci/check-package.py` passed every assertion locally.

**A device run did happen this cycle, on 2026-09-10, on the maintainer's own dev set** — the LG 49SM9000PLA above, debug-flavour install, panel off, under the TV lock. Three synthetic pipeline-tier cases passed: `pipe_h264_ac3_1080p` (19 s), `pipe_abr_steady_modest_link` (the full 75 s cap; its segmented HLS is the priming path the fix targets) and `pipe_seek_inplace`. Two server-tier cases passed against a real PMS: `transcode_av1_no_dp_audio` (a real HLS transcode — the reporter's own route into the crash) and `seek_transcode` (a fresh Load on seek). Every log from this run shows the new ordering the fix installs — `native: loadCompleted while Load in flight — deferring` → `SMP: Load returned ok=1` → `native: Load returned after Nms` → `SMP loadCompleted` → Play (or priming) → first feed → `SMP ACB bound` — and the measured in-flight window on this chassis was 874 ms for direct play, 359 ms for segmented HLS and 176 ms for the PMS transcode: the race P1a's gate closes is real on this board too, not only on the reporter's. `devjail: soc=m16p3 rtkmem=n/a` was logged on every boot, as expected on a non-Realtek board.

**The set this team has physical access to is a different board from the reporter's, and this run answers a correspondingly narrower question.** `m16p3` does not have `/dev/rtkmem` missing from its jail and cannot exercise P1b's guard at all — every boot simply logged `devjail: soc=m16p3 rtkmem=n/a` and moved on. This run confirms P1a's sequencing gate does not regress ordinary playback on real hardware, across direct play, segmented HLS and a server transcode; it says nothing about whether P1b's k5lp/k3lp guard, or the underlying crash it works around, is actually fixed on the chassis that reported it. These are two different claims and this audit does not conflate them: **P1a is device-confirmed on the m16p3 set; P1b is not, on any hardware this team holds — its `missing` branch has never been exercised, only its `n/a` branch.** The reporter's own set remains the only place P1b can be confirmed; what to look for there is a `devjail:` line naming a `k5lp`/`k3lp` SoC with `rtkmem=missing`, and whether `SMP: Load returned` now appears within the 20 s budget where issue #74's own log shows it never arriving. The by-hand run with the new `plxnative-holdload` trigger was also done on the `m16p3` set the same day: the pump logged its `deferring` line while the flag was held, the 20 s budget expired into `load_failed`, and the failure read-out was drawn on the panel (`docs/measurements/issue-74-holdload-readout-tv-2026-09-10.png`) — which exercises D.1's budget and read-out path on real hardware without needing the affected chassis.

**The sign-in change (issue #75) was looked at on the dev set on 2026-09-10, after the host suite and the desktop simulator.** With `plxnative-signinfail=error` armed the `m16p3` set drew the failed read-out — the "Couldn’t sign in" heading, the error sentence, the Try again button, the new reachability line (`plex.tv is not answering — couldn't resolve host (curl 6), 1 try, 3 s`) and the new version/firmware footer — and, over it, the "Send a report about this sign-in problem?" alert with its full disclosure text and both buttons. LG's rasterization of every new line is clean at glyph size and the alert's layout over the sign-in route holds on real hardware (`docs/measurements/issue-75-signin-alert-tv-2026-09-10.png`, `docs/measurements/issue-75-signin-readout-tv-2026-09-10.png`). This is a look at the screen, not a reproduction of the report: the dev set reaches plex.tv, so the failure was injected by the dev trigger, and nothing here says the reporter's 2024 OLED now signs in.

## External compatibility reports known at release time

Reporter evidence was available for this release. [GitHub issue #74](https://github.com/GLinnik21/plx-native/issues/74) carries three attached logs from the failing set — `plxnative-events.log`, `plxnative-crash.log` and `plxnative-stderr.log` — plus a Sentry event (`PLX-NATIVE-E`) from a later attempt. The attached logs show a SIGSEGV in libc with `r0=0`, occurring immediately after `SMP loadCompleted (priming before Play)` with no `SMP: Load returned` line ever appearing — exactly the in-flight race P1a's sequencing gate closes. The Sentry event is a different failure mode from a later attempt on the same set: a SIGABRT on the firmware's `OMX_VideoComp` thread, with the main thread inside `StarfishMediaAPIs::Play` at `pump.rs:1096` and the load thread still inside `Load`, at `gst_dvo_context_start` → `DirectVoInit` → `DirectVoRelease`, both blocked in `pthread_mutex_lock`. The set reports platform release 4.10.0 (codename `goldilocks2-grampians`, api `4.1.0`), board `K5LP_DVB`, model `k5lp` — LG's own settings screen shows it as software version `05.40.20` — installed under `/media/developer` (Developer Mode). No firmware tier moves on this report: it is recorded here as the origin and reproduction evidence for the fix, not as a compatibility confirmation, since the fix itself has not been run on this reporter's own hardware. Neither the reporter nor any host is named here, per this project's third-party-data rule.

[GitHub issue #75](https://github.com/GLinnik21/plx-native/issues/75) is a 2024 LG OLED that could not sign in with a PIN, with no log and no telemetry attached — the sign-in screen gave the reporter nothing to go on, and the app had nothing to send even if it had. The dev-set flow itself works on webOS 10.3.1 (the lab's K24 chassis), so the reporter's failure is most likely a plex.tv reachability problem the old screen could not distinguish from "you have not scanned yet". This report is recorded here as the motivation for the change — the reason the screen now says why plex.tv is not answering and the reason a failed sign-in can be reported — not as a compatibility confirmation of any kind: the fix has not been run on the reporter's own set, and no firmware tier moves on it.

## Compatibility tiers, and what moved

Nothing moved. This release's compatibility block is copied verbatim from v0.6.0's, per this project's release-notes standard for a maintenance-line hotfix: no new television and no new report arrived between v0.6.0 and v0.6.1, so the tiers stay exactly where v0.6.0 left them. See v0.6.0's own [tiers and evidence](https://github.com/GLinnik21/plx-native/blob/main/docs/release-audits/v0.6.0.md#compatibility-tiers-and-what-moved) for how each line got there.

## Known issues at release, with provenance

- **P1b's k5lp/k3lp jail guard is unverified on the chassis it targets.** It is built from community-tier evidence — [webosbrew/webos-homebrew-channel PR #202](https://github.com/webosbrew/webos-homebrew-channel/pull/202) and ss4s's own `/dev/rtkmem` detection — not a vendor statement, and this team holds no `k5lp`/`k3lp` hardware to confirm it against. See Device test evidence above.
- **P1a's sequencing gate has host-suite coverage (RED then GREEN, per the fix's own commit) and has now been run on the m16p3 dev set (2026-09-10):** five cases across both harness tiers passed, the new dispatch-deferral ordering appears in every log, and `presented_rate` stayed healthy throughout. The race it closes is general — it does not depend on chassis — so this confirms P1a does not regress ordinary playback; it says nothing about P1b, which this board cannot exercise.
- Every known issue carried in [v0.6.0's audit](https://github.com/GLinnik21/plx-native/blob/main/docs/release-audits/v0.6.0.md#known-issues-at-release-with-provenance) is unchanged by this release; none of it was re-measured this cycle.
- **The sign-in reachability fix (issue #75) is unconfirmed against the reporting set.** Its screens were looked at on the dev television (see Device test evidence) with the failure injected by a dev trigger, which proves the rendering and the alert's layout on real hardware and nothing about the reporter's network. The reporter's own set — a 2024 LG OLED — is unavailable to this team, so whether that set now signs in, or now sends a usable report, is only answerable there.

## Deliberately not done

- **P1b was not, and could not be, confirmed on real hardware for this release.** A device run did happen on 2026-09-10 (see Device test evidence), but only on a board this team holds that is not `k5lp`/`k3lp` — its `rtkmem=missing` branch has never been exercised on any hardware here. Confirming it needs the reporter's own chassis or one like it. The deferral-and-budget path itself WAS exercised by hand on the same set the same day, with the new `plxnative-holdload` trigger holding the Load-returned flag for 30 s under a `plxnative-playurl` direct-play clip: the log read `native: loadCompleted while Load in flight — deferring` → `SMP: Load returned ok=1` → `holdload: armed — holding the Load-returned flag for 30000ms` → `native: Load did not return within 20s — publishing load_failed` → (no Play, no feed) → `native: Load returned after 30255ms`, and the panel capture showed the full-screen failure read-out ("Playback failed — This TV could not start the video stream", footer `PlxNative 0.6.2-dev · webOS 4.10.2 · m16p3 · … · tv_pipeline`) rather than a spinner — the capture is [docs/measurements/issue-74-holdload-readout-tv-2026-09-10.png](https://github.com/GLinnik21/plx-native/blob/release/v0.6/docs/measurements/issue-74-holdload-readout-tv-2026-09-10.png). That proves the budget lands on the read-out on real hardware; it says nothing about why Load would take that long on a k5lp.
- **The reporter's exact set was not sought out or acquired.** Confirming P1b against the actual failure requires `k5lp`/`k3lp` hardware this team does not have; buying or borrowing one was out of scope for this release.
- **Issue #75's reporting set was likewise not sought out or acquired**; the fix was reproduced red-then-green on the host suite and the simulator, and its screens were given one injected-failure look on the dev set rather than a real unreachable-plex.tv run, which would have meant cutting the household's uplink for a screen the trigger already reaches.
- **The consent re-ask on first launch of 0.6.1 is deliberate, not a side effect.** `POLICY_VERSION` moved from 4 to 5 because the sign-in report is a newly-collected event this project's own rule (`telemetry/consent.rs`) requires a bump for: a bump asks every existing install again rather than silently extending a previous answer to cover a form of collection it was never asked about. It costs every returning user one more screen on their next launch; that cost was accepted rather than worked around.
- **Four files sit outside every package's declared file-ownership list, and were left there rather than reverted.** `rust-modules/src/plex/identity.rs` (making `version_is_the_package_or_the_next_minor_dev` line-aware, so it does not fail once `RELEASE_LINE` exists), `rust-modules/src/lib.rs` and the new `rust-modules/src/release_line.rs` (the version-derivation helpers moved into the crate so a host unit test could exercise them, per P2's own requirement), and `rust-modules/src/app.rs` (the `crate::player::clear_jail_refusal_for_route_exit()` call added to `exit_player`, so a jail refusal retires when the viewer leaves the player route instead of latching `PlaybackState::Error` onto every later screen) are all necessary consequences of a required fix rather than opportunistic changes, so undoing them to fit the ownership list would either reintroduce the bug the fix closes or remove the coverage the spec asked for. Recorded here rather than reverted.

## Release configuration

| | |
|---|---|
| flavour | `com.beb.plxnative` (stable) — the id users install; `FLAVOR: stable` is pinned in the release workflow's build job |
| cargo features | `RELEASE=1`, which drops `devtools` and `devtriggers` |
| built by | GitHub Actions, from the tag, at a path identical on every runner |
| toolchain | webOS NDK, Rust nightly pinned by date in the `RUST_NIGHTLY` repository variable — unchanged from v0.6.0 |

This is the first release cut from a maintenance line rather than from `main`: a tracked `RELEASE_LINE` marker (`0.6`) at the repo root now says this checkout is the v0.6 line, and `rust-modules/build.rs` reports a developer build's version as the next patch (`0.6.2-dev`) rather than the next minor while it is present. Nothing else about the release configuration changed from v0.6.0.

<!-- BEGIN GENERATED — ci/gen-release-audit.py. Do not edit by hand. -->

*Every value below was read out of `com.beb.plxnative_0.6.1_arm.ipk` by `ci/gen-release-audit.py`. Nothing in this block was typed by a person.*

### Identity and provenance

| field | value |
|---|---|
| tag | `v0.6.1` |
| commit | `68212dd5cc52cdf56c785c5a5645bb80fe90aeae` |
| workflow run | https://github.com/GLinnik21/plx-native/actions/runs/34436003111 |
| asset uploader | `github-actions[bot]` |
| built by | GitHub Actions (`.github/workflows/release.yml`, job `build + verify`) |

### The package

| field | value |
|---|---|
| filename | `com.beb.plxnative_0.6.1_arm.ipk` |
| app id | `com.beb.plxnative` |
| version | `0.6.1` |
| type | `native` |
| sha256 | `2a1944ce11076d7c7eb74c9138820ef83f483ef72f3c0e71e07f480bba22635c` |
| download size | 16 731 526 bytes |
| installed size | 31 318 KiB (control `Installed-Size`) |
| ar members | `debian-binary` (4 B), `control.tar.gz` (397 B), `data.tar.gz` (16 730 935 B) |

**Hash agreement:** all 3 copies agree.

| source | sha256 |
|---|---|
| the artifact's own sha256 | `2a1944ce11076d7c7eb74c9138820ef83f483ef72f3c0e71e07f480bba22635c` |
| `com.beb.plxnative.manifest.json` → `ipkHash.sha256` | `2a1944ce11076d7c7eb74c9138820ef83f483ef72f3c0e71e07f480bba22635c` |
| `ipk.sha256` | `2a1944ce11076d7c7eb74c9138820ef83f483ef72f3c0e71e07f480bba22635c` |

`ipk.sha256` names `com.beb.plxnative_0.6.1_arm.ipk`, so `shasum -a 256 -c ipk.sha256` works in the directory a user downloads into.

The Homebrew Channel manifest declares `ipkSize` 16 731 526 bytes and `installedSize` 31 318 KiB; the size it declares matches the artifact. `rootRequired` is `false`.

### Control file

| field | value |
|---|---|
| `Architecture` | arm |
| `Description` | PlxNative — unofficial native Plex client for LG webOS (not affiliated with Plex) |
| `Homepage` | https://github.com/GLinnik21/plx-native |
| `Installed-Size` | 31318 |
| `License` | MIT |
| `Maintainer` | Gleb Linnik <23104281+GLinnik21@users.noreply.github.com> |
| `Package` | com.beb.plxnative |
| `Priority` | optional |
| `Section` | misc |
| `Version` | 0.6.1 |
| `webOS-Package-Format-Version` | 2 |
| `webOS-Packager-Version` | plxnative-mkipk/1 |

### Build configuration

| field | value |
|---|---|
| flavour | `com.beb.plxnative` — the stable id, which is what users install |
| cargo features | `features:--no-default-features+symbols+tel:78a04a1fd0f5` |
| dev-trigger surface | absent — `plxnative-noidle`×0, `plxnative-remote`×0, `plxnative-capture`×0, `plxnative-drawmask`×0, `plxnative-heroground`×0 |
| telemetry endpoints | Sentry `https://30574b7659ec4fcdc39d6b3072deace8@o4511994943700992.ingest.de.sentry.io/4511994955890768` (EU); PostHog `phc_kVAmK72TzZXH4VwLkcJuJs5tqAgWh97GRqaSpR9cKn3M` — write-only ingest credentials, publishable by design |

The witnesses are literals from `dev.rs`'s `DIAG` array, which is `#[cfg(feature = "devtriggers")]`; two of them name the surfaces a reviewer reading this repository's source would rightly ask about — the world-writable `plxnative-remote` FIFO that can drive the UI, and the unauthenticated TCP capture listener. Zero of all five is what `RELEASE=1` looks like in the bytes, and it is a property of this package rather than of the command line that produced it. Each witness has been measured from both sides (0 here, 1-2 in a dev build), because a witness that cannot fail is not evidence.

### Package surface

| field | value |
|---|---|
| declared permissions | none — `appinfo.json` declares no `requiredPermissions` |
| `requiredMemory` | 160 MB |
| `id` prefix | not one of LG's reserved prefixes |
| localized descriptors | 12 locales |

**Host-shaped strings in the shipped binary.** A floor, not a proof: a string here may be a log message rather than a destination, and a host assembled at runtime would not appear at all.

```
answeringplex.tv
eu.i.posthog.com
github.com
index.crates.io
ns.adobe.com
o4511994943700992.ingest.de.sentry.io
plex.tv
plxnative.com
www.plex.tv
www.w3.org
yetplex.tv
```

**Absolute paths outside the app's own directory, same caveat.**

```
/dev/null
/dev/random
/dev/rtkmem
/dev/rtkmem/etc/prefs/properties/machine
/dev/urandom
/etc/os-release
/etc/umediaserver/device_codec_capability_config.json
/etc/xdg
/media/cryptofsunknowndevmodeapplications/media/developer/apps/usr/
/media/developer
/media/developer/
/media/developer/apps/usr/palm/applications/com.beb.plxnativeseason
/media/internal/.
/proc/self/auxv
/proc/self/cgroupcpu.cfs_period_us/sys/fs/cgroupcgroup.controllers
/proc/self/exe
/proc/self/fdfatal
/proc/self/maps
/proc/self/mountinfocgroup/
/proc/sys/kernel/random/uuidplayback
/tmp/%s
/tmp/plxnative-dvnonode
/tmp/plxnative-profile
/tmp/plxnative-url
/var/run/nyx/device_info.json
/var/run/nyx/os_info.json
```

### Reproducibility evidence

No payload file carries a build machine's directory layout (all 35 files scanned; the NDK's own `--cross-prefix`, which FFmpeg records and which is identical on every runner, is the one allowed exception). That is what makes two builds of this commit on one machine byte-identical. It is not yet a cross-machine claim.

### Payload inventory

| path | bytes | mode | owner | sha256 (first 16) |
|---|---|---|---|---|
| `usr/palm/applications/com.beb.plxnative/OFL.txt` | 4 377 | `0644` | root:root | `5b9321a4298cfeb6` |
| `usr/palm/applications/com.beb.plxnative/THIRD-PARTY-NOTICES.md` | 24 025 | `0644` | root:root | `f5328c1cb3bee9e2` |
| `usr/palm/applications/com.beb.plxnative/appfont-bold.ttf` | 408 976 | `0644` | root:root | `b30d8082184474b4` |
| `usr/palm/applications/com.beb.plxnative/appfont-cjk.ttf` | 21 185 868 | `0644` | root:root | `eeb1b524ac15a8bb` |
| `usr/palm/applications/com.beb.plxnative/appfont.ttf` | 407 448 | `0644` | root:root | `4ef24f0bac98d3a6` |
| `usr/palm/applications/com.beb.plxnative/appinfo.json` | 452 | `0644` | root:root | `4a44fb4d6234e7db` |
| `usr/palm/applications/com.beb.plxnative/icon.png` | 3 972 | `0644` | root:root | `d70bcff921afb66f` |
| `usr/palm/applications/com.beb.plxnative/largeIcon.png` | 9 716 | `0644` | root:root | `dc169758d9bc7fa9` |
| `usr/palm/applications/com.beb.plxnative/libavcodec-plx.so.63` | 441 112 | `0644` | root:root | `ec14fafb73febeea` |
| `usr/palm/applications/com.beb.plxnative/libavformat-plx.so.63` | 445 272 | `0644` | root:root | `cbf5763941a3f98c` |
| `usr/palm/applications/com.beb.plxnative/libavutil-plx.so.61` | 452 796 | `0644` | root:root | `813fcbe68d041ee7` |
| `usr/palm/applications/com.beb.plxnative/licenses/Apache-2.0.txt` | 10 280 | `0644` | root:root | `074e6e32c86a4c0e` |
| `usr/palm/applications/com.beb.plxnative/licenses/LGPL-2.1.txt` | 26 419 | `0644` | root:root | `20e50fe7aae3e563` |
| `usr/palm/applications/com.beb.plxnative/licenses/LICENSE` | 1 068 | `0644` | root:root | `3b297a24aa928a85` |
| `usr/palm/applications/com.beb.plxnative/licenses/LLVM-exception.txt` | 919 | `0644` | root:root | `e34c58338bd89d43` |
| `usr/palm/applications/com.beb.plxnative/licenses/MIT.txt` | 1 078 | `0644` | root:root | `b05785f9f18e6716` |
| `usr/palm/applications/com.beb.plxnative/licenses/TRADEMARKS.md` | 2 722 | `0644` | root:root | `9126d07ee3b66ac2` |
| `usr/palm/applications/com.beb.plxnative/licenses/Unicode-3.0.txt` | 1 995 | `0644` | root:root | `f7db81051789b729` |
| `usr/palm/applications/com.beb.plxnative/licenses/Zlib.txt` | 838 | `0644` | root:root | `bfb1112d49db5b1d` |
| `usr/palm/applications/com.beb.plxnative/plxnative` | 8 041 880 | `0755` | root:root | `d70cec117cc24a12` |
| `usr/palm/applications/com.beb.plxnative/resources/de/appinfo.json` | 121 | `0644` | root:root | `b99d7d0a4dcec269` |
| `usr/palm/applications/com.beb.plxnative/resources/es/appinfo.json` | 108 | `0644` | root:root | `7a1a661a8fb32705` |
| `usr/palm/applications/com.beb.plxnative/resources/fr/appinfo.json` | 104 | `0644` | root:root | `99cdb882afc49208` |
| `usr/palm/applications/com.beb.plxnative/resources/it/appinfo.json` | 106 | `0644` | root:root | `740304fde57c5f1f` |
| `usr/palm/applications/com.beb.plxnative/resources/ja/appinfo.json` | 140 | `0644` | root:root | `0d37e380dfb76ab4` |
| `usr/palm/applications/com.beb.plxnative/resources/ko/appinfo.json` | 134 | `0644` | root:root | `a798bbe139304277` |
| `usr/palm/applications/com.beb.plxnative/resources/nl/appinfo.json` | 107 | `0644` | root:root | `33e3f2e2a2e64dff` |
| `usr/palm/applications/com.beb.plxnative/resources/pl/appinfo.json` | 106 | `0644` | root:root | `9f73431888bf6759` |
| `usr/palm/applications/com.beb.plxnative/resources/pt/appinfo.json` | 112 | `0644` | root:root | `fc2cb8567bac914f` |
| `usr/palm/applications/com.beb.plxnative/resources/ru/appinfo.json` | 140 | `0644` | root:root | `97d9f269c96c5b38` |
| `usr/palm/applications/com.beb.plxnative/resources/sv/appinfo.json` | 107 | `0644` | root:root | `690f9ffe3b72c9e7` |
| `usr/palm/applications/com.beb.plxnative/resources/tr/appinfo.json` | 117 | `0644` | root:root | `990aabb413e146e5` |
| `usr/palm/applications/com.beb.plxnative/sentry-crash` | 514 776 | `0755` | root:root | `0e1617485cee8890` |
| `usr/palm/applications/com.beb.plxnative/splash.png` | 81 929 | `0644` | root:root | `b07abcf1b71e7b4d` |
| `usr/palm/packages/com.beb.plxnative/packageinfo.json` | 173 | `0644` | root:root | `91c7ec78f9613810` |

### Linkage

`plxnative` is ELF32 / ARM, with **15 `DT_NEEDED` entries**:

```
libSDL2-2.0.so.0
libSDL2_ttf-2.0.so.0
libGLESv2.so.2
libluna-service2.so.3
libglib-2.0.so.0
libwayland-client.so.0
libplayerAPIs.so.1
libpf-1.0.so.1
libdl.so.2
librt.so.1
libpthread.so.0
libm.so.6
libgcc_s.so.1
libc.so.6
ld-linux.so.3
```

`ci/check-elf.sh` asserts this list against `ci/expected-dt-needed.txt` on every build: they agree. Two library families are deliberately absent because their SONAME moves between firmwares and a `DT_NEEDED` entry cannot express "either of these" — libcurl and libAcbAPI are `dlopen`ed by candidate list instead (`rust-modules/src/dynlib.rs`, `src/starfish.c`).

Shared libraries shipped **beside** the binary and opened by absolute path out of the app's own directory, so they can neither shadow nor be shadowed by the television's own FFmpeg:

| file | identity |
|---|---|
| `libavcodec-plx.so.63` | SONAME `libavcodec-plx.so.63`, 441 112 bytes, sha256 `ec14fafb73febeea` |
| `libavformat-plx.so.63` | SONAME `libavformat-plx.so.63`, 445 272 bytes, sha256 `cbf5763941a3f98c` |
| `libavutil-plx.so.61` | SONAME `libavutil-plx.so.61`, 452 796 bytes, sha256 `813fcbe68d041ee7` |

### FFmpeg and the LGPL position

| field | value |
|---|---|
| corresponding source | `ffmpeg-9.0.tar.xz` attached to this release, sha256 `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| build script | `build-ffmpeg.sh` attached, sha256 `7b49d924ae7c673f` |
| upstream tarball pinned in the script | `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| copyleft-widening flags | none of `--enable-gpl`, `--enable-nonfree`, `--enable-version3` (comments excluded from the scan) |
| licence texts in the payload | `Apache-2.0.txt`, `LGPL-2.1.txt`, `LICENSE`, `LLVM-exception.txt`, `MIT.txt`, `TRADEMARKS.md`, `Unicode-3.0.txt`, `Zlib.txt` |
| `THIRD-PARTY-NOTICES.md` in the payload | yes |
| `LICENSE` in the payload | yes |
| `TRADEMARKS.md` in the payload | yes |

The configure invocation FFmpeg records inside `libavutil`, verbatim — this is the primary evidence for both the licence position and the reproducibility scan above:

```
--prefix=/plx --enable-cross-compile --cross-prefix=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/bin/arm-webos-linux-gnueabi- --host-cc=cc --arch=arm --cpu=cortex-a9 --target-os=linux --sysroot=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/arm-webos-linux-gnueabi/sysroot --build-suffix=-plx --enable-shared --disable-static --enable-pic --extra-cflags='-Dstatic_assert=_Static_assert' --disable-programs --disable-doc --disable-avdevice --disable-avfilter --disable-network --disable-swresample --disable-debug --enable-small --disable-everything --disable-autodetect --enable-demuxer='matroska,mov,mpegts,h264,hevc' --enable-parser='h264,hevc,aac,ac3,dvdsub,dvbsub' --enable-bsf='h264_mp4toannexb,hevc_mp4toannexb,extract_extradata' --enable-decoder='pgssub,dvdsub,dvbsub,ass,srt,subrip,text,mov_text,webvtt' --enable-protocol=file --disable-swscale
```

### Verification gates

| gate | verdict |
|---|---|
| `tag / version agreement` | success |
| `redistributable assets` (LICENSE, notices, font family) | success |
| `build + verify` | success |
| `ci/check-elf.sh` — ELF identity, CP15 barriers, DT_NEEDED | pass |
| `ci/check-package.py` — versions, ar layout, descriptors, payload, notes | pass |
| the Homebrew Channel manifest's sha256 matches the artifact | pass |
| LGPL corresponding source attached (ffmpeg-9.0.tar.xz + build-ffmpeg.sh) | pass |

### Static firmware compatibility

This grades whether the process STARTS: it resolves the binary's libraries and undefined symbols against webosbrew's firmware inventories. It says nothing about whether video plays. Playback evidence is in the authored half above.

```
## Package com.beb.plxnative

### App com.beb.plxnative

|           | 4.4.2 | 4.10.0 | 5.3.1 | 6.4.0 | 7.4.0 | 8.3.0 | 9.2.0 | 10.2.0 | 11.2.0 |
|-----------|-------|--------|-------|-------|-------|-------|-------|--------|--------|
| plxnative | :ok:  | :ok:   | :ok:  | :ok:  | :ok:  | :ok:  | :ok:  | :ok:   | :ok:   |


#### plxnative

All OK
```

### What the public can download

`ci/verify-published.sh` re-derives every claim from the **published** assets after the release exists — the hash in every place it appears, `shasum -c` working where a user stands, CI rather than a person as the uploader, and no payload file carrying a build machine's paths.

```
== what the public downloads — v0.6.1 ==
  ok    the .ipk is published
  ok    shasum -c works where the two assets land side by side
  ok    checksum file matches the artifact
  ok    manifest hash and size match the artifact
  ok    the release note quotes this hash
  ok    assets uploaded by CI
  ok    no payload file carries a build-machine path

7 passed, 0 failed, 0 to look at
```

<!-- END GENERATED -->

## Updates to this audit

**2026-09-10 — the shipped `PRIVACY.md` names the wrong version.** The notice inside the published `com.beb.plxnative_0.6.1_arm.ipk` opens with "Applies to PlxNative 0.6.2", written on the assumption that the sign-in change would ship in a later cut; the text of the notice is the one 0.6.1 carries and the number is the only error. It is corrected on `release/v0.6` (58bf20c1) so the next package from this line reads correctly. Not re-cut: the published artifact, its hashes and the generated block below are unchanged, and a version number in a document's first line does not change what the app does or sends.

**2026-09-10 — the P1b remedy claim above ("Device test evidence" and "Known issues at release, with provenance") needs a correction on the same point the release note above corrects.** Nowhere in this audit's authored text does it independently claim the Homebrew Channel carries the PR #202 fix — it correctly scopes P1b's k5lp/k3lp guard as unverified on this team's own non-Realtek hardware — but the release note this audit backs did make that claim, citing [webosbrew/webos-homebrew-channel PR #202](https://github.com/webosbrew/webos-homebrew-channel/pull/202), and PR #202 was reverted by [PR #211](https://github.com/webosbrew/webos-homebrew-channel/pull/211) on 2025-02-26 and never shipped in any tagged Homebrew Channel release. This audit's own community-tier framing of P1b — "built from community-tier evidence... not a vendor statement" — is unaffected: the citation is still the right record of the *condition* (`/dev/rtkmem` missing from the `native_devmode` jail on k5lp/k3lp), only the note's separate claim about the *remedy* was wrong. Readers should treat "reinstall through the Homebrew Channel" as no fix at all; the community's surviving workaround is [mariotaku/kodi.addon.webos-jailer-fix](https://github.com/mariotaku/kodi.addon.webos-jailer-fix), unverified by this project, which needs a full power-cycle to take effect. The app's own failure screen carries the same correction starting with the release after v0.6.2.
