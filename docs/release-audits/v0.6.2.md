# Release audit — v0.6.2

The evidence behind [the v0.6.2 release note](https://github.com/GLinnik21/plx-native/blob/v0.6.2/docs/release-notes/v0.6.2.md). Written for a webosbrew reviewer, a contributor auditing an old release, and us in a year. The section at the bottom is generated from the artifact that was published; everything above it was written by a person before the release ran. The generated block is filled in by `.github/workflows/release.yml`'s own last step, AFTER the `v0.6.2` tag is cut, and committed to this maintenance line's own branch (`release/v0.6`) rather than to the tag — so the tagged tree can never carry it. Read this document at `blob/release/v0.6/docs/release-audits/v0.6.2.md`, not at `blob/v0.6.2/…`, if the generated section matters to you.

## What this release contains

One fix, from two reports that turned out to be the same bug, and the telemetry that would have seen it.

- **Issues #75 and #76 — the sign-in did not persist on 2024 and newer LG sets.** `plex::session::save` seals the session through `com.webos.service.keymanager3` on firmware that has it (2024 and newer sets), and `peek` re-read and re-decrypted the file on every account-menu open; on both reporting sets the service sealed the session but could not open it again, so every read landed `Locked`, the chip said "Sign in", `update` refused, the next launch showed the QR code, and the next save re-sealed into the same unopenable envelope. The sign-in screen's slowness and its "try again" loop are the same failure seen from the discovery stage, which waits on the key service's 4 s LS2 budgets. The dev set (webOS 4.10) has no keymanager3, so this path had never round-tripped on hardware. The fix: `keymanager` proves the round trip before returning a `Sealed`, logs every service refusal once (never the reply text on the calls whose request carries the IV or the payload), drops the undocumented `mac_length` parameter, and classifies an unanswered call (`no_reply`, `unreachable`) and the hub's "Service does not exist" reply (the service's absence) as distinct from a refusal; `session` keeps one in-process copy, treats an unopenable envelope as `LOCKED_RECOVERABLE` for the process, replaces it with the 0600 plaintext file after a fresh sign-in, and persists a `<id>-secure-storage.refused` marker (0600, naming the failing stage) that keeps the install on the plaintext file until sign-out or erase. Reads are never gated on the marker and an ordinary `update()` never converts a present envelope.
- **Storage telemetry.** `session_storage` (`none` / `plaintext` / `secure` / `secure_locked` / `secure_refused` / `unknown`) rides on every usage event; a handled `StorageError` Sentry event (standing error-report consent only) carries the failing stage from a closed vocabulary, the service's numeric error code when one was reached, the storage class and whether the marker is present, once per process per stage, sent after the session lock is released; the sign-in report gained the storage class. `PRIVACY.md`, the in-app policy, the sign-in alert's body and both consent previews name every new field. `POLICY_VERSION` moved to 6, so every existing 0.6.x install is asked the reporting question again on first launch, and the Crash stage now shows why it asks again.

## Device test evidence

| | |
|---|---|
| television | dev set: LG 49SM9000PLA, board `m16p3`, platform release 4.10.2 — a set with NO keymanager3, which is the point: it cannot exercise the path this release fixes |
| server | none needed this cycle |
| suite | `cargo +nightly test --lib` (default features) — 2233 passed, 0 failed |
| suite | `cargo +nightly test --lib --features hostsim` — 2262 passed, 0 failed |
| suite | `./tests/run.py` — not run this cycle; nothing on the playback path changed |
| perf | not run; no UI motion or playback change |
| by hand | one read-only probe on the dev set, 2026-09-10: `luna-send` to `com.webos.service.keymanager3/generateKey` as root answers in under 2 ms with `errorCode -1`, `"Service does not exist: com.webos.service.keymanager3."`, and the legacy `com.webos.service.keymanager` answers `-1`, `Unknown method`. That measurement is what makes the app treat the hub's reply as absence rather than a refusal, which would otherwise have sent a `StorageError` from every older install without the service, on its first save |

**What was verified, and what was not.** The host suite for the final tree ran clean on both feature sets, and `CARGO_INCREMENTAL=0 cargo +nightly check --no-default-features` is clean. The keymanager3 service is scripted in the host suite (`keymanager::platform`'s test client), which is how the locked-read, round-trip, marker, no-reply, absent-service and once-per-process report behaviours are each pinned by a test that was watched red before the code that makes it pass. `make FLAVOR=stable RELEASE=1 ipk` followed by `python3 ci/check-package.py` passed every assertion locally, and `tools/fwcompat.py` grades the release binary OK on every firmware from 4.4.2 through 11.2.0 with `DT_NEEDED` unchanged. **No television with keymanager3 was available**, so the fix has not been run against the service it is for; the two reporters' sets are the only place that can happen, and the note asks them.

## External compatibility reports known at release time

[GitHub issue #75](https://github.com/GLinnik21/plx-native/issues/75): an OLED55C41LA, a 2024 model (platform release 9.2.2), Developer Mode install. PIN linked on the phone; the television shows loading, then a "try again" loop, eventually reaches Home, the account menu still says sign in, a relaunch is back at the QR code. Same on 0.6.0 and 0.6.1. No log was obtainable.

[GitHub issue #76](https://github.com/GLinnik21/plx-native/issues/76): an OLED77G45LW, platform release 11.2.0, Developer Mode install, not rooted. Slow "finding your server", the app works for the run, the account chip says "Sign in", pressing it restarts the QR flow, a relaunch is back at step one. The reporter tried to fetch the event log from the Developer Mode shell: that shell runs jailed as uid 5919 (`prisoner`) and sees no `/tmp/plxnative-events.log` at all, so on this firmware an unrooted owner has no way to hand over a log — which is the reason the storage telemetry exists. Neither reporter is named here, per this project's third-party-data rule; both are linked from the issues. No firmware tier moves on either report: they are the origin of the fix, not a confirmation of it.

## Compatibility tiers, and what moved

Nothing moved. This release's compatibility block is copied verbatim from v0.6.1's, per this project's release-notes standard for a maintenance-line hotfix: no new television was watched and no new playback report arrived. See v0.6.0's own [tiers and evidence](https://github.com/GLinnik21/plx-native/blob/main/docs/release-audits/v0.6.0.md#compatibility-tiers-and-what-moved) for how each line got there. The two reports above concern sign-in persistence, not playback, and are recorded in the section above rather than in a tier.

## Known issues at release, with provenance

- **The sign-in fix is unconfirmed on any set that has keymanager3.** It is pinned by the host suite against a scripted service and by the dev-set probe above, which can only show what a firmware WITHOUT the service answers. Whether the reporting sets now stay signed in is answerable only there; the note's Help test this release section asks for exactly that.
- **On a set whose key service never answers, the first launch after this update still pays the service's 4 s budget once** before the stored sign-in is treated as locked. Every later launch skips the service entirely because of the persisted marker. Measured nowhere; derived from the code's budgets.
- Every known issue carried in [v0.6.1's audit](https://github.com/GLinnik21/plx-native/blob/release/v0.6/docs/release-audits/v0.6.1.md#known-issues-at-release-with-provenance) is unchanged by this release; none of it was re-measured this cycle.

## Deliberately not done

- **A television with keymanager3 was not sought out or acquired.** The path was made testable on the host instead (the scripted service client), and the reporters were asked to try the build.
- **The refused marker is written on ANY failure to open an envelope this install wrote, including a timeout and a failed registration**, not only on a service reply that refuses. A review argued for the reply-only gate, and a trace under a stalled service showed its cost: no marker, so every later launch re-pays the 4 s budget forever, and no report, so nothing ever says so. The trade accepted is that a healthy set that hiccuped once keeps a 0600 file instead of an envelope until its next sign-out. Older sets have only ever had that file.
- **A `no_reply`/`unreachable` during a seal on an install with no envelope is not reported.** With no envelope on disk it is indistinguishable from an older firmware without the service, and reporting it would have meant one event from every such install.
- **The event log's location was not changed for Developer Mode installs.** The #76 reporter's shell cannot see the app's `/tmp` on webOS 11; whether it can see the app's own directory is unmeasured, and the owner chose not to ask the reporter for more. A devmode-only mirror of the logs in the app directory is a possible later change, not part of this release.
- **The consent re-ask on first launch of 0.6.2 is deliberate.** `POLICY_VERSION` moved from 5 to 6 because the storage facts are newly collected; the same rule and the same cost as 0.6.1's re-ask, with one improvement: the question now states the reason it is asked again.

## Release configuration

| | |
|---|---|
| flavour | `com.beb.plxnative` (stable) — the id users install; `FLAVOR: stable` is pinned in the release workflow's build job |
| cargo features | `RELEASE=1`, which drops `devtools` and `devtriggers` |
| built by | GitHub Actions, from the tag, at a path identical on every runner |
| toolchain | webOS NDK, Rust nightly pinned by date in the `RUST_NIGHTLY` repository variable — unchanged from v0.6.1 |

Cut from the `release/v0.6` maintenance line, like v0.6.1. No new payload file, no new outbound host, no new file written outside the app's own directories except the `<id>-secure-storage.refused` marker, which lives beside the session file at the same candidate paths and mode. Nothing else about the release configuration changed from v0.6.1.

<!-- BEGIN GENERATED — ci/gen-release-audit.py. Do not edit by hand. -->

*Every value below was read out of `com.beb.plxnative_0.6.2_arm.ipk` by `ci/gen-release-audit.py`. Nothing in this block was typed by a person.*

### Identity and provenance

| field | value |
|---|---|
| tag | `v0.6.2` |
| commit | `20f4feec539de296d11e85e5ee7f9331e3d6ae02` |
| workflow run | https://github.com/GLinnik21/plx-native/actions/runs/34471869595 |
| asset uploader | `github-actions[bot]` |
| built by | GitHub Actions (`.github/workflows/release.yml`, job `build + verify`) |

### The package

| field | value |
|---|---|
| filename | `com.beb.plxnative_0.6.2_arm.ipk` |
| app id | `com.beb.plxnative` |
| version | `0.6.2` |
| type | `native` |
| sha256 | `876097b5bdcdf5fce411ed9e15b7ea26f06c6c850740dc53e8490d6ce2ca7e36` |
| download size | 16 754 764 bytes |
| installed size | 31 355 KiB (control `Installed-Size`) |
| ar members | `debian-binary` (4 B), `control.tar.gz` (396 B), `data.tar.gz` (16 754 176 B) |

**Hash agreement:** all 3 copies agree.

| source | sha256 |
|---|---|
| the artifact's own sha256 | `876097b5bdcdf5fce411ed9e15b7ea26f06c6c850740dc53e8490d6ce2ca7e36` |
| `com.beb.plxnative.manifest.json` → `ipkHash.sha256` | `876097b5bdcdf5fce411ed9e15b7ea26f06c6c850740dc53e8490d6ce2ca7e36` |
| `ipk.sha256` | `876097b5bdcdf5fce411ed9e15b7ea26f06c6c850740dc53e8490d6ce2ca7e36` |

`ipk.sha256` names `com.beb.plxnative_0.6.2_arm.ipk`, so `shasum -a 256 -c ipk.sha256` works in the directory a user downloads into.

The Homebrew Channel manifest declares `ipkSize` 16 754 764 bytes and `installedSize` 31 355 KiB; the size it declares matches the artifact. `rootRequired` is `false`.

### Control file

| field | value |
|---|---|
| `Architecture` | arm |
| `Description` | PlxNative — unofficial native Plex client for LG webOS (not affiliated with Plex) |
| `Homepage` | https://github.com/GLinnik21/plx-native |
| `Installed-Size` | 31355 |
| `License` | MIT |
| `Maintainer` | Gleb Linnik <23104281+GLinnik21@users.noreply.github.com> |
| `Package` | com.beb.plxnative |
| `Priority` | optional |
| `Section` | misc |
| `Version` | 0.6.2 |
| `webOS-Package-Format-Version` | 2 |
| `webOS-Packager-Version` | plxnative-mkipk/1 |

### Build configuration

| field | value |
|---|---|
| flavour | `com.beb.plxnative` — the stable id, which is what users install |
| cargo features | `features:--no-default-features+symbols+tel:78a04a1fd0f5` |
| dev-trigger surface | absent — `plxnative-noidle`×0, `plxnative-remote`×0, `plxnative-capture`×0, `plxnative-drawmask`×0, `plxnative-heroground`×0 |
| telemetry endpoints | Sentry `https://30574b7659ec4fcdc39d6b3072deace8@o4511994943700992.ingest.de.sentry.io/4511994955890768` (EU); PostHog `phc_kVAmK72TzZXH4VwLkcJuJs5tqAgWh97GRqaSpR9cKn3Mproductiontelemetry` — write-only ingest credentials, publishable by design |

The witnesses are literals from `dev.rs`'s `DIAG` array, which is `#[cfg(feature = "devtriggers")]`; two of them name the surfaces a reviewer reading this repository's source would rightly ask about — the world-writable `plxnative-remote` FIFO that can drive the UI, and the unauthenticated TCP capture listener. Zero of all five is what `RELEASE=1` looks like in the bytes, and it is a property of this package rather than of the command line that produced it. Each witness has been measured from both sides (0 here, 1-2 in a dev build), because a witness that cannot fail is not evidence.

### Package surface

| field | value |
|---|---|
| declared permissions | none — `appinfo.json` declares no `requiredPermissions` |
| `requiredMemory` | 160 MB |
| `id` prefix | not one of LG's reserved prefixes |
| localized descriptors | 12 locales |

**Host-shaped strings in the shipped binary.** A floor, not a proof: a string here may be a log message rather than a destination, and a host assembled at runtime would not appear at all.

```
answeringplex.tv
eu.i.posthog.com
github.com
index.crates.io
ns.adobe.com
o4511994943700992.ingest.de.sentry.io
plex.tv
plxnative.com
www.plex.tv
www.w3.org
yetplex.tv
```

**Absolute paths outside the app's own directory, same caveat.**

```
/dev/null
/dev/random
/dev/rtkmem
/dev/rtkmem/etc/prefs/properties/machine
/dev/urandom
/etc/os-release
/etc/umediaserver/device_codec_capability_config.json
/etc/xdg
/media/cryptofsdevmodeapplicationsauth.jsontelemetry.json
/media/developer
/media/developer/
/media/developer/apps/usr/palm/applications/com.beb.plxnativeseason
/media/internal/.
/proc/self/auxv
/proc/self/cgroupcpu.cfs_period_us/sys/fs/cgroupcgroup.controllers
/proc/self/exe
/proc/self/fdfatal
/proc/self/maps
/proc/self/mountinfocgroup/
/proc/sys/kernel/random/uuidplayback
/tmp/%s
/tmp/plxnative-dvnonode
/tmp/plxnative-profile
/tmp/plxnative-url
/var/run/nyx/device_info.json
/var/run/nyx/os_info.json
```

### Reproducibility evidence

No payload file carries a build machine's directory layout (all 35 files scanned; the NDK's own `--cross-prefix`, which FFmpeg records and which is identical on every runner, is the one allowed exception). That is what makes two builds of this commit on one machine byte-identical. It is not yet a cross-machine claim.

### Payload inventory

| path | bytes | mode | owner | sha256 (first 16) |
|---|---|---|---|---|
| `usr/palm/applications/com.beb.plxnative/OFL.txt` | 4 377 | `0644` | root:root | `5b9321a4298cfeb6` |
| `usr/palm/applications/com.beb.plxnative/THIRD-PARTY-NOTICES.md` | 24 025 | `0644` | root:root | `f5328c1cb3bee9e2` |
| `usr/palm/applications/com.beb.plxnative/appfont-bold.ttf` | 408 976 | `0644` | root:root | `b30d8082184474b4` |
| `usr/palm/applications/com.beb.plxnative/appfont-cjk.ttf` | 21 185 868 | `0644` | root:root | `eeb1b524ac15a8bb` |
| `usr/palm/applications/com.beb.plxnative/appfont.ttf` | 407 448 | `0644` | root:root | `4ef24f0bac98d3a6` |
| `usr/palm/applications/com.beb.plxnative/appinfo.json` | 452 | `0644` | root:root | `adbda90730b9c131` |
| `usr/palm/applications/com.beb.plxnative/icon.png` | 3 972 | `0644` | root:root | `d70bcff921afb66f` |
| `usr/palm/applications/com.beb.plxnative/largeIcon.png` | 9 716 | `0644` | root:root | `dc169758d9bc7fa9` |
| `usr/palm/applications/com.beb.plxnative/libavcodec-plx.so.63` | 441 112 | `0644` | root:root | `ec14fafb73febeea` |
| `usr/palm/applications/com.beb.plxnative/libavformat-plx.so.63` | 445 272 | `0644` | root:root | `cbf5763941a3f98c` |
| `usr/palm/applications/com.beb.plxnative/libavutil-plx.so.61` | 452 796 | `0644` | root:root | `813fcbe68d041ee7` |
| `usr/palm/applications/com.beb.plxnative/licenses/Apache-2.0.txt` | 10 280 | `0644` | root:root | `074e6e32c86a4c0e` |
| `usr/palm/applications/com.beb.plxnative/licenses/LGPL-2.1.txt` | 26 419 | `0644` | root:root | `20e50fe7aae3e563` |
| `usr/palm/applications/com.beb.plxnative/licenses/LICENSE` | 1 068 | `0644` | root:root | `3b297a24aa928a85` |
| `usr/palm/applications/com.beb.plxnative/licenses/LLVM-exception.txt` | 919 | `0644` | root:root | `e34c58338bd89d43` |
| `usr/palm/applications/com.beb.plxnative/licenses/MIT.txt` | 1 078 | `0644` | root:root | `b05785f9f18e6716` |
| `usr/palm/applications/com.beb.plxnative/licenses/TRADEMARKS.md` | 2 722 | `0644` | root:root | `9126d07ee3b66ac2` |
| `usr/palm/applications/com.beb.plxnative/licenses/Unicode-3.0.txt` | 1 995 | `0644` | root:root | `f7db81051789b729` |
| `usr/palm/applications/com.beb.plxnative/licenses/Zlib.txt` | 838 | `0644` | root:root | `bfb1112d49db5b1d` |
| `usr/palm/applications/com.beb.plxnative/plxnative` | 8 079 168 | `0755` | root:root | `87eb1ab0551d73e1` |
| `usr/palm/applications/com.beb.plxnative/resources/de/appinfo.json` | 121 | `0644` | root:root | `b99d7d0a4dcec269` |
| `usr/palm/applications/com.beb.plxnative/resources/es/appinfo.json` | 108 | `0644` | root:root | `7a1a661a8fb32705` |
| `usr/palm/applications/com.beb.plxnative/resources/fr/appinfo.json` | 104 | `0644` | root:root | `99cdb882afc49208` |
| `usr/palm/applications/com.beb.plxnative/resources/it/appinfo.json` | 106 | `0644` | root:root | `740304fde57c5f1f` |
| `usr/palm/applications/com.beb.plxnative/resources/ja/appinfo.json` | 140 | `0644` | root:root | `0d37e380dfb76ab4` |
| `usr/palm/applications/com.beb.plxnative/resources/ko/appinfo.json` | 134 | `0644` | root:root | `a798bbe139304277` |
| `usr/palm/applications/com.beb.plxnative/resources/nl/appinfo.json` | 107 | `0644` | root:root | `33e3f2e2a2e64dff` |
| `usr/palm/applications/com.beb.plxnative/resources/pl/appinfo.json` | 106 | `0644` | root:root | `9f73431888bf6759` |
| `usr/palm/applications/com.beb.plxnative/resources/pt/appinfo.json` | 112 | `0644` | root:root | `fc2cb8567bac914f` |
| `usr/palm/applications/com.beb.plxnative/resources/ru/appinfo.json` | 140 | `0644` | root:root | `97d9f269c96c5b38` |
| `usr/palm/applications/com.beb.plxnative/resources/sv/appinfo.json` | 107 | `0644` | root:root | `690f9ffe3b72c9e7` |
| `usr/palm/applications/com.beb.plxnative/resources/tr/appinfo.json` | 117 | `0644` | root:root | `990aabb413e146e5` |
| `usr/palm/applications/com.beb.plxnative/sentry-crash` | 514 776 | `0755` | root:root | `0e1617485cee8890` |
| `usr/palm/applications/com.beb.plxnative/splash.png` | 81 929 | `0644` | root:root | `b07abcf1b71e7b4d` |
| `usr/palm/packages/com.beb.plxnative/packageinfo.json` | 173 | `0644` | root:root | `c71c4613fa57aae5` |

### Linkage

`plxnative` is ELF32 / ARM, with **15 `DT_NEEDED` entries**:

```
libSDL2-2.0.so.0
libSDL2_ttf-2.0.so.0
libGLESv2.so.2
libluna-service2.so.3
libglib-2.0.so.0
libwayland-client.so.0
libplayerAPIs.so.1
libpf-1.0.so.1
libdl.so.2
librt.so.1
libpthread.so.0
libm.so.6
libgcc_s.so.1
libc.so.6
ld-linux.so.3
```

`ci/check-elf.sh` asserts this list against `ci/expected-dt-needed.txt` on every build: they agree. Two library families are deliberately absent because their SONAME moves between firmwares and a `DT_NEEDED` entry cannot express "either of these" — libcurl and libAcbAPI are `dlopen`ed by candidate list instead (`rust-modules/src/dynlib.rs`, `src/starfish.c`).

Shared libraries shipped **beside** the binary and opened by absolute path out of the app's own directory, so they can neither shadow nor be shadowed by the television's own FFmpeg:

| file | identity |
|---|---|
| `libavcodec-plx.so.63` | SONAME `libavcodec-plx.so.63`, 441 112 bytes, sha256 `ec14fafb73febeea` |
| `libavformat-plx.so.63` | SONAME `libavformat-plx.so.63`, 445 272 bytes, sha256 `cbf5763941a3f98c` |
| `libavutil-plx.so.61` | SONAME `libavutil-plx.so.61`, 452 796 bytes, sha256 `813fcbe68d041ee7` |

### FFmpeg and the LGPL position

| field | value |
|---|---|
| corresponding source | `ffmpeg-9.0.tar.xz` attached to this release, sha256 `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| build script | `build-ffmpeg.sh` attached, sha256 `7b49d924ae7c673f` |
| upstream tarball pinned in the script | `7f607a00dd0d28a729d5a4811205812eef01cf6ef6155025febb6f36a9062d52` |
| copyleft-widening flags | none of `--enable-gpl`, `--enable-nonfree`, `--enable-version3` (comments excluded from the scan) |
| licence texts in the payload | `Apache-2.0.txt`, `LGPL-2.1.txt`, `LICENSE`, `LLVM-exception.txt`, `MIT.txt`, `TRADEMARKS.md`, `Unicode-3.0.txt`, `Zlib.txt` |
| `THIRD-PARTY-NOTICES.md` in the payload | yes |
| `LICENSE` in the payload | yes |
| `TRADEMARKS.md` in the payload | yes |

The configure invocation FFmpeg records inside `libavutil`, verbatim — this is the primary evidence for both the licence position and the reproducibility scan above:

```
--prefix=/plx --enable-cross-compile --cross-prefix=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/bin/arm-webos-linux-gnueabi- --host-cc=cc --arch=arm --cpu=cortex-a9 --target-os=linux --sysroot=/home/runner/work/plx-native/plx-native/.webos-ndk/arm-webos-linux-gnueabi_sdk-buildroot/arm-webos-linux-gnueabi/sysroot --build-suffix=-plx --enable-shared --disable-static --enable-pic --extra-cflags='-Dstatic_assert=_Static_assert' --disable-programs --disable-doc --disable-avdevice --disable-avfilter --disable-network --disable-swresample --disable-debug --enable-small --disable-everything --disable-autodetect --enable-demuxer='matroska,mov,mpegts,h264,hevc' --enable-parser='h264,hevc,aac,ac3,dvdsub,dvbsub' --enable-bsf='h264_mp4toannexb,hevc_mp4toannexb,extract_extradata' --enable-decoder='pgssub,dvdsub,dvbsub,ass,srt,subrip,text,mov_text,webvtt' --enable-protocol=file --disable-swscale
```

### Verification gates

| gate | verdict |
|---|---|
| `tag / version agreement` | success |
| `redistributable assets` (LICENSE, notices, font family) | success |
| `build + verify` | success |
| `ci/check-elf.sh` — ELF identity, CP15 barriers, DT_NEEDED | pass |
| `ci/check-package.py` — versions, ar layout, descriptors, payload, notes | pass |
| the Homebrew Channel manifest's sha256 matches the artifact | pass |
| LGPL corresponding source attached (ffmpeg-9.0.tar.xz + build-ffmpeg.sh) | pass |

### Static firmware compatibility

This grades whether the process STARTS: it resolves the binary's libraries and undefined symbols against webosbrew's firmware inventories. It says nothing about whether video plays. Playback evidence is in the authored half above.

```
## Package com.beb.plxnative

### App com.beb.plxnative

|           | 4.4.2 | 4.10.0 | 5.3.1 | 6.4.0 | 7.4.0 | 8.3.0 | 9.2.0 | 10.2.0 | 11.2.0 |
|-----------|-------|--------|-------|-------|-------|-------|-------|--------|--------|
| plxnative | :ok:  | :ok:   | :ok:  | :ok:  | :ok:  | :ok:  | :ok:  | :ok:   | :ok:   |


#### plxnative

All OK
```

### What the public can download

`ci/verify-published.sh` re-derives every claim from the **published** assets after the release exists — the hash in every place it appears, `shasum -c` working where a user stands, CI rather than a person as the uploader, and no payload file carrying a build machine's paths.

```
== what the public downloads — v0.6.2 ==
  ok    the .ipk is published
  ok    shasum -c works where the two assets land side by side
  ok    checksum file matches the artifact
  ok    manifest hash and size match the artifact
  ok    the release note quotes this hash
  ok    assets uploaded by CI
  ok    no payload file carries a build-machine path

7 passed, 0 failed, 0 to look at
```

<!-- END GENERATED -->
