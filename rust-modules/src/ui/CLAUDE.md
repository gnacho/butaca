# ui/ — the shared UI system (read before touching any screen)

This is a **real product built for production quality.** "Not worth it for a throwaway" is never a
reason to skip a proper component, leave a bespoke `draw_*` in place, or half-finish a primitive.
If a shared piece is missing (a real text-flow view, a chip, a list),
**build it** — a reusable primitive that pays off across screens is exactly the work worth doing.

This directory is a **design system**, not a pile of per-screen draw code. Home, detail, and the
player HUD are all *compositions of the same tokens + components*. When you add or change UI,
**reach for the shared pieces and improve them in place** — do not paste a new `draw_*` function
with hard-coded colors and hand-tuned text offsets. That is exactly the drift this system exists to
kill. Full design + migration status: `docs/ui-system-migration.md`.

## The four rules

1. **Never write a raw color literal.** Every color comes from `theme.rs` as a named token
   (`theme::TEXT_PRIMARY`, `theme::CONTROL_IDLE_FILL`, `theme::scrim(a)`, …). Need a shade that
   doesn't exist yet? **Add a token to `theme.rs`** (with a doc line saying what it's for) and use
   that — don't inline `[0.9, 0.9, 0.9, 1.0]`. If your shade is within a hair of an existing token,
   use the existing one; the point is one value per role, not a value per call site.
   `theme.rs` is **two layers**, so "add a token" has two halves: a new token is a **role** (a job:
   what is this colour *for*) and it resolves to a **primitive** (a stop on the palette, private to
   that module) — reach for an existing stop first, and only add one when the palette genuinely has
   no such shade, as an exact 8-bit code via `rgb8`. Two roles landing on one stop is expected and
   stays two roles (`ACCENT` and `TEXT_PRIMARY` are both Cool 0): retuning the focus fill must not
   restyle every title on screen. Overlays are `with_a(WHITE, a)` on the measured alpha ramp — a new
   overlay is a weight, never a new hue. The layering mirrors the `PlxNative Design System` project
   (`tokens/primitives.css` + `tokens/colors.css`), so a palette decision is one edit in each.

2. **Never write a raw text size — every text size in the UI is a `theme::size` token.** The named
   rungs are `size::HERO` 72 / `size::DISPLAY` 48 / `size::TITLE` 40 / `size::HEADLINE` 32 /
   `size::BODY` 28 / `size::LABEL` 26 / `size::CAPTION` 24 / `size::MICRO` 22 /
   `size::DIAGNOSTIC` 20 — the *size* axis of the design system.
   `CAPTION` (24) is the **couch legibility floor for ordinary product content**; `MICRO` exists
   solely for one-line de-emphasized labels — an episode's air date, a rating row's provider
   caption, the Source chip's handle — never for content. `DIAGNOSTIC` belongs only to the
   deliberately opened `ui::stats` engineering instrument: its fixed schema and complete values
   matter more than couch-copy scale, and it must never migrate into chrome or prose. The hero
   SYNOPSIS is the case that proves
   the rule and it is no longer here: a blurb is the longest run of prose on its page, so it is
   reading copy, and both heroes now draw it at `LABEL` 26 through `ui::hero_synopsis`. (The hero
   META line this parenthetical used to name is `BODY` 28 and always was.) Pass a rung to `Painter::text`/`Label`/`TextView`/`text::elide` instead of a bare
   `24`/`28`/… — a size is a role, not a magic number; pick the nearest rung, and if no rung fits a
   genuinely new role, **add a documented rung to `theme.rs`**, don't inline a literal. Exactly two
   carve-outs live outside the scale (the player-HUD display title `HUD_TITLE_SZ` and the subtitle
   caption); both are **named + commented at their call site**, never bare literals — don't add a
   third; new roles go on the scale. `anim.rs` is a dev-diagnostic overlay, not chrome.

3. **Never hand-place text with a magic y.** `y - sz*0.58` guesses are banned — they mis-center the
   moment a string has a descender (g j y p). Text is positioned by its **cap band** (layout ≠
   paint): use `ui::label::Label` (single run) or `ui::text_view::TextView` (multi-line, pixel-
   wrapped), or if you must call `Painter::text` directly, derive the y from `text::text_vcenter_y` /
   `text::text_cap_band`. See `label.rs`'s module docs for the rule.

4. **Improve a component before forking one.** If a shared widget almost does what you need, add a
   builder method / style variant to it (e.g. `Button::style(ControlStyle)`), don't copy it. A new
   bespoke widget is only justified when nothing here is close — and then it lands *here*, as a
   reusable `View`, so the next screen gets it for free.

## The route family says where BACK goes ONCE, as a crumb

Every Settings-family route — Settings, Privacy & data, Legal notices, a legal document, About, the
Home-sources editor, the first-run consent pair — draws a caption line above its title: a
left-pointing chevron (`Icon::ChevronLeft`, the mirror of the row chevron and sharing its `ink_x`
entry) and the name of the place BACK returns to. `RouteLayout::draw_narrative` takes it as
`Option<&str>` so a new route cannot forget to answer. **`None` means "BACK does not go anywhere
inside the app"** — three routes qualify today (the Settings modal, the first first-run consent
question, and the QR sign-in), but take the census with
`git grep -A2 'draw_narrative(' -- 'rust-modules/src/ui/*.rs'` rather than trusting that number.

**It replaced `Press [BACK] to return` in the bottom action band across the whole family**, and the
trade is the point: the hint spent a 60px band restating a key the remote already has and could not
say where the key WENT, which on a three-deep push is the only part anybody needs. Freeing the band
is what let first-run consent put its two answers there. The sweep is complete —
`git grep -n 'KeyHint::new'` finds no call under a route any more, and the consent document
reader's was the one that survived the first pass, so that route said where BACK went twice.
`KeyHint` is unchanged and still correct where it lives: the read-only ALERT panels
(`about_panel`, `tracks_panel`, `person_bio`), which hold no control at all, so there the line
really is the whole affordance.

## What lives where

This table is the map of the directory — **every** `.rs` file under `ui/` has a row, so a missing
entry means the table is stale, not that the module is unimportant. Keep it that way when you add
one: an undocumented module is one the next person re-implements.

`widgets.rs` also owns the reusable `Glass` policy, and it now has exactly ONE axis: how often the
backdrop snapshot is refreshed. `Glass::CACHED` is the default — capture once, on open. A surface
over a MOVING page opts into `Glass::DYNAMIC_BACKDROP`, which invalidates on the shared cadence
(`DEFAULT_DYNAMIC_PERIOD`, 1 — every CHANGED present) while the widget itself draws every present.
`Popover::with_glass` carries that policy through the shared modal choreography.

**Freezing the HOST PAGE is a second, independent policy, and since 2026-09-02 it is shared too —
`Popover::caching_host()` + `ui::popover::host`.** It was `account_menu`'s private `gfx::FrameCache`
with a special case for it in `app.rs`, and no other popover could reach it; the set that now can
— the Library's sort and filter menus, the decision alert and Settings since 2026-09-03, and four
which draw from INSIDE their page (`tracks_panel`, `about_panel`, `alt_sources`, `person_bio` all
draw at the tail of `detail::draw`/`person::draw`) and so could never have used the old shape. The mechanism is a REFUSAL at the renderer's one shared gate (`gfx::culled`, plus
`frame_clear` and the glass composite) rather than a skipped call tree: the page's draw still runs,
so its layout, hit rects and texture uploads are untouched, and only the fill goes away. **Adding a
popover to that set is one `.caching_host()` on its constructor plus three one-line guards** — a
`host::live()` at the top of its `draw` and `draw_scrim`, a `popover::own_motion()` at the top of
its `update`, and a `popover::note_own_damage()` beside whatever `idle::invalidate` its key handler
already owes (that one feeds the GLASS ledger; the host cache attributes input by scope —
`host::input_scope` in `app.rs` — and counts page damage as whatever no popover scope raised). A popover that replaces its host with an OPAQUE ground (Settings) needs no
`host::live` guards: once its ground is ready (`settings::host_ground_ready`) `app.rs` skips the
page pass altogether and the family draws outside it — cheaper than serving a snapshot nobody sees. Read `popover.rs`'s module doc before touching any of it; the snapshot has two stages
(the undimmed page, then the page + scrim + lifted `Opener` + the panel's own ground once the appear
spring has settled) and the second one is where the frames are. **Do NOT give it to a player-route
overlay**: behind those is punch-through alpha to a hardware plane GL cannot read at all.
Measured on the television, detail 2012 under an open track panel, six DOWNs: `draw≈83 ms`/`loop=41`
before, `draw≈40 ms`/`loop=60` after, and a `drawmask=all` control on the shipped build — the app
submitting no quad at all — still reports `draw≈40 ms`, i.e. nothing the app draws is left in it.
A dynamic GLASS policy over a frozen host is still right and still cheap: the scrim is drawn live
above the snapshot, so a re-source sees the ramp it is meant to.
All dynamic owners prepare before
their shared host draws; recurring cadence is global so adjacent
widgets cannot multiply snapshot frequency. Report host motion, not the glass widget's foreground
springs. Never use any of it over the hardware video plane — GL cannot read the punch-through alpha.
The measured envelope and pacing caveat live in `docs/liquid-glass.md`.

**Every slow FIELD in the app takes ONE dither whenever it is drawn, and it is not a per-shader decision.** An
8-bit framebuffer quantises, so a ramp whose whole range is a handful of codes arrives as flat
plateaus — measured on the television, Settings over Home: a 700-row column through the modal ground
spanning luma 55.7 to 59.1 in FOUR levels, treads of 158, 157 and 146 rows. `shaders/dither.glsl` is
the one answer and `gfx::glsl_dithered!` is how a program gets it — the three whose ramp is a blur or
a whole-screen wash (`fs_ambient`, `fs_modal_ground`, `fs_glass`), and deliberately NOT the per-rect
`fs_src`/`fs_shadow`, which carried it for two days and cost the hero paging scene 57→50 fps for a
branch that answered "no" on every draw (2026-09-04). `gfx::dither_for_field` is the one policy for
whether a field pays, and it is an AREA test only: a field dithers whenever it is drawn, moving or
not. The two PAGE washes under moving artwork (Home's, Detail's) are the one exception, through
`gfx::page_wash_dither` — their own slide flag plus the present gate's motion verdict — and
`ui::idle::should_present` presents one SETTLE frame after motion stops so the picture that stays
on the panel is the dithered one. For one day that motion gate was global and every focus spring
on Settings, the picker and first run flickered the wash's bands in and out; do not put it back. A uniform, square, unfocused rect (every
full-screen scrim) goes through `shaders/fs_flat.frag` instead of the fill program — one colour out,
no interpolation. **Do not reach for `highp`** — banding is an OUTPUT-quantisation
problem, and promoting a mix to fp32 changes nothing you can see while costing ~4.5 arithmetic words
a fragment. **And do not write a hash.** `fract(sin(dot(p,k))*43758.5)` is ~7 arithmetic words on
this part, and `fs_glass.frag` — the popover background — ran one UNCONDITIONALLY, on every fragment
of every glass surface, for as long as `fs_ambient.frag` had been carrying a note about that exact
mistake costing 38% of a Home frame. Five programs, four answers, and nothing compiling the
difference; a host test now grades all five together. Measured effect on the panel: the flat runs
that ARE the staircase fall from 9.6 px and 15.7 px to 2.2 px, the horizontal autocorrelation at
lag 64 from +0.144 to −0.010, and paging fps is unchanged.

**A modal's dim is drawn, never applied to the source.** There was a second axis (`GlassUnderlay`)
whose other variant multiplied the host page's RGB going into the backdrop instead of compositing a
scrim over it, and it was measured and removed: dimming the source destroys the modulation the frost
is then layered over, so the panel arrives flat however dense the material is. Do not re-add it from
this note. One consequence is load-bearing and easy to get wrong — **the scrim is part of the PAGE**,
because the direct source path re-renders the page closure before any popover draws. A scrim drawn
with the panel reaches the visible frame but not the snapshot, and the panel's frosted ground then
comes out brighter than the dimmed screen around it. `Popover::scrim` + `content_painter` is the
pairing that puts it in the right place; `account_menu` is the worked example.

| File | Owns |
|---|---|
| `route_screen.rs` | **The route family's shared engine — geometry, ground and push, none of it screen-local.** `RouteLayout` (narrative/content/action columns, the crumb, `action_pair`); `RouteGround` (the frozen four-corner ambient wash — see below); `RoutePush` (the ONE nested push/back spring the family shares — `PUSH_K` 200, parent travels `-0.35` of the screen width while fading, child leads in from `+0.22`; reversing `open` runs the exact inverse, so BACK never invents a second motion); and `ActionRow<N>` (the shared `widgets::CtlPop<N>` wrapper every action-row control face draws through, added 2026-09-02 alongside the other two — see below). **`RouteGround`'s three-tier fallback (2026-09-02):** a route that opens before Home has drawn anything (first-run consent, ahead of the profile picker) tries (1) Home's own live hero if this boot already fetched one, else (2) the LAST hero envelope this television ever showed (`plex::session::last_hero`/`record_last_hero`, written by `RouteGround::draw_home` itself whenever tier 1 succeeds — device-wide, no profile key, the same shape `consent`'s own storage uses), else (3) `theme::ROUTE_GROUND_FALLBACK`. Tier 2's write site lives here rather than in `home.rs` because nothing in `home.rs` is in this family's file scope; it fires only on the routes that call `draw_home` (first-run consent, first-run `onboard`), so an ordinary already-onboarded session updates the seed rarely rather than on every Home visit — a known, accepted narrowness, not an oversight. **The fallback itself was corrected 2026-09-02**: the original four stops sat within ~15 8-bit codes of `theme::SURFACE_APP` in both hue and luminance on three of four corners, which is what a device capture of first-run consent showed as "no ambient light" rather than a wash — `route_screen::tests::the_pre_home_fallback_reads_as_a_directional_wash` and `every_fallback_corner_is_visibly_apart_from_the_app_surface` pin the correction (~7x luminance spread corner to corner, every corner still clearing the same 3:1/7:1 floors `AmbientWash::keyed` holds a real hero to). **`ActionRow<N>`** exists so a control face in this family's bottom action row gets a real focus pop (`widgets::CtlPop`) and, once a caller arms `press::begin_ctl` on its OK-down, the tvOS press dip/ring — and since 2026-09-04 **every action-row control in the family is wired end to end**, `onboard.rs`'s pill through `app.rs`'s `key_onboarding` arm and `consent.rs`'s Share/Don't-share and Settings-mode Done through the consent arm above it (this line used to say those two were "NOT yet armed"). `ActionRow` also owns the band's **press RECTS** now (`place`/`clear`/`hit`, recorded at draw): that is what makes a band hoverable and clickable, and `clear` on the branch that does not draw is what stops a Done an undo has removed from leaving a live click target behind it. **The family's focus MODEL lives beside it** — `RouteFocus`/`RouteShape`/`RouteStep` and the eleven rules in `route_screen.rs`'s module doc, which every screen here obeys rather than reading the keys its own way; read those before adding a screen or a key to one. |
| `consent.rs` | **Two independent telemetry decisions, asked once PER SIGN-IN (they end with it).** First run asks Crash reports and Product analytics on consecutive opaque route screens, each with equal Share / Don’t Share answers **in the route's bottom ACTION ROW** (they were table rows until 2026-09-02, which put a one-press decision in the same column, type and gesture as the two documents you may read first); the list beside them keeps only the example report and the Privacy policy, focus opens on Share, LEFT/RIGHT walks the two answers and — since the family got one focus model (2026-09-04) — carries on out of the band into the reading list rather than stopping dead, and the two sit one `widgets::CONTROL_GAP` (20) apart — the control-GROUP distance the decision alert's two answers use, not the `space::LG` 40 region rung, which reads as two unrelated controls sharing a row. Nothing is stored until both have been answered. **It is asked as soon as there is an authorized account and BEFORE the profile picker** — the storage was always account-wide (`telemetry_candidates()` is one file with no profile key, and `auth::forget_account` unlinks it at sign-out), so asking on arrival at Home put a data-protection question to whichever household member happened to be picked, up to a managed child profile. BACK reverses Product → Crash and is then SWALLOWED: the step behind the first question is sign-in, which cannot be undone, so that route carries no crumb. Settings shows the latest chosen answers as independent switches, BACK discards the draft, and the bottom action grows a Done only after a value changes. The bounded save receipt outlives this route; a non-durable latest result is admitted through the shared Popover registry and can be retried, while sign-out or a newer choice invalidates it. First run, Settings, Shared Sources, Legal and About all use `RouteLayout`; Privacy Policy and the exact-payload preview push into the shared spring-scrolled `DocumentReader`, not an alert — drawn at `RouteLayout::document`, which hangs a TEXT-ONLY content column from the narrative title's anchor rather than the crumb's guide (a table spends that band on its first section label; a document has none, so its first line used to sit up on the return chevron's line). Ordinary automated boots never see consent, while `plxnative-consent=<crash|product>` is the explicit visual/FPS seam and records nothing. The payload preview is built through the real serializers and its tests assert every schema/field, the anonymity flag and the absence of a live identifier. **Crash → Product is a real `RoutePush` now (2026-09-02), not a cut** — Crash is the fixed PARENT role and Product the fixed CHILD, exactly as `legal.rs`'s index/document never swap; each stage owns its own fixed-content `TableView` (`TABLE_CRASH`/`TABLE_PRODUCT`, built once and never rebuilt, unlike Settings' own `TABLE`) because a single shared table would already show the new stage's rows before the outgoing one had finished animating off. `draw_stage`/`stage_visible` are the host-testable half of "both stages are on screen mid-transition" — device/sim-verified 2026-09-02 (frame sequence, both directions). |
| `settings.rs` | The reachable post-setup Settings root, and one of the three routes that pass **no crumb** — it is a modal over Home, so BACK leaves the family rather than moving inside it. It reuses `onboard` for Home sources, `consent` for Privacy, and `legal` for notices and About. BACK dismisses the unchanged root; child editors own their own commit semantics. It fades over one frozen ambient sample of the host, then stops drawing/updating the hidden page. A settled child also culls the covered Settings root's table/text while retaining that one shared ground; only the two content trees participate during push/back. While the Settings-hosted Home editor is visible, that child route owns input even though the Settings modal remains open behind it. Its root↔child transition is a `route_screen::RoutePush` (2026-09-02) rather than a private `Spring` — the numbers (`200`, `-0.35`) were already hand-duplicated from the shared constants before the swap, so this was a drop-in rather than a retune; sim-verified with a frame sequence the same day (Settings root fading/travelling left while Privacy & data arrives). |
| `legal.rs` | **Privacy / Open-source licences / FFmpeg source offer / Source code / Trademarks / Contact.** Settings opens a standalone Legal index `TableView`; choosing a row pushes to the same route-sized `DocumentReader` Privacy uses. It is deliberately not master/detail: only one D-pad scroll context exists at a time, and BACK runs the exact reverse `RoutePush` to the index — which the pushed document's crumb names (`‹ Legal notices`), as the index's names Settings. About is the same document route entered directly, so its crumb names Settings too. Documents are constants so the one compliance surface a store reviewer needs cannot fail on file I/O. The reader preserves paragraph/JSON structure, scrolls on a spring and draws the shared continuous rail. The `.ipk` still carries the complete notice and licence files; the sofa-readable pages explain the current build and link Plex's own Privacy Policy without accepting responsibility for Plex's processing. **This is the pattern `consent.rs`'s own Crash↔Product push copied 2026-09-02** — a fixed parent/child role pair on one shared `RoutePush`, each side its own real content object rather than one rebuilt table. |
| `onboard.rs` | **"What goes on your Home?"** — the first-run Home-sources route, and its Settings-hosted twin (`enter_settings`/Home editor). One action pill (`Start watching`/`Done`/`Try again`) beside a library-pin list built from `source_list`. The pill's press surface is a `route_screen::ActionRow<1>` (named `ACTION_POP`, migrated 2026-09-02 from a private `widgets::CtlPop<1>` — same numbers, shared type) and `focus_is_ctl`/`app.rs`'s `key_onboarding` arm arm the real tvOS press (`press::begin_ctl`/`take_commit`) on it. It was the one screen in the family with the FULL press treatment wired end to end; since 2026-09-04 `consent.rs`'s answers and Done are wired the same way, and this screen's `key` delegates its arrows to `route_screen`'s shared focus model like every other. **Its `pointer_hold` exists for `ui::press`'s "focus cannot move mid-press" contract**: `app.rs` calls it on every hover and cancels an armed press when the pointer has left what it was armed on, which the nav keys already paid for and the pointer did not. It judges by `route_screen::PressFrom` — a POINTER-origin press is bound to the control's own rect (sliding off into dead space retracts the click), a KEY-origin press to the focus stop (dead space moves no focus, so a key still being held is left alone) — and the arm expires against **`press::is_live()`**, so a locally recorded identity can never outlive the crate-global press it belongs to. **Not `is_active()`**, which was the same claim made falsely for one commit: a cancel only clears the commit and leaves the press on screen for its ~200 ms bounce, so an arm expired against `is_active` outlives its own press — long enough that pressing the pill, navigating into the list and pressing OK judged the ROW's immediate activation against the pill the user had already left, and the pin never flipped (`onboard::tests::a_press_abandoned_for_the_list_does_not_swallow_the_row_s_own_ok`; Codex review, 2026-09-04). `consent.rs` carries the twin, plus `ActionKind`/`arm_action` for the one identity focus cannot express: the Home editor's pill silently changes verb between `Try again` and `Start watching` when the roster lands mid-press. First run draws its own frozen `RouteGround::draw_home`; the Settings-hosted mode draws no ground of its own; `settings.rs`'s frozen host sample shows through instead. |
| **Control-ground correction (2026-08-30)** | The older `widgets.rs` prose below says an unkeyed ground changes “exactly two things” and that focus has the same fill on both grounds. That sentence is historical, not a live contract: a keyed Hero focus now takes hue from `gfx::sample_control_ground`, the broad linear-light mean of the pixels already rendered beneath its row; its authored lightness stays fixed. `Unkeyed` cannot read the hardware video plane, so it ignores that palette and keeps the fixed `ACCENT` focus and light idle film. Its two states share a 1.25px white-rim geometry: idle is .34 so the edge survives separate-plane video composition, focus raises it to pure white. No blur or refraction is drawn by this sampler. |
| `theme.rs` | **all color tokens, in two layers** (private PRIMITIVES — the palette, the only place a colour code is written down — then public ROLES that resolve to them; see rule 1) + the **`size` type scale** (HERO…DIAGNOSTIC; ordinary-product legibility floor CAPTION 24, with DIAGNOSTIC 20 reserved solely for `stats`) + the **`space` gap scale** (`XS` 8 / `SM` 16 / `MD` 24 / `LG` 40 / `XL` 64 — the sibling axis of `size`; a gap between stacked blocks comes from a rung, never a hand-tuned offset) + the **`alert` head ladder** (`alert::EYEBROW_LEAD` 24 / `GAP_EYEBROW_TITLE` 14 / `TITLE_LEAD` 44 / `GAP_TITLE_SUB` 12 — the four numbers §1B and §1C both spell for eyebrow → title → subtitle. A family token for `ALERT_PANEL_RAD`'s reason, and it had already drifted: §1C spelled it a third way, in measured cap heights plus `space::SM`, and drew its kicker **26px** above the name where its sibling drew 38. A rung ladder is the wrong tool — `SM` 16 is the nearest rung to BOTH 14 and 12, so rungs round two different gaps to one number and invert them) + `scrim`/`scrim_black`/`with_a`/`dim`/`mix` helpers + focus-ring geometry consts, + the **`logo` presence ladder** (`logo::HERO_AREA`/`HERO_H_MIN`/`HERO_H_MAX` and the COMPACT trio — how big a clearLogo is DRAWN, the third size axis: the rule is constant AREA, not constant height, because under a height clamp a 5:1 wordmark covered 5× the ink of a 1:1 emblem). The single palette + type/space/logo ladders. **`ROUTE_GROUND_FALLBACK`'s four `ATMOS_*` stops were corrected 2026-09-02** — see `route_screen.rs`'s row for the measurement; the values now spread ~7x in relative luminance rather than sitting within ~15 8-bit codes of `SURFACE_APP`, and still clear the same TEXT_TERTIARY/TEXT_PRIMARY contrast floors a real keyed hero ground does. |
| `mod.rs` | the retui core: `Painter` (cascading alpha/translate — draw through it, never call `gfx::*` directly from a screen; `ambient` is the four-corner field that writes OPAQUE pixels and `grad4` its per-corner-alpha twin, the only 2-D gradient the renderer has — `rect`'s is vertical only. `ambient` does honour the cascade, but as the only thing an opaque full-screen field can mean by it: an alpha below 1 mixes its corners toward `theme::SURFACE_APP`, so a page wash dips to the app's own ground for `nav`'s route transition instead of sitting there at full strength while everything else fades), `Rect` (+`cover`, the `background-size: cover` fit every full-bleed backdrop needs because `tex` maps UV 0..1 across the rect and would otherwise SQUASH a source that is not the frame's aspect)/`Size`/`Spring`/`Env`, the `View` trait, and the shared screen primitives `on_axis` (the ONE off-screen cull test the scroll flow uses to skip off-frame children — culling, not the `Painter::clip` scissor) / `hero_alpha` (the ONE hero-fade curve both screens call) / `hero_synopsis` (+`HERO_SYN_LEAD`/`HERO_SYN_MAXLINES`/`hero_syn_h` — the ONE hero blurb both screens build: `size::LABEL` 26 on a 36px leading in `TEXT_READING`, capped at 3 lines, with an optional bold lead run for detail's episode prefix. It is shared because for one release the two heroes disagreed about the rung, the leading AND the ink — home was MICRO/29/`TEXT_SECONDARY` until detail was redrawn from the mock in `aa598bf2` and home was left behind. Detail's values won: its canvas states them, the home canvas has no hero to contradict them, and the on-device reading is on that side. `hero_syn_h` exists because the block's height is quoted by two layout CONTRACTS that cannot open a font — home's "the clearLogo must not reach the tab bar" test and `widgets`' hero-scrim legibility table — and both carried the literal `87.0` with a stale comment beside it) / `ScrollColumn`+`Column` (the scroll-into-content container detail's below-hero flow is composed from). |
| `label.rs` | `Label` — single-run cap-band text (the layout ≠ paint primitive). Also `HAlign`/`VAlign`. |
| `text_view.rs` | `TextView` — multi-line cap-band text: pixel word-wrap + ellipsis + `measure_h`, wrap-cached. It owns the app's **truncation affordances, and they are ONE choice rather than two flags**: `fade_last(px)` reserves a zone at the column's right edge and dissolves a cut-off last line into it through `shaders/fs_text_fade.frag` (the About card's and the person bio's right-pinned MORE — the production treatment), while `trailing(run, col)` floats an inline run at the text's own ragged end (no caller today; kept for a block with no right edge to pin to). **Each builder CLEARS the other**, last call wins, because `draw`'s fade branch `continue`s past the inline-run block — a view carrying both would lose its mark on exactly the lines that fade. The dissolve band is `sz × 6.25 em`, not a literal: a fade reads by how many GLYPHS it spans, so it belongs to the ink, and 6.25 em is the About card's original 150 px at `size::CAPTION` exactly. A screen that pins its own MORE beside the fade asks `last_line_cap_y(top, drawn_h)` for the y — never `top + h − <the leading>`, which is a second copy of the pitch. **`edge_fade(top, bot)` (2026-09-02) is the SECOND fade axis** — VERTICAL, in absolute logical screen y, for a line crossing a SCROLLING viewport's clipped edge rather than a fixed truncation mark; it replaced `widgets::edge_feather`, whose opaque `SURFACE_PANEL`-tinted gradient read as a grey band over glass instead of the text itself dissolving. `draw` decides PER LINE whether it actually intersects a band (only those route through `text::draw_text_fade`'s program via the new `Painter::text_fade_v`; everything else stays on the plain path) and it is independent of `fade_last`/`trailing` — nothing in this app combines a truncation dissolve with a scrolling viewport today, so `draw` `debug_assert`s against a line trying to do both rather than silently picking one. `ui::person_bio`'s bio panel is the one caller. |
| `widgets.rs` | reusable leaves: `Button` (+`ControlStyle`, +`progress` — a left-to-right fill sweep that makes the pill its own timer; drawn as the face twice under a scissor so the label's ink flips at the sweep line instead of going illegible over half of it; + the layout companion `Button::pill_w`, the ONE pill width formula — every pill relabels from state, so the frame is measured from the label rather than fixed: both hero rows pass an icon (home's "Play"/"Continue", detail's "Play"/"Resume"), Home's status-screen Retry passes none. `TabPill` sizes itself with `TabPill::width` instead — a tab has no icon box), `TabPill` (+`TabStyle`) and the strip motion it is inked by, `TabStrip`/`Capsule` — **the ONE tab-strip motion**: a subtle capsule marking the SELECTED tab and a bright one marking the FOCUSED tab, both of which TRAVEL between pills instead of being repainted onto a new one (two, not one, because on the Library screen the selected tab is the section you are browsing while focus walks the row). Shared by the top tab bar and the detail season tabs, because the owner directive recorded above `TAB_PILL_H` makes the two rows one control and its `K_TAB_SCROLL` doc extends that to their motion — a highlight settling at a different rate from the row under it reads as two objects. A `TabPill` is a retui LEAF and so structurally *cannot* animate between pills, which is why the state fills are hoisted into the strip and the pill keeps only a label, its constant plated ground and `TabPill::mix`-driven ink; the ink is derived from `cap_cover` (how covered the pill is) rather than from a boolean or a second spring, so it can never disagree with the fill sliding under it. Draw the capsules BEFORE the pill loop — they are the pills' ground — and note the plated case composites in two layers now (`theme::TAB_PLATE_SELECTED_OVER` under the pill's own idle plate, `a + b − ab` back onto `TAB_PLATE_SELECTED`, which the tests assert rather than the eye), `TransportButton`, `CircleButton` (+ the **UNFURL**, `CircleButton::label`/`cap_w`: a focused disc grows rightward into a capsule carrying the verb its press performs — icon-only at rest, LABELLED on focus, because a disc is the one control that cannot say what it does and a television has no hover to hint with. `e == 0` is the plain 60px disc, so opting in costs a caller nothing. Two rules travel with it: the width is ONE expression (`cap_w`) that the painter AND the row's accumulation/hit-test both read — pass the resulting rect through `frame`, never `at`, which keeps the bare disc and clips the label to a circle — and the label's fade is derived from the GEOMETRY (it comes in exactly as fast as the tail air opens behind it) rather than a chosen fraction, so a half-cut glyph is never legible. Sibling of the profile chip's own unfurl, and on the same `K_DISC_UNFURL`==`K_CHIP` for that reason. A caller with a hard right edge asks `label_budget` first and shows the whole verb or none of it — `detail::watch_cap_at` is the worked example), **the CONTROL TREATMENT all three faces share** (`Button`, `CircleButton`, `TransportButton`) — which changed on 2026-08-22 and is worth knowing before you re-add half of it. A control face carries **no drop shadow AT REST**: it is held by its EDGE (`control_rim` — the card's own `CARD_SHEEN` .22 round the perimeter, boosted to `GLASS_RIM_LIGHT` .28 on the side facing the light, weighted by the surface normal). **Focus is an elevation** and adds `theme::CONTROL_CAST_FOCUS` under the face (2026-08-30) — two stops of black, a contact and a pool, and nothing a resting control wears. Note the conversion, because it is the thing that gets transcribed wrong: the design writes the cast as CSS `drop-shadow`, whose blur is a standard DEVIATION, and this renderer's is a box-shadow's DIAMETER, so 16 and 3 arrive here as 32 and 6. **And a capsule is NOT a stadium.** Every control face wider than it is tall draws to the design's blended outline — three circular arcs per corner (big arc → blend `f = √(R·r)` → end circle), solved in `ui/pill.rs` and evaluated by `fs_src.frag`'s `sdPill`; a DISC stays a circle. Two things travel with it. The box a capsule is DRAWN in is taller than the frame it was laid out with (`widgets::face_box`, `pill::box_h`), because the end circle is under half the box and the ends have to come out at the frame's stated height — so no layout number moved. And the outline is solved in `f64` from a closed form: the big arc is ~73 000px on a 260×61 control, `f32` cannot tell `hypot(100, 1e9)` from `1e9`, and a bisection would be ~1200 soft-float library calls per control per frame on an armv7 television. Measured, and worth knowing before judging a screenshot: at `PILL_END_R` .492 the side bows by **0.49px** at this app's control height, so what actually reads is the blended CORNER, not a pillowed side; `PILL_END_R` is the dial. **One thing overrides that edge and it is the GROUND, not the state — `ControlGround`.** Everything the treatment does assumes the ground can be SAMPLED, and the player cannot: the picture is on the hardware video plane, never in our framebuffer, undimmable by any scrim of ours and different every frame. So a surface standing on video declares itself `ControlGround::Unkeyed` (`Button`/`CircleButton`/`TransportButton` all take a `.ground()` builder; the player HUD's control row, the skip pill and the post-play card's pair are the only callers) and exactly two things move: the IDLE face becomes a light FILM (`theme::CONTROL_IDLE_FILL_UNKEYED`, white .10 — over the HUD's own ramp a white film is lighter than its ground on EVERY frame, where the dark plate is a hole punched in a bright one), and the FOCUSED edge goes to pure white at 1.25px (`theme::CONTROL_RIM_FOCUS_UNKEYED`, the app's one rim that is a state). **FOCUS itself does not move** — same fill, same ink, same pop on both grounds — and neither does anything else, or it would be a second design system. `Keyed` is the default on every widget including `TransportButton`, whose only caller is the HUD: a widget that assumed its caller's ground would be the one control whose look came from where it was used rather than from what it was told. The design system says all of this as a CSS scope (`[data-ground="unkeyed"]` in `tokens/colors.css`, drawn in `components/core/control-unkeyed.card.html`). **The HUD's Info/Chapters `TabPill` takes it too** (owner directive, 2026-08-30: on the player, transparent) — and it had to gain the EDGE in the same move. It consumed `CONTROL_IDLE_FILL` but drew a bare `rrect`, and a 10% film with no rim has nothing holding it off its ground; the standalone `TabStyle::Button` pill is a control FACE — the same object as the disc beside it in that row — so it now draws through `control_rim` like the rest of the family. A `TabStyle::Segment` keeps the bare fill and takes no ground: a segment is inked by its strip's travelling capsules and stands on that row's own track, which is never video. The perimeter used to be `GLASS_RIM` .14, the GLASS container's line, which `tokens/glass.css` scopes to a surface you read THROUGH while saying in the same breath that every control is flat — a flat control wears the card constant. Both faces also used to draw `CARD_SHADOW_REST`, on a measured argument kept written down at `Button::draw`: an ACCENT capsule over a WHITE frame measures ~1.2:1 against its surround. That is what the focus cast now answers, and the half of the old rule that survives is the resting one: the control family still has ONE elevation, and it is focus. A rim could never have been the lever there — a near-white line on a near-white ground has nothing to separate. And every control face takes the **FOCUS POP**, `CTRL_FOCUS_SCALE` 1.07 (`--focus-scale-control`), through a `scale()` builder the CALLER supplies — the same split `ArtTile` and `card_row` already use, because the pop is a property of a control's place in a ROW and the widgets keep nothing between frames. `CtlPop<N>` is that row state: one spring per control, stepped once a frame from the screen's `update`, so the control being LEFT keeps animating after it has stopped being the focused one (a single global scalar could only snap it back, a visible 4px jump). Its spring is **critically damped on `consts::K_SCALE`** — the TILE's focus spring, the same one `card_row` steps every shelf with, so a capsule and a poster answer the same event at the same rate. **It does NOT bounce**, and getting that wrong is the mistake this sentence used to BE: it read "underdamped, borrowing `press`'s own `K_POP`/`ZETA_POP`, because the design system names exactly two things that bounce, the click and a control arriving at focus". The design system names **one**. `tokens/motion.css` opens with "the only thing that BOUNCES is the press release: focus ARRIVING is a calm grow, the CLICK is what rings", and the `--ease-bounce` token's own comment says to use it "for the press spring-back and NOTHING else — never a focus pop, a fade, a slide, or anything that carries text"; `Button.jsx` reaches for it only while `releasing`. Three artefacts agreed with each other and none had been checked against the source — the doc, the constant's doc in `press.rs`, and a test that asserted the overshoot. The owner saw the bounce on a television. **The press release is now the only underdamped spring in the app** (`grep step_zeta`), which is the invariant to keep. **And `CtlPop::scale` folds `press::scale()` in for the focused control, which is the CLICK** — the dip on OK-down, the ring on the release, the activation a beat later so the ring is on screen (`ui/press.rs`, `tokens/motion.css`'s `--press-dip`/`--k-press-*`). That fold was there from the start and read a permanent 1.0 until 2026-08-22, because a press was only ever ARMED on a card: every control in the app activated on the key-DOWN, so there was no interval in which a dip could exist. Arming it is `app.rs`'s, through `press::begin_ctl` (the control twin of `begin` — same animation, no long-press latch, because nothing grows a context menu out of a Play pill and a firmly-held OK must still act), and the door a screen opens it through is a `focus_is_ctl()` predicate beside its existing `focus_is_card()`. **A new control row owes one**, or its faces will pop on focus and sit inert under a press. The seven that have one today: the home hero's action row, the detail hero's, the player transport's control row, the Info card's action column, a decision alert's two answers, the profiles footer and the onboarding action pill. The pop is a factor on the FRAME and never feeds a row's accumulation or its hit test: the popped face strictly contains its resting rect, so every pixel that was clickable still is (`home::focused_card_rect`'s trade). It does NOT scale the label's type — rule 2 has no rung between 28 and 32. **A control inside a TRACK does not pop** (the top tab pills, the profile chip: their row's own motion is their focus mark), and neither does `StatusOverlay`'s action pill, which is the only focusable thing on the region it owns and so has no sibling to be distinguished from — that one is argued at its draw), `Spinner`, `PageDots`, the non-interactive read-out leaf `FieldList` (+`Field`/`Tone`, `FIELD_ROW_H`) — a key/value page for `stats.rs`, and deliberately NOT a `TableView`: that is a selection widget that paints an accent pill under `sel` on every draw with no "nothing selected" mode, its 60px rows put ~25 fields at 1540px against a 1080 panel and SCROLL them behind a scissor, and its row anatomy has no right-hand value column at all. Owns no state, no focus and no springs, `AmbientWash` (the full-screen four-corner wash keyed to an item's `UltraBlurColors` — it is a component because the FADE is the non-obvious part: `Painter::ambient` writes opaque pixels and ignores the cascade alpha, so a wash cannot be cross-faded by alpha at all and the only way is a spring per corner channel. Build the target through `AmbientWash::target`/`keyed` rather than a per-screen mix loop: both blend from `SURFACE_APP`, which makes "no artwork" the app's own flat ground with no special case, and `keyed` additionally holds every ARTWORK corner under the private `GROUND_LUMA` ceiling so a white poster cannot outshine the `TEXT_TERTIARY` fine print that sits on the ground. `K` is the one dissolve rate and `GROUND_W` the one item-keyed strength; `is_flat` is the skip test for a wash that has resolved to the clear colour, at the shared `FLAT_EPS` — a screen whose wash resolves to the app ground must apply it, or that page pays a ~2M-fragment opaque pass a frame to write pixels that are already there), `StatusOverlay` (+`StatusKind` — the Working/Failed/**Empty** treatment, in up to THREE blocks each answering one question: the **verdict** (what happened), an optional **reason** (why, and what is NOT broken) and an optional **action** (the one thing to press). The player draws it for the states where **no picture is on the panel** (Resolving/Connecting/Buffering/Seeking before this session's first frame, and Error) — a seek over a live picture belongs to the transport's inline spinner instead, and `player_hud::busy_surface` is the ONE rule and the only place the two surfaces are divided — Home draws it over its loading/error/empty hub states, and `library.rs` over a section whose SOURCE did not answer. The *caller* supplies the caption so the state machine stays the single source of that string, and it is a borrowed `&CStr` rather than a `&'static` one because a section read-out interpolates a machine name (“Can't reach <machine>”) — the `Label` lifetime rule applies to a runtime one. The **action pill is the component's**, not a screen's: Home hand-built its Retry beside a shared caption, so one block was laid out by two pieces of code, and the Library's read-out would have been a third copy; `action_frame` is the ONE geometry the draw and the screen's hit test both read. Layout hangs off ONE anchor — the caption band, in exactly the place it occupied before either optional block existed — and the optional blocks stack BELOW it on `theme::space` rungs, so adding one cannot move a read-out that carries neither. `Empty` is deliberately NOT `Failed` — a library with nothing in it is an answer, not a fault, and must not wear the danger tint. The tint is the KIND's; the action is the CALLER's, and the two differ on purpose: the Library's empty section offers none (asking the same question again gets the same answer) while Home's empty hub offers Refresh (there, “nothing yet” is most likely a server still scanning). Neither is a retry of a failure), the shared art-tile core (`card`/`draw_card` + `Art`) and the watched-state marks that ride it — `watched_mark` (the poster's corner TICK: a bare white check over `tile_mark_veil`'s radial corner falloff, the only thing that touches the picture, plus the tick's own soft shadow; `veil_tex` explains why it is a cached texture and not geometry) and `art_scrim` (the bottom gradient that lets a label sit directly ON artwork with no capsule behind it). Its **sibling** — same "marks that ride artwork" job, deliberately not a flag on it — is `hero_scrim` (+ the pure `hero_scrim_quads`/`hero_scrim_a`/`hero_scrim_right_a`), the CORNER wedge both full-bleed heroes draw so their text column is readable over a bright backdrop: `art_scrim`'s whole body is the scissor-vs-fill seam on a rounded CARD, while a hero has no arcs, no scissor and a different axis. It exists because a bottom-up ramp cannot fix a left-corner problem — the hero title's cap top gets only ~0.11 of one, and raising the ramp there flattens the whole picture on the same line — and it is built on `Painter::grad4` rather than N abutting strips, which at `theme::SCRIM_TEXT_A`'s peak would need ~36 quads and a visible alpha staircase (`GL_DITHER` is off). Its alpha field is pure and its legibility promise is an executable anchor table in this module's tests, read from `home::base_scrim_a` / `detail::base_scrim_a` / `detail::hero_chain` so the contract and the paint can never be two different curves — and `hero_scrim_quads` builds every quad CORNER by evaluating `hero_scrim_a`/`hero_scrim_right_a` at that corner, so the drawn field and the graded one are one expression rather than two that agree. Its `right` wedge is opt-in and belongs to whatever right-aligned copy asked for it: detail passes `has_people`, the same test that decides whether the right-hand people column is drawn at all. The ATMOSPHERIC ramp under it starts at `HERO_BASE_SCRIM_Y0`, shared by both heroes (the two screens' curves BELOW that line are deliberately not — home's has a midpoint knee, detail's is one linear stop). **One vocabulary, one mark at a time, and only TWO of the three states are marked at all.** A POSTER wears the corner tick when finished, the full-bleed amber resume bar when in progress, and **nothing at all** when it has never been started — most of a server is unstarted, so a clean shelf is the common case and a mark is information. (Both halves of that turned over on 2026-08-13: the corner carried an amber ANGLE meaning *unwatched*, became an amber check DISC meaning watched, and settled the same evening on the design system's bare TICK over a veil — amber is the resume bar's alone now, so the two states cannot be confused across a shelf. `widgets::card` records the reasoning; `angle.svg` is deleted.) A landscape STILL wears one state line on its scrim — `▶ 48 min` never started, `12 min left` + the same full-bleed bar in progress, `✓ 48 min` done, its tick in the LINE's own ink because only the invitation earns the amber (`detail::ep_state` is the resolver, and its doc has the table). The bar is `card_row::resume_bar`'s treatment in both cases — full-bleed on the bottom edge, square fill end, clipped to the corner arcs — because it is meant to be *the same bar*: the still used to draw an inset rounded capsule 16px up while a Continue Watching card drew the full-bleed one, so "how far in am I" was two different objects on two screens of one app. A new tile type's state mark belongs here, not in the screen — plus the metadata chip `badge`/`badge_w` (+`BadgeStyle`), which takes an OPTIONAL leading glyph; its `BadgeStyle::OverArt` (the neutral fully-rounded capsule for riding ARTWORK) has **no user today** — it backed the episode still's old duration pill, which `art_scrim` + the state line replaced — and is kept because it is the right answer the next time something has to sit legibly on a thumbnail. Also the review-score row `rating_group`/`rating_group_w` (+`RatingCell`/`MarkLayer`): the unit of flow is the PROVIDER, not the score — its name as a quiet `MICRO`/`TEXT_TERTIARY` caption, then its score or scores, so Rotten Tomatoes' critic and audience readings share one caption while IMDb and TMDB get one each (`metadata::RatingArt::provider` is the grouping key, and `rank` already orders equal names adjacently). A `RatingCell`'s `mark` is a back-to-front slice of tinted masks — one per colour, so the tomato keeps its green calyx — and is EMPTY for a provider with no verdict to draw: IMDb and TMDB publish a number and nothing else. **Rewritten 2026-08-02**, replacing four saturated brand marks (RT's fruit and popcorn tub as silhouettes, IMDb and TMDB as logotype chips in brand colours) with our own verdict marks and words. RT's had no licensing route; the chips were reproducing two more logotypes to answer "whose score is this?", which a word answers for free and lawfully. The point is what carries the colour: the captions recede to caption weight and the only colour left in the row is the verdict. And the poster-resolve family, which all share ONE key builder so a prefetch and the draw that follows it can never name two different slots (`(server, path, w, h, png)` IS the store key): `resolve_tex` (texture or 0), `resolve_tex_wh` (+ the decoded pixel size, for anything that must `Rect::cover` rather than stretch) and `warm_tex` (start the fetch, take no texture and no LRU evict-protection — `posters::poster_warm`). Each has an `_on(ServerId, …)` twin, and the bare form is exactly `_on(plex::current_server(), …)`: a thumb path is a key on the server that issued it — rating keys are server-local integers from 1 — so art belonging to an item from a SHARED server must be resolved with that item's own server or it is fetched from the wrong host, with the wrong token, into a slot the other server's tile will then hit. Plus `Spinner`'s two named radii, `R_PAGE` (a wait that owns a whole surface) and `R_INLINE` (a mark beside one line of text), with `Spinner::dot_r` the one place the dot/ring ratio lives so a layout can measure a spinner without re-deriving it (`StatusOverlay::above`). **There is still no `ProgressBar` type**: the HUD scrubber is immediate-mode inside `player_hud.rs` (see the deliberately-immediate-mode gotcha below). `Button::progress` (the fill sweep that turns a pill into its own timer) is the app's ONE countdown idiom, and `up_next.rs`'s *Next Episode* pill is its user. If you need a standalone bar, that is the moment to promote one here, not to assume it exists. `ControlStyle::Keyline` currently has no caller — like `BadgeStyle::OverArt` it is kept as a finished variant of a shared control (the SDF has no stroke-only mode, so its knockout construction is the non-obvious part), not as a promise that some screen draws it; **`ControlStyle::Custom` joined it 2026-08-21**, when the detail hero's watch control stopped inking a watched check amber and became two discs whose GLYPHS carry the state (`detail::hero_ctls`) — the two `style()` builders are now the way to reach a variant nothing draws, which is worth knowing before you assume a caller exists to copy. A third variant, `ControlStyle::Bare`, was added for the home hero's pager and **removed again the same day** — the pager is not a control at all. It is worth a line because the next person to reach for a bare-looking button will re-derive it: idle it painted no plate (no fill, no rim, no shadow, the glyph alone at `TEXT_SECONDARY`) and focused it took the family's `Accent` face, gated by a `plated(focused)` predicate both `Button::draw` and `CircleButton::draw` asked before drawing their whole face — a transparent fill alone is not enough, because the rim would still ring the shape and the shadow would still cast under nothing. It went because the owner's correction ("chevron should not be even selectable") made its only caller an INDICATOR: a mark drawn with `icons::draw` at its natural size, with no frame, no focus and no hit rect (`home::hero_actions`). The lesson is the ordering — a style that strips a control's plate is answering "should this be a control at all", and that question belongs to the screen, not to `widgets`. A bare MARK's idiom lives in the Library A–Z rail, where it always did. **The `profile_chip` is here too, whole** — the visual, its `CHIP_FRAME` (a CONSTANT, unlike `tab_pill_at`'s recorded rects, because the pills scroll and the chip never moves), its `profile_chip_at` hit test and its unfurl spring, stepped in `tab_row_update` with the strip's own motion. It used to take its rect and its focus amount from the caller, which is how a control drawn on three screens came to be activatable on one: each screen wrote out the same rect, only Home also recorded one for a hit test, and the other two passed a hard-coded `expand: 0.0`. What a screen still answers is `TopFocus` — `Away` / `Chip` / `Pill(i)`, ONE value rather than two booleans that could both say yes — which `tab_row_update` animates the bar from and `app.rs` routes OK on. **Its focused capsule wears the tab track's MATERIAL, not a copy of its weights**: `draw_tab_row` publishes the face it solved this frame (`BarMaterial`) and `chip_capsule` draws the band's second glass surface from it, so there is one `gfx::sample_ground` solve for the whole bar and the two capsules can never be two densities. Three things follow and each was a bug first — the chip is skipped during a blur SOURCE pass exactly as the row is (a surface may not appear in its own backdrop: measured over `flat:92`, the capsule's face came out at 61 against the track's 142 before it was); the unfurl fades every alpha of the face and not just the tint, because `fs_glass.frag` lets the rim exceed its own coverage and a tint-only fade leaves a bright empty hairline; and `GLASS_TRACK_MAX` is now solved so the widest capsule clears the widest glass track — there is ONE blur cache, so an overlap would double the frost and the rim rather than blur twice. **And one thing that is NOT settled, recorded where the claim is made rather than left for the next reader to rediscover: the shared face carries no contrast promise about the chip's own ground.** `track_alpha_for` solves a scrim weight so `TEXT_READING` clears `TRACK_INK_CONTRAST` over *the pixels under the track*, and the chip is ~800px outside that rect — so on a hero that is dark under the centred track and bright under the chip, the unfurled capsule's name reads **1.86:1** where the flat capsule it replaced read 9.74:1 (measured through the module's own solve; `the_bands_face_carries_no_promise_about_the_chips_own_ground` pins it, `BarMaterial`'s note has the table and the three candidate fixes). Nothing darkens the top band before this, and `gfx::sample_ground`'s census puts the median hero at 26.8 L* of span across the track alone. Judge it on a ground that varies in luminance ACROSS the band — `pat:ramp` / `pat:edge` / `pat:orient`; every pattern the material was verified on is uniform across x or uniform in L*, and so cannot show it. Plus `text_block_highlight` (+`TEXT_BLOCK_HL_RAD`) — the focus mark for a run of PROSE rather than a card, i.e. detail's About columns and its episode filmstrip's metadata row, which this guide has always called one idiom and which drew it as two independent `p.rrect(…, OVERLAY_FOCUS_SOFT)` calls until they were promoted here. It is the 7%-white wash **plus a drop shadow of its own** (`theme::TEXT_BLOCK_SHADOW_BLUR`/`_DY`/`_A`), because at that weight the panel has no edge of its own over an ambient ground and reads as a smudge; lifting the wash instead would put a bright rectangle over the prose it exists to point at. **No stroke, no rim, no glass** — the owner ruled all three out for a selection (2026-08-21). This guide said the opposite of the truth for a month — "No shader work, `Painter::shadow` already discards the occluder interior, which is exactly what a translucent panel wants" — and that sentence is how the FRAME shipped. `Painter::shadow` discards a BOX inset by `radius + 1`, which leaves a full-strength band of ink under the occluder's own rim, ending in a hard step: invisible under a card, and under a 7% wash a dark rounded frame with the wash inset inside it. Use `Painter::shadow_outside`, which cuts the occluder's rounded SHAPE (the `u_cut` uniform in `fs_shadow.frag`), and the translucent triple rather than `CARD_SHADOW_REST_*` — a contact shadow tuned to be seen only where it escapes a poster has a readable boundary when the whole pool is on show. **And the key-cap family** — `key_cap`/`key_cap_w` (one remote button named in prose: `MICRO` bold inside a 36px keyline, min-width 74 so `OK` and `BACK` are one object) and `KeyHint`, the `{pre} [KEY] {post}` line assembled around it. It is here because three alert panels wanted it at once, RIGHT-aligned against their own padding edge, so `KeyHint` places by x and each caller solves its own alignment. `player_hud::draw_hint_with_keycap` is deliberately NOT migrated onto it: hollow-vs-knockout is a visible brightness change on the one screen whose brief is surviving a phone photograph, and that is a device judgement, so the read-out keeps its own cap until it can be graded on the panel. The cap is a genuinely hollow `Painter::rring` in the LABEL's own ink, which is `keyline_chip`'s construction and its measured reason: at a 1.5px stroke the SDF's rim coverage is the product of two 1.5px smoothsteps whose overlap is about a quarter of a pixel, so a stated `white .34` resolves to roughly a tenth of it and the outline disappears. The player's original got away with a KNOCKOUT (white rect, interior repainted opaque black) only because the video plane behind that read-out is black by construction; on glass that interior is a hole. **Four more leaves landed with the bio ALERT panel (`ui/person_bio.rs`), one of them a PROMOTION rather than new code** — the pattern to copy when a screen needs something a second screen already draws. (The key-cap family above is the fifth and the promotion that mattered most: three alert lanes each promoted it out of `player_hud.rs` independently, under three different names, which is why the reconciliation kept one and why the HUD itself was left alone.) `dotted_run` came out of `detail.rs`: fine-print facts joined by a `·` in `theme::TEXT_SEPARATOR`, where an EMPTY part contributes neither a draw nor a separator — the reason it is a component and not a `join(" · ")` is that the dot is a different ink from the words it divides, which one `p.text` call cannot express. `scroll_rail`/`rail_geom`/`RAIL_W` is the 6px pill rail for a PAGED viewport — quantised to pages, not pixels, so one press moves the fill by exactly its own height and the last page ends flush with the track. `edge_feather` is GONE (2026-09-02) — it used to soften a **scissor-clipped** viewport's top/bottom by painting an opaque `SURFACE_PANEL`-tinted gradient over the seam, each edge drawn only when there is content on the far side of it; that reads fine over an opaque sheet and as a distinct GREY BAND over GLASS, which the person bio panel is. `text_view.rs`'s `TextView::edge_fade` replaced it: the crossing line's own glyph alpha dissolves against whatever is actually behind it (through `text::draw_text_fade`'s vertical bands), so nothing new is painted at all — see that row. And `tracked_run`/`tracked_run_w` draws a letter-tracked kicker: the text backend has no letter-spacing, so tracking is always a per-character pen advance, and the label is pre-split into `&CStr` literals because it is only worth doing for a CONSTANT word. |
| `decision_alert.rs` | **The shared yes/no alert** — a centred glass sheet (`theme::ALERT_PANEL_RAD`, the one panel radius that is a TOKEN rather than a module-local `PANEL_RAD` — shared with the three read-only alerts, and the thing four lanes each invented separately) carrying a question at `size::TITLE`, an optional body, and two EQUAL 260-wide answers, focus resting on the safe one. Since issue #75 it has **two** shipping hosts, each choosing its own second-answer face through a `Tone`: consent's *Delete all local data* sets `Tone::Destructive` (the default; the safe answer stays `ControlStyle::Accent`, the destructive one `ControlStyle::Danger`), and the sign-in screen's one-off *Send report* (`ui/login.rs`) sets `Tone::Neutral` — the press leaves nothing destroyed, so both answers read as ordinary controls with no red. `exit_alert.rs`, which asked "Exit PlxNative?" at Home's root, was RETIRED on 2026-09-03 when a root BACK stopped asking anything and started handing the screen back to the television (`webos::go_home`). Three things are worth knowing before editing it. **It is a `Popover`, not a `Route`** — a whole-frame modal is a route when it can stand on more than one page (`Account { over }`, `ItemMenu { over }`) or lives in the player's `Overlay`; an alert with one host forever would instead be four more places (`route_wears_tab_bar`, `page_of`, `Nav`, `node_route`) obliged to keep answering a constant. Its modality is positional instead, and the two hosts get it from different arms: for consent's alert, `app.rs` checks `consent::is_open()` in both the key ladder (`key_onboarding`, ~7741) and the pointer chain (~8102/8206), and both `continue` unconditionally so nothing behind the sheet can be driven; for the sign-in report, the same guard is `ui::login::modal_open()`, read at the `Route::Login` arm of both `key_onboarding` (app.rs ~5091) and the pointer branch (app.rs ~8598), with `login.rs`'s own `alert_press_at`/`alert_hold` taking the hit-testing from there — since issue #76's report lane, `modal_open()` answers for a SECOND modal on that same route too, the storage Details panel opened from the sign-in screen's QR/status read-out, on the same `visible()`-not-`is_open()` predicate as the alert. **Geometry is the pure `layout(q_h, body_h)`** for `up_next::layout_of`'s reason — the impure half measures through SDL2_ttf, which the host suite cannot LINK (a test that calls it fails at `ld`, the boundary `StatusOverlay::bands` documents) — so every placement decision is gradeable without a window while the one measurement is the caller's, and `body_h == 0.0` is byte-identical to the geometry that existed before bodies did. And **a pointer MISS is not a dismissal**, the one place this differs from every menu in the app: "not that one" answers "which of these?", but "not either" answers nothing at all to a yes/no question. |
| `card_row.rs` | `CardRow` + `RowStyle` — the animated shelf component shared by the home grid, detail Related, the library grid, the person page's shelves, the search result shelves, and (circular, via `RowStyle::PROFILES`) the who's-watching avatars. `RowStyle::HOME` is the single source of shelf motion+geometry; the row owns per-cell scale springs + the scroll spring and exposes `draw_tile`/`draw_focused` plus the `reveal`/`scroll_into_view` scroll math. **`strip` is the whole-row loop** (non-focused tiles first, focused last, `on_axis`-culled so only VISIBLE tiles hit `resolve_tex`) — detail's Related/credits rows and the person page's shelves all run it; reach for it before writing another per-screen tile loop. The focused tile's metadata block is a **`TileLabel`** (title + optional caption + the Continue-Watching play glyph), and `TileLabel::height(has_caption)` is the ONE authority on how tall it is — every screen must reserve that band in its own flow, because `reveal`/`scroll_into_view` only keep air under the block a screen *declares*, and five call sites used to hand-author the number from constants they couldn't see. **The focused title is ONE path everywhere now**: a single elided-when-it-fits line, and a looping `title_marquee` under the SAME widened budget whenever it does not — its `glyph: bool` parameter leads the line with Continue Watching's play triangle without being a fourth title path, sharing the marquee's clock and budget arithmetic (the icon's width + gap subtracted from the window first). There is no more word-wrap: `RowStyle::title_lines` and `wrapped_title` are gone, so a shelf of real Plex titles ("Wallace & Gromit: The Curse of the Were-Rabbit") marquees instead of wrapping to two lines, which is also why `TileLabel::height` no longer takes a `RowStyle` — the label band is now fixed for every row. Home still keeps its own x/scroll loop, because its focused-last pass is **cross-row** and lives in `Grid`. |
| `hero_logo.rs` | `HeroLogo` — the ONE clearLogo sizing rule, shared by the home hero, the detail hero and the detail page's pinned compact title (they used to pass `posters::logo_tex` three different literal boxes — 660×96 / 680×120 / ∞×54 — so the same mark was three sizes in one app). Sizing is CONSTANT AREA (`theme::logo`), not a height clamp: under a clamp the drawn area is linear in aspect, so a 5:1 wordmark covered 5× the ink of a 1:1 emblem, which is why square logos read as tiny; the clamps apply in a fixed order — area, then the rung's floor/ceiling, then the caller's column LAST, the one bound allowed to break the floor. The band a caller reserves in its flow is `band_h(rung)` — a CONSTANT — and a taller logo spills UPWARD as paint (layout ≠ paint), so a landing texture never reflows the hero and two pool pages with different logo aspects stay level through a flip. It also owns the fallback-to-title text, which used to be three different (and two of them unelided, one on a hand-placed magic y) runs. `posters::logo_src` hands back the texture and its TRUE pixel size and owns no layout policy. |
| `table.rs` | `TableView`/`Section`/`Row`/`Badge` — the animated list (settings/track-menu look). A cell has **FOUR content places**, in this screen-x order, and a row that has something to say should use the one that fits rather than crowding an earlier one: the **leading column** (`checked`'s tick or `licon`'s action glyph — one or the other), the **label over its optional `detail` sub-line** (which takes all the slack, and both runs elide to the same right edge), the **trailing read-out** (`value`/`toggle`), and the **right-aligned `badge` run** outermost. The badge run is RIGHT-aligned and centred on the ROW BOX — it was drawn inline from the label's own drawn right edge and centred on the title's cap band, which gave a chip the design pins flush a per-row x and, on a two-line row, floated it ~16px high; a column of resolution classes or FORCED/SDH tags read as ragged text instead of a column you can compare down. Its ring is `theme::OVERLAY_BORDER`, not the label's ink. Rows carry an optional **leading icon** (`Row::licon`, sharing the column the active-row checkmark owns) and there is a **`Row::separator()`** grouping hairline that `move_sel`/`hit_row`/`set_sections` all refuse to focus — build a divided action list out of ONE section with a separator row, not out of two sections (a headerless second section adds air but draws no rule, because the hairline rides the section header). A row's trailing slot is ONE read-out — `Row::toggle`'s `On`/`Off` or `Row::value`'s word, resolved by the private `readout` so the two can never both draw — with `Row::value_dim` quietening it a step. The **`Section` carries the group's own two facts**: `accessory`, a right-aligned run on the HEADER line one rung down, elided to `ACCESSORY_W` because it is the last run there and so the one that gives way (a long plex.tv handle truncates on a character rather than widening the panel); and `Section::dim`, ONE alpha over header, accessory, rows AND the selection pill — not `Row::dim` applied row by row, because the state it expresses (an unreachable server) is a fact about the group, and a dimmed row's own `On` must stay legible as itself since nothing was unpinned. |
| `icons.rs` | `Icon` enum + antialiased SVG rasterizer; color is the `tint` you pass. **Adding an asset has two non-obvious rules** (module doc has the full contract + the failure each one prevents): a mark is **ONE `<path>`**, composed of overlapping SUBPATHS — separate elements are alpha-composited and crease along the join, subpaths are winding-unioned and don't — and **every subpath winds the same way**, since nonzero fill turns a reversed one into a hole. Author at the size it is DRAWN (the rating marks are drawn for a 34px box, not shrunk from poster art) and grade it by rasterizing through `src/svg.c` at 1× and 4×. |
| `stats.rs` | **Playback diagnostics** — the on-screen engineering read-out toggled from the player's `…` overflow popover (`more_menu.rs`); dev triggers may open it directly for automated captures, but it is not a global profile-menu setting. A latched flag drawn LAST over live playback, sampled at 2 Hz and HELD so a phone photograph captures one coherent `player::Diag` snapshot. Its wide, shallow opaque card uses `size::DIAGNOSTIC` 20 and two FIXED schemas: **STREAM / OUTPUT** follows connection → route → codecs → plane → transfer/load/feed, while **DELIVERY / CONTROL** keeps Mode / Quality / Sample / Conservative / Buffer / Risk / Acquisition / Action / Reason at the same y in Manual, Auto/Original and Auto/HLS. An inactive model says so instead of deleting rows. `Sample` is explicitly stream-limited (not a connection-speed claim); `Acquisition` is end-to-end request cadence rather than an independently identified PMS/encoder constraint; observed buffer slope and the model's conservative horizon are separate rows. Values use cached pixel wrapping, never elision, and the panel grows rather than clipping a suffix. Before any playback, the same card shows the device/firmware/decoder/server facts instead of fabricating zeroed pipeline faults. Read the module doc before adding a field: it carries the redaction rules (no URL/path/token, server name/address/machineIdentifier, or item title). |
| `alt_sources.rs` | **"Also available"** — the same film held by a second pinned source, as a `Popover`+`TableView` list (Shared Sources, deliverable E). Three things live here and nowhere else. The **gate**: `is_available()` counts distinct SOURCES, not copies, and the detail page's actions row asks before it draws a control at all — so a one-server library pays nothing, no query, no pill, no layout, which is the same "with one source, none of this is drawn" rule `home.rs`'s shelf annotation implements as absence. The **row model** `rows()`, pure: `[library] [owner · runtime] [resolution class]`, ordered *the copy that plays first*, then quality, then yours before a friend's at equal quality (the design's own example is the case that separates the first two — a 1080p copy you are standing on outranks a friend's 4K one), with **exactly one** row ticked, marked on the first copy matching the page's own (server, ratingKey) so a duplicated input cannot say "you are here" twice. And the rule that **OK NAVIGATES** to that server's page rather than swapping the copy in place — which is also what settles the per-server RESUME POSITION, since a copy switched under you would have to explain a position that jumped. (Not the watched flag: that one follows the title across every source now — `viewstate`'s fan-out, `docs/shared-servers.md` §11 — and the two halves have to be kept apart, which is the correction `alt_sources.rs`' own module doc carries.) It only reports an `Action`; `detail::open_alt_copy` re-points the registry and raises the page's ordinary open request. Copies come from the item's **`guid`**, never its `ratingKey` (both servers in a household have a `ratingKey` 4) — `install()` is the seam the multi-server data layer feeds, empty until it lands, with `/tmp/plxnative-altsources` as the draw-only stand-in the device captures were taken through. That collision reaches the STORE too, which is why it is a **mailbox keyed on the `(server, ratingKey)` pair**: `reset()` stamps both from the row that opened the page and `install()` matches a landing against both through `plex::same_item`. A resolve is one round trip PER SOURCE and a dead share costs a whole `connect(2)` timeout, so one asked for the page you just left routinely lands on the page you just opened — and `metadata::pump_alt_sources`' generation guard cannot see it, because it only moves when a DETAIL lands and the new page's has not. Matched on the key alone, the panel listed the other machine's copies and OK opened a different film. |
| `about_panel.rs` | **About** — the detail page's About footer read in full, as a glass ALERT (`Alert Views.dc.html` §1A). Opened with OK from the footer's **card** (section 5, col 0): the design's rule is that *a block earns an alert only when the page truncates something worth reading in full*, and the card is the one that does — its synopsis is capped at five `CAPTION` lines. Every CLICKABLE About block wears a `MORE` whether or not its text truncates (2026-09-02) — the card (opens §1A) and Languages (opens §1B) — because the press works either way; a truncated last line dissolves into the mark's zone, a last line that merely FITS but reaches into it keeps every glyph and the mark drops a leading (`detail::more_drops`), with the block's measured height carrying that line. Information and Accessibility are inert and unfocusable. **It holds the PROSE and nothing else: eyebrow, synopsis, tagline, hairline, footer.** That is a CORRECTION, made 2026-08-21 against the updated §1A — it shipped with a title, a genre line and a four-column facts grid on the earlier spec's *"the Information facts ride along here"* reasoning, and both halves were wrong on screen: the facts sat in the panel while the page printed the same four, in the same order, in the Information column **directly behind it**, visible around the panel's own edge; the title and genres repeated the card the press came from while that card was still showing them. `detail::about_info` existed only to feed that grid and is gone with it. Do not re-add a block from this row. Of the other three columns, **Languages (col 2) opens §1B** (`tracks_panel`) — the design refuses it a panel of its own because the audio list *"belongs beside the bitrates in Track information"*, which says where the press belongs as much as what not to build — while Information and Accessibility open NOTHING, being four facts the page prints whole and three fixed definitions that never change. It holds **no list, no focus and no control** — §1E's family rule is that the read-only panels close on BACK, so the `Press [BACK] to return` footer is the whole affordance, and it is the shared `widgets::KeyHint`. Its **height is content-driven** (the mock's hint is a canvas placeholder): a panel that cut the synopsis again would have no reason to exist, so it grows, and `syn_lines` spends whatever is left inside the glass keep-out and never returns 0 — solved from a ONE-LINE panel, since `synopsis: 0.0` means ABSENT to `stack` and would take `G_SYNOPSIS` out of the budget's ladder. It **charges past `gfx::GLASS_REGION_BUDGET`**, as every panel in the family does once grown 88px a side, and that is accepted because the budget prices a MOVING host while the page under an open alert is standing still. Dropping the repeated blocks was also the cheapest thing available for the frame-rate complaint that arrived with them: 300px off the panel is a **third off its snapshot region** (1.15M → 0.77M authored px², pinned by a test, because a block spliced back in re-inflates it silently). Its corner is `theme::ALERT_PANEL_RAD` 32, and its eyebrow lead is `theme::alert`'s. Layout is pure and host-graded (`stack`/`syn_lines`). |
| `tracks_panel.rs` | **Track information** — the detail page's FILE INSPECTOR, as a glass alert (`Alert Views.dc.html` §1B): a fixed 1240×820 sheet at `theme::ALERT_PANEL_RAD`, opened with OK on the About footer's **Languages column** (section 5, col 2 — the block that lists the tracks it inspects) and closed by BACK. It hung off a ⓘ disc in the detail hero until 2026-08-21; `detail::hero_ctls` records why the door moved to the block the panel is about, and why it must not be given a second one. It holds no control, only a paged scroll — `widgets::KeyHint`'s `Press [BACK] to return` and a chevron pair are the whole footer. The gate is `is_available()`: the item must have a FILE of its own, which excludes a show container, and the press is simply refused otherwise — unlike the disc it replaced, the column does not appear and vanish with the answer, so nothing moves under the ring. Three things are not obvious and the module doc argues each at length. **The feather is per-RUN, not per-pixel** — `fs_text_fade.frag` fades on a string's own width and has no y-axis twin, so the design's 88px edge mask is applied as a cascade alpha per text run at that run's cap-band centre (`feather_alpha`), inside a hard `Painter::clip`; that is monotone in y over every run, which is what a gradient IS, and so is NOT the inconsistent fade `table.rs` warns about. **Codecs are named TECHNICALLY** (`AC3`, `MOV_TEXT`, `HEVC`), not through `metadata::friendly_codec` — `track_menu` is a CHOOSER where a friendly name is what a viewer recognises, this is an INSPECTOR where the container's own spelling is the answer. And **language names are the SERVER's**, so they are not English (`Русский`), because the app must not name one track two ways on two screens. Its pure half is large and host-graded — `fmt_size`/`fmt_bitrate`/`fmt_codec_profile`/`fmt_depth_chroma`/`fmt_aspect`/`fmt_layout`, the `file_rows`/`video_rows`/`dovi_rows` groups, `audio_rows`/`subtitle_rows`, and the paging maths (`pages_for`/`scroll_for`/`mask_edges`/`rail_fill`). `/tmp/plxnative-tracks[=<page>]` opens it headlessly AT a page, which is the only way to capture a scroll state; by hand, `detailsec`+`detailcol`+`detailok` now reach it too, since the Languages column is a FIXED index where the hero disc's moved with the control set. Its head ladder is `theme::alert`'s, shared with §1A and §1C. |
| `more_menu.rs` | The player transport's **`…` overflow popover** — `Popover` + `TableView`, the same panel idiom as the track and profile menus, **two sections, two idioms, one rule**. *Options* carries `Row::toggle` — the trailing word `On`/`Off` — for `stats.rs`; *Quality* carries `Row::checked` — the leading mark — for `route::Quality`'s Original-plus-five-fixed-rungs ladder, with Auto prepended only after the adaptive playback readiness gate opens. **A mark says where you are, a word says what is set, and no row says both**, which is also why a rung's rate rides inside its own label instead of in a trailing value beside the mark. Reports an `Action`; `app.rs` performs it. This row said "the `Row::checked` toggle" and "a menu with one row" — the first was already inverted (the stats row has drawn `Row::toggle` since 2026-08-21) and naming one section's idiom for the other's is exactly the confusion the rule above exists to stop. A one-row menu was never a mistake either, for the reason that still holds: the alternative is a hidden key chord, undiscoverable by exactly the people who would report a bug, or a fourth disc for a control most users touch once. |
| `idle.rs` | **Whole-FRAME present gating** — a screen with nothing moving on it stops repainting. Read the module doc before assuming this is the dirty tracking `mod.rs` rejects: that rule is about what happens *inside* a frame (the renderer stays immediate-mode, every frame still clears and rebuilds the whole tree, and the way to not draw something is still to CULL it); this only decides whether to run that frame **at all**, skipping `glViewport`…`SDL_GL_SwapWindow` wholesale. Two independent signals, deliberately not one heuristic. **Motion is EXACT, not guessed**: `gfx::spring`/`spring_zeta` are the only two spring integrators in the app, so they call `note_spring` and the gate *knows* whether any of the ~439 springs a settled Home grid steps per frame is still in flight — there is no settle-window timer that could truncate a slow animation. The rest test is **visibility, not epsilon**: the threshold scales to the spring's own magnitude and is capped below a quarter pixel, and velocity is judged as `vel * dt` — the travel THIS frame. The first version compared a bare velocity against an absolute `1e-3`, which on a pixel-valued spring means "still moving at a thousandth of a pixel per second" and kept the panel awake through 36–52% of every interaction's presents. **Springs are not the only clock, and that is the standing hazard**: anything animating from raw time is invisible to `note_spring` by construction and must report itself — `Xfade::tick` (every route dip) and `Spinner::draw` (every loading read-out) both froze in the product before they did. `Spring::jump` reports too, **change-guarded**, because `home.rs` jumps to the same value every frame while the hub list is empty. **Discrete changes call `invalidate()`** — a poster texture landing, a hub refetch committing, a browse page arriving, any SDL event or remote token, any detail/play landing; that call-site list lives on `invalidate` and is the module's correctness argument, so a new async landing means a new call there. `KEEPALIVE_MS` (2 s) is insurance, not pacing: it bounds staleness if someone forgets, at 0.5 fps. The asymmetry is the design — a false "moved" costs one frame, a false "still" freezes the screen, so both the motion epsilon and the backstop lean toward presenting. It slows the **present**, never the **loop**: input polling, the pumps and every `*_update` keep full rate, so key latency is unchanged and timers still fire (the hero's 8 s auto-flip still flips). Measured on device 2026-07-31: a still Home grid went **39.6% → 1.67% of one core** (ours 15.4→1.05, `surface-manager` 24.2→0.62), a keypress presenting exactly the 33 frames its focus animation moved. **Not applied to the player route** — `system.rs` documents the hardware video plane as *slaved* to our wayland surface. `/tmp/plxnative-noidle` disables it (a DIAG trigger, so an A/B does not also change which screen you boot to). |
| `popover.rs` | **Every modal leaves the way it arrived (2026-09-02).** `Popover::dismiss` runs the appear choreography backwards: the panel stops being OPEN on the press frame (keys return to the page, the glass lifetime ends) and stays VISIBLE while the spring returns to 0, reporting each frame as its own damage; a CACHING panel keeps its host frozen through that fade and releases it at the end, since the page under it has not changed. Draw, update AND scrim gates read `visible()`; input gates keep `is_open()`. Each module exposes the pair: `close()` is the interactive exit (BACK, a pointer dismissal) and runs the fade; `hide()` is the INSTANT `Popover::close`, for page teardown — `detail::close`/`reset`, `person::leave`, the data sweep that ends on sign-in, a source menu that is navigating away — where the subject is being replaced under the panel and a fade would draw the old sheet over the incoming page. Settings' exit was the report, and its frozen ground fades with the sheet instead of being reset on the press. Also owns **`Opener` + `Popover::scrim_lifting`, the modal scrim's CUT-OUT** — the element a panel was opened from, re-drawn on top of the dim so the one thing on screen the panel is about does not recede with the page behind it. It needs no new machinery: the renderer is immediate-mode and z-order IS call order, so "above the scrim" means "drawn after the scrim call". An `Opener` is the HOST's two halves together — the element's drawn rect (what the panel is anchored beside) and a bare `fn()` that re-draws it — because only the screen that drew it knows where a shelf's scroll spring and a cell's focus pop put it. Its users are the item context menu (five hosts), the account chip menu and the Library's Sort/Filter/Source chip menus; the player's `track_menu`/`more_menu` deliberately have none, since there the scrim's job is dimming the video plane and there is no browsing selection behind it. **The lift belongs to the PAGE closure, with the scrim**, for the same reason the scrim does and one more: the direct blur source path re-renders that closure before any popover draws, so a lift drawn with the panel would reach the visible frame and not the snapshot — and the panel's own glass would frost a dimmed copy of the card it is about. `Popover` — the ONE open/appear choreography every modal panel shares (track menu, Info card, Chapters strip, profile menu): an OPEN flag + a critically-damped 0→1 appear spring driving fade + slide, with an optional full-screen scrim, handed out as a ready-made `painter(scrim_a, rise)`. Each panel used to hand-wire its own `static OPEN + APPEAR` pair, so any motion change was a four-file edit — **do not re-fork it.** `painter` builds from `Painter::root()` (the scrim must not compound with the content fade) and therefore multiplies BOTH roots by `nav::page_alpha`: a popover is drawn ON a page, so when a route change dips the page to the app ground a panel left at full strength over it is the one thing on the panel saying the transition is not happening. That is 1.0 on every frame of the in-player panels, since playback has no page transition. It also owns **`panel`, the frosted GROUND** — `Painter::backdrop_blur` where the platform gives one and the near-opaque sheet where it does not, as ONE function, because `theme::PANEL_FROST_*` is only legal on top of a live blur and a screen that picked those tokens itself would draw a translucent hole on exactly the devices nobody here owns. **The glass material has HARD limits that a design has to be drawn inside — `docs/liquid-glass.md` is the envelope.** It can never sit over playing video because the hardware plane is unreadable from GL. There is one shared blur region: neighbouring surfaces converge on one steady-state chain, while far-apart surfaces grow its region through empty space. That geometry is not a timing guarantee; area alone did not predict the measured five-pass cost, so every new layout still needs a TV measurement. The glass tab bar drops to flat while any popover is open because that pair is far apart. |
| `xfade.rs` | `Xfade` — the ONE content cross-fade: fade the outgoing content out, swap the data at the floor, fade the incoming one in, parked at 0 until the new content exists. Sibling of `popover.rs` (which owns the modal *appear*) for the case where a screen's CONTENT is replaced under it, and of `nav.rs`, which is the same choreography one altitude up (the whole PAGE) and is built on this type rather than beside it — `cancel()` exists for that caller, to withdraw a fade-out whose request was taken back before it committed. It **schedules** the swap (`tick` returns `true` on exactly one frame and the screen applies its own typed pending action there) rather than reacting to one, because the stores it rides on wipe themselves synchronously on the key press — `browse::requery` empties `st.items` in the same call as the input, so a fader that only *watched* the data could never fade anything OUT, only in. Milliseconds (`OUT_MS` 70 / `IN_MS` 140), **not** a `Spring`: it gates a state commit, which needs a hard deadline, and a spring's "arrived" frame depends on the entry velocity — non-zero and pointing the wrong way whenever a second reload interrupts a fade-in (the module doc has the arithmetic). The ramp is linear so the deadline is exact; the alpha handed out is smoothstepped so the dissolve still eases. It reaches the content as ONE `Painter::alpha` push around the faded band — never around the chrome that persists across the swap, and never around a loading spinner, which must stay legible while the band is dark. It owns motion only: no data, no pending action (the screen keeps its own typed one, because a generic queue would be a `Box<dyn FnOnce>` behind a `static mut`). Library's grid is the first user; the person page's shelves, detail's episode row and home's hub arrival want the `arrive()` half. |
| `nav.rs` | `nav` — the **ROUTE-level page transition**: the whole SCREEN being replaced, the third altitude beside `xfade.rs` (a screen's content replaced under it) and `popover.rs` (a modal over it). Built ON an `Xfade` rather than beside one — same scheduled Out/Hold/In, same 70/140 ms, because a route change and a content reload are one statement at two altitudes. It is a **DIP, not a cross-dissolve**: every screen's draw opens with `gfx::frame_clear` so two pages can never be on the panel at once, and `theme::CLEAR_RGB` IS `theme::SURFACE_APP`, so the outgoing page fades to the app ground, the route flips at the floor, and the incoming one fades up off it with no seam. **Every screen a route transition can reach pushes `page_alpha()` as ONE cascade alpha at the root of its tree** — `home_draw`, `library::draw`, `detail::draw`, `person::draw`, `search::draw`, and those five are the whole list (it said four until Search landed, which is the shape of staleness to expect here — the list grows by one every time a screen becomes routable); a page transition is never a second full-screen pass. The player has none because playback is a hard cut, and `login.rs`/`profiles.rs` have none because nothing ever *transitions* to or from them: the boot gate and the login landing both assign the route outright, so there is no fade for an alpha to ride. Give a screen one only when something routes to it through `nav`. Two alphas, and the split is the whole design: `page_alpha()` for content the swap replaces, `chrome_alpha()` for the **shared top tab bar**, which is the SAME control on Home, the Library and Search (its last pill is Search's own) and must hold still while the pages swap under it — fading it too would read as the screen blinking. Detail and Person carry no `chrome_alpha()` line at all, because the bar is not on them: a Home→Detail transition fades the bar out WITH the page, which is `Nav::wears_tab_bar`'s job in `app.rs` (a `match`, so a new destination is a compile error rather than a silent wrong answer). It also owns the pending tab SELECTION (`view_tab`), read inside `widgets::tab_row_update` so the capsule leaves for the pressed pill on the PRESS frame instead of learning about it on the first frame the destination draws (the bug: the highlight visibly chased the swap). And it carries the outgoing page's **teardown** (`begin`'s `leave`, spent by `spend_leave`): `detail::close`/`person::leave` drop the loaded item, so running them on the press frame empties the page *during its own fade-out* — they ride the request as a bare `fn()` and run at the floor with the incoming entry. `spend_leave` takes the caller's supersede answer as an ARGUMENT rather than handing the payload out, so a teardown can neither leak into the next transition nor run for a request that was dropped. Motion + the chrome rule + that one payload — the pending `Route` is `app.rs`'s typed `NavReq`, whose `from` field is the supersede test, for the same reason `xfade` keeps no pending action. |
| `trail.rs` | `Node`/`Trail` — the **BACK trail**: which pages are behind the one on screen. Sibling of `nav.rs` and the division is worth stating once — `nav` owns the MOTION of a route change, this owns its HISTORY; neither knows the other exists. It replaces `app.rs`'s `opened_from_library`/`opened_from_person` pair, which were a precedence LADDER (person outranks library outranks Home) with exactly one slot per screen KIND — and the detail page stands on ITSELF all the time (the filmstrip's metadata row and the Related shelf both open a second detail page over the first), so BACK from an episode page fell past its show onto Home. A ladder needs a new rung per stacking screen; a stack needs none. Nodes carry **identity, never an index** (a hub refetch rebuilds the catalog wholesale — the bug `detail::reselect` exists for) and each payload is exactly its screen's own entry arguments, so re-entry is a call rather than a reconstruction: a `Person` node holds `person::reopen`'s fields, a `Detail` node an rk plus the `detail::Spot` the page was standing at. That identity is also **server-scoped**: a ratingKey and a `personId` are both server-local integers dense from 1, so with a shared server registered a bare key names a page on neither machine — browse the share, open an item, go deeper, press BACK, and you land on *our* item with that number. Every node therefore carries a `plex::ServerId` and identity is asked through `Node::same_page` (which ignores the `Spot` — the same page scrolled elsewhere is the same page) or, for the person guard `enter_node` needs, `trail::same_person`. A person prefers the plex.tv `tagKey`, which is GLOBAL, and falls back to the server-scoped local id only when either side lacks one. This per-node `sid` is the deliberate answer to the question `reset`'s profile-switch doc raises about a SERVER switch: a source change does not revoke access to what is behind you, so scoping each node keeps the history walkable instead of spending it. **It decides nothing about screens** — putting one back is `app.rs`'s `enter_node`, whose rule is `person::leave`'s old one generalized (re-open only if the page behind is not still the one loaded), which is what makes the common pop a route flip; that same guard is why `enter_node` also serves every FORWARD navigation onto a stacking page, so one function mounts in both directions. `under()` is the peek the BACK PRESS needs, because the pop itself is deferred: a BACK starts a `nav` transition and the trail only moves at the fade FLOOR (a push or a pop inside the window would be history the user loses when the transition is withdrawn), while the press frame still has to know whether the destination wears the tab bar. A reset to the ROOT is the deliberate exception and stays on the press frame — Home is `stack[0]`, so truncating is idempotent and a withdrawal costs nothing. That split is also what makes the decision host-testable at all, since the BACK arm lives inside the SDL event loop where no test can reach it. The `CAP` drops the OLDEST non-root node rather than refusing a push (a refused push is a loop with no way out) and the ROOT is never dropped, which is what keeps **BACK at Home reaching the DOOR**: `Trail::back` returns `None` there and the arm's Home branch never consults the trail at all. (Since 2026-09-03 that door is the TELEVISION'S OWN HOME — `app.rs::back_at_root` → `webos::go_home`, with the app still running; it was a Cancel/Exit alert from 2026-08-21 and the exit itself before that. The trail's obligation is unchanged whichever it is, because at the root there is nothing to pop either way.) An `app.rs` run-loop LOCAL, like the booleans it replaces. |
| `press.rs` | The shared tvOS-style **click**: OK-down dips the focused control, OK-up releases with an overshoot bounce and commits the activation a beat later (so the bounce is actually on screen). Genuinely event-driven, so a held OK is a measurable long-press (`is_long`), not a tap. ONE control is pressed at a time (focus can't move mid-press — navigation `cancel`s it), which is why it is a global: `begin`/`release`/`cancel`/`tick`/`take_commit` + the `scale()` the renderer multiplies the focused tile by. Resolves stuck presses three ways because the Magic Remote drops key-ups. |
| `consts.rs` | Layout + input + animation constants — card/gap/margin geometry, `ROW_PITCH`, and the webOS **remote wcodes** with the `is_ok`/`is_back` predicates (see the raw-`SDL_KeyboardEvent` gotcha in `docs/agent-reference.md`). One source so hand-tuned pixel offsets can't drift between widgets. |
| `fmt.rs` | The shared display formatters — `dur_short` ("2h 15m") / `dur_long` / `time_left` / `clock` / `episode_ordinal` ("S2, E3", an episode's bare address — the detail hero's meta line, where the title band above it already IS the name) and `episode_kicker` ("S2, E3 · Laura") built on it / `pretty_date` ("1 Feb 1921", from a Plex `YYYY-MM-DD`, with a year fallback — detail's air dates and the person page's Born/Died line) / `rating_score` (a review score back in its own provider's units: RT + TMDB are percentages, IMDb is out of ten, while PMS sends all three as 0–10). ONE home for these, replacing the "2 hr 15 min" vs "2h 15m" vs "0 hr 45 min" drift across screens. Format a duration here, never inline. |
| `overdraw.rs` | **who submits the fragments** — the dev-only draw-class ledger and the draw-class MASK, and the reason `FRAG_QUADS_RAST` alone was reading the frame wrong for a month. `/tmp/plxnative-overdraw` sums, per PRIMITIVE FAMILY (`ambient`/`grad`/`rect`/`shadow`/`card`/`image`/`text`/`glass`/`blur`), the screen-VISIBLE area of every quad the app actually submits — clipped to the panel and to `Painter::clip`'s live box, skipped during a blur SOURCE pass so a second page draw cannot double every class — and logs one `OVERDRAW` line a second. It is CPU-side, so it is not `glFinish`-serialised, it cannot be billed for the wayland compositor's work the way the GPU counters are, and it runs in the simulator (it works in AUTHORED pixels, so a half-size drawable gives the same answer). `/tmp/plxnative-drawmask=<classes>` refuses every draw of the named classes, which is how a class is PRICED: a whole-frame `frame.ui` HWCNT A/B against the unmasked control, un-serialised. `all` draws nothing and measures the **compositor floor** — 34.4% of a hero frame on the dev set, work no change in this repo can touch. Every leg except the control is a broken picture by construction; this is a measurement knob, not a feature. `blur` is accounting-only (its passes write reduced-resolution FBOs, and half a masked chain would leave the panel sampling a stale texture — mask `glass` to remove the whole surface). Compiled out entirely without `devtriggers`: `gate` is `false` and `note_px` empty at compile time. `docs/backdrop-blur-profiling.md` §5 is what it found — chiefly that 37% of a hero frame's fragments cost 0.047 core-cycles each, because they are varying interpolation on a pipe that is half idle while the arithmetic pipe sits at 89.5%. |
| `profile.rs` | draw-phase diagnostics with three separate modes — arm exactly one. `/tmp/plxnative-cpuprof` is the render thread's own inclusive wall time per phase, every phase at once, no `glFinish`, logged as `PROFILE CPU` lines in the event log (a `~src` suffix marks a phase met inside the blur source pass); it takes precedence in `phase()` over the two GPU modes and is the only one that can read a frame the frame-drop detector reports as `draw=24ms swap=0.3ms` — on this driver the GPU wait lands in the frame's first framebuffer-0 command, `hm.clear`. `/tmp/plxnative-profile` uses asynchronous `GL_EXT_disjoint_timer_query` timing (no `glFinish`) and writes `/tmp/plxnative-gputime.jsonl`. `/tmp/plxnative-hwcnt` uses the direct r12p0 vinstr reader and serialized boundaries for counter attribution only, writing `/tmp/plxnative-hwcnt.jsonl`. Put one exact phase name in either trigger; never combine them. Analyze with `tools/analyze-gputime.py` or `tools/analyze-hwcnt.py`, then measure production frame pacing with both triggers absent. |
| `anim.rs` | spring diagnostic (not chrome). `anim::probe(name, pos, vel, target, dt)` right after a `Spring::step` logs that spring's settle metrics (frames, ms, overshoot %) and draws a live approach-curve overlay; on via `/tmp/plxnative-anim` or the ANIM toggle, zero cost off. Reach for it before hand-tuning a stiffness. |
| `skip_pill.rs` | The **Skip Intro / Skip Credits** button, driven by the playing leaf's server markers (`metadata::playing_markers`). One of two **stand-ins** for the transport's Subtitles/Audio discs — same row, same right edge (`player_hud::CTRL_*`) — so it is focus row 1 with a single item, and `player_hud::icon_hit` refuses that band while it is up. A `final` credits marker resolves to `SkipAction::Finish` (the item is over) instead of a seek. |
| `up_next.rs` | The **Up Next tile** (credits marker + a queued successor; `Player Screen.dc.html`'s third `ControlSlot` occupant), stacked above the control row's own slot and sharing its right edge: the next episode's landscape STILL, a bold `CAPTION` caption (`"Up Next · S2, E4 · Laura"`, built from the shared `fmt::episode_kicker`), and **two buttons that are deliberately not equals** — *Watch Credits* at its own label's width, *Next Episode* at the `ctrl_slot` floor, because that floor exists to hold the row's right edge steady and two equal capsules would say the choices are equivalent. The countdown is the primary's own `Button::progress` fill sweep, so control and timer are ONE object (it animates from a clock, so it reports to `ui::idle` — the player route bypasses that gate today, and the call is what keeps that reversible). Geometry is the pure `layout_of(next_w, credits_w)`, because the impure half measures with `TTF_SizeUTF8` and the host suite cannot link SDL2_ttf. **The row's cursor is `hud_nav.btn`, not module state** — `ControlSlot::items()` answers 2 and `primary_btn()` answers 1 (the one row in the app whose primary is not index 0), so LEFT/RIGHT need no special case. The whole cancel rule is the pure `countdown_may_run(row_focused, btn)`, read as a STEADY STATE at `app.rs`'s one frame block: the clock survives only while the ring rests on *Next Episode*, so walking away, reaching for Watch Credits and pressing either all disarm it — latched, so nothing re-arms this segment. Data is `route::up_next()` (free off the `continuous=1` PlayQueue every playback creates); slot precedence is unchanged (it outranks `skip_pill`). HUD furniture, not an overlay: playback runs underneath and the frame block holds the HUD up while the countdown is armed, clearing `hud_dismissed` with it — a timer nobody can see is a cut to the next episode out of nowhere. *(A full-screen post-play CARD — poster, `DISPLAY` title, dot-drawn countdown ring — exists at commit `84ff9328` for the cross-show case; it is not in the tree because its trigger does not: `continuous=1` is per-show.)* |
| `item_menu.rs` | The **press-and-hold card context menu** (Apple TV's card popover, NOT Plex's full-screen sheet): a `Popover`+`TableView` panel anchored BESIDE the focused card — the card and the screen stay in place behind it — with `[icon] [label]` rows, a `Row::separator()` between the navigation and state groups, and the row set adapted per item kind. **Every card surface opens it**: `open` for a card row (a home shelf, the Library grid, a Search result shelf, a person's filmography, the detail page's Related shelf) and `open_episode` for the detail page's episode filmstrip (the same panel with NO navigation group, because both nav rows lead to the page you are already on). One `state_rows` builds the watch-state rows + Play from Start for all of them, so they can't say one thing on Home and another on the episode page. **That group is one row or TWO**, the detail hero's rule off the same three-state `widgets::PosterMark` vocabulary: each row states the OUTCOME its press produces, so a finished item offers only *Mark as Unwatched* and a never-started one only *Mark as Watched*, while a PART-WATCHED item is at neither end of the range and gets both (✓ then −, the order the hero's discs sit in). The state comes from `widgets::row_watch_state` for a card row and `detail::ep_watch_state` for a filmstrip still — never from a bool, which is what the row set was keyed on until 2026-08-21 and is why the way back from a half-watched item was unreachable. `Action` carries the TARGET state as two variants (`MarkWatched`/`MarkUnwatched` + `watch_write`), not what the item is NOW: with a pair of rows an inverted-at-the-press bool would be the same value twice, and one row would silently perform its neighbour's write. The surfaces deliberately left out are the ones whose tiles are a PERSON or a TAG and so carry no rating key and no watch state at all — detail's cast headshots, Search's Cast & Crew / Collections (`Item::Tag` has no rating key) — where every row this menu builds would be absent, so a hold keeps doing nothing. **Detail's RELATED shelf used to be on that list and is not any more** (2026-08-21): the premise was half true — the old `metadata::Related` struct was `{rk,title,thumb}`, but `/related` returns the same wire DTO as every other listing, so `viewCount`/`viewOffset`/`duration`/`type` were on the wire and simply dropped. Making `Related` a real `pms::PmsMovie` (via the shared `pms::parse_item`) gave the shelf its watched tick and resume bar — it was the ONE poster surface passing `Art::Thumb`, which carries no row and so can wear no mark — and made the hold an ordinary `open` on a card row. The lesson generalises: before excluding a surface for "the tile has no state", check whether the SERVER sent it and the parse threw it away. **The menu CARRIES the `PmsMovie` it was opened on** (`item()`), and that is not an optimization: Play from Start used to re-resolve the key through `pms::index_of_rk`, which walks the HOME hub catalog alone, so on every other surface the arm found nothing and the press did nothing at all — silently, since a miss there is a legitimate "off-catalog" answer. It only REPORTS an `Action`; `app.rs`'s `apply_item_action` does the routing, the scrobble and the refresh — and still takes the `MenuHost`, because the DETAIL page is the one host where an action means something different (its Play from Start goes through `detail::play_episode_rk_from_start`, and its scrobble re-reads the page). Opened off `press::is_long` (`Route::ItemMenu { over }`); a SHORT press still plays a Continue Watching tile / an episode still immediately, which is intentional. Its scrim is the PAGE's (`draw_scrim`, called from `app.rs`'s page closure) and it LIFTS the tile it was opened on back out of the dim — see `popover.rs`'s `Opener`. |
| `person.rs` | The **person / actor page**, built to `Person Screen v2.dc.html` (Apple-TV shape, not Plex's field list): an **asymmetric band** — circular portrait left, CENTRED on a text column beside it (name → roles → Born/Died → an optional 3-line bio that DISSOLVES into a right-pinned `MORE`, the About card's treatment: `TextView::fade_last` reserves the zone and fades the last line through `shaders/fs_text_fade.frag`, the screen pins the word to the column edge on that line's cap band. It is one choice, not two flags — `fade_last` and `trailing` clear each other, because the fade branch in `TextView::draw` `continue`s past the inline run and a view carrying both loses its mark on exactly the lines that fade) — over plain `Movies`/`Shows` shelves of ordinary poster cards, each heading carrying its item count, all flowed through a `ScrollColumn` whose children are the band and the PRESENT shelves. Opened by OK on any **credits** headshot — cast or crew alike, since a director is a person with a `tagKey` too (`detail.rs`'s section-4 arm, via `metadata::credit(i)`); its data is `crate::person`. Four things to know before editing it. **(1) The band CONDENSES** (320→160 portrait, name HERO→TITLE, everything else fades) the moment focus leaves the header, on one `cond` spring that also drives `Column::height(0)` — so the shelf tops, the scroll target and the pointer hit-test all follow the resize for free. The name **crossfades between two rung sizes** rather than animating its point size: every intermediate size would rasterize a fresh glyph run. **(2) The header is a FOCUS ROW** (flow child 0, as detail's hero is section 0) even though it holds no control — it is the page's top scroll position, what UP from the first shelf lands on, and the state the condense keys off. Anything that reads focus for the shelves must test `on_header` too, or a tile draws captioned while the portrait is what the user is on. **(3) Every header line below the name is optional and comes from plex.tv** (`plex/discover.rs` — the LOCAL server has no biography). A line is drawn only when it has content and the band height is measured from what is present, so the no-bio page reads as finished rather than broken — do not "fix" it with placeholder lines or a header spinner. **(4) The mock's `padding-top: 14px` is deliberately NOT ported** — it compensates for a CSS line box's leading, which our cap-band text placement does not have. **(5) The `MORE` mark now has a panel behind it** — `ui/person_bio.rs`, the bio ALERT (`Alert Views.dc.html` §1C): a centred 1120×700 glass sheet at `theme::ALERT_PANEL_RAD`, holding the whole biography as a PAGED viewport (200px a step) with `TextView::edge_fade` dissolving the crossing lines at each end (`widgets::edge_feather`'s opaque-gradient replacement, 2026-09-02) and a `widgets::scroll_rail` beside it. `MORE` itself is unchanged and is still a MARK, not a control — but **the block it ends now wears the page's selectable-block lift** (`widgets::text_block_highlight`, literally the call detail's About columns make), drawn under the prose on `bio_mark_visible`: focus is here, the bio is truncated, AND a D-pad press has actually landed on the header (`Scene::header_marked`) — a fresh mount opens on the header as a scroll position, not a selection, so it draws no mark until UP/LEFT/RIGHT/OK arrives; `header_ok` deliberately keeps its two-term form, because OK on an unmarked header is itself the arrival. That narrows a rejection `header_ok` records rather than reversing it — the bio is still not a second focus stop, the band is still one — and it fixes the band being the only focusable thing in the app that drew nothing at all for holding focus. Gate it on `on_header`, never on the condense spring, which is still ~0 for several frames after focus has left. The entry point is **OK on the header**, which was inert before (the row carries no card, so `app.rs` arms no tvOS press on it and `person::header_ok` acts on the key DOWN instead). It is gated on `bio_is_truncated()`, the same predicate the mark is drawn from, so the panel exists exactly when there is more to read. Wiring follows `alt_sources` — the SCREEN intercepts, never `app.rs`: `move_focus` traps the nav keys, `focus_is_card` declines behind the sheet, `on_ok` swallows, `tile_at` returns None so the pointer cannot reach a card under it, and `person::back()` closes it. That last one needed `app.rs`'s BACK arm SPLIT: it called `detail::back()` for both Detail and Person, which was only correct while Person had no panel of its own. Two traps are recorded on the code and worth knowing before editing either: its glass is DYNAMIC because a cached snapshot is taken on the frame the panel opens, with its own scrim still ramping through zero — so it would frost an UNDIMMED page for the whole session, which on this page is the design's contrast note made real (the person's 72px name sits directly behind the panel's fine print); and nothing reachable from a KEY handler here may measure text, because `person::move_focus` is called by the host suite and reaching `TTF_SizeUTF8` from it does not skip a test, it stops the whole suite LINKING. |
| `person_bio.rs` | **The person page's biography, in full** — the alert behind that page's `MORE` mark (`Alert Views.dc.html` §1C): a centred 1120×700 glass sheet at `theme::ALERT_PANEL_RAD`, holding the whole bio as a PAGED viewport (200px a step) with `TextView::edge_fade` dissolving the crossing lines at each end (`widgets::edge_feather`'s opaque-gradient replacement, 2026-09-02) and a `widgets::scroll_rail` beside it. Its footer is the design's split one — the film count on the LEFT, `widgets::KeyHint`'s `Press [BACK] to return` on the RIGHT. `MORE` itself stays a MARK, not a control: the entry point is **OK on the header**, which was inert before (the header row carries no card, so `app.rs` arms no tvOS press on it and `person::header_ok` acts on the key DOWN instead), gated on `bio_is_truncated()` — the same predicate the mark is drawn from, so the panel exists exactly when there is more to read. Wiring follows `alt_sources`: the SCREEN intercepts, never `app.rs`. Its head — eyebrow, name, identity line — is the family's `theme::alert` ladder, and every y in it is a CAP TOP: `tracked_run` reaches `Painter::text` directly and so converts, exactly as the identity line does. Both halves were wrong until 2026-08-21 (rungs instead of the ladder, and the kicker drawn at a bare y), which together put `PERSON` 26px above the name against its siblings' 38 — "too close to the name", and invisible from inside this one panel, since the bug is only the difference between two sheets nobody sees side by side. Two traps are recorded on the code. Its glass is **DYNAMIC**, because a CACHED snapshot is taken on the frame the panel opens with its own scrim still ramping through zero — it would frost an UNDIMMED page for the whole session, which on this page is §1C's contrast note made real (a 72px name sits directly behind the panel's fine print). And **nothing reachable from a key handler here may measure text**: `person::move_focus` is called by the host suite, and reaching `TTF_SizeUTF8` from it does not skip a test, it stops the whole suite LINKING. `/tmp/plxnative-personbio[=<text>]` seeds a biography, and has to exist — the bio comes from plex.tv, which answers a *server* token 401, so no automated boot reaches a page with any prose on it at all. |
| `search/mod.rs` | The **Search screen**, split across five files and this one is the STATE MACHINE — zones, focus, scroll and the draw ORDER — with each region drawing itself off the same `View` snapshot, so a region can never read a different focus than the state machine believes in. Text entry is the **television's own keyboard** (`crate::textinput`, raised by plain `SDL_StartTextInput`), so this screen owns no keyboard layout at all; what it owns is the ▼ handoff, which is the rule the whole machine exists for — `below()` is the ONE expression answering "what is under the field", `draw` dispatches its regions from that same call, and with nothing there the press is a visible no-op rather than focus dropped into an invisible list. The caret blink (`BLINK_MS` 530, `step_blink`) is the intentional device exception to the design component's solid bar: it reports to `ui::idle` on phase flips alone, about two presents a second with the keyboard up and none with it down. The layout rule the numbers come from: with the keyboard raised **nothing the app owns hides behind it** (the panel's measured top edge is 756; `results`' own test grades the clearance for every tile size a first shelf can have). One scroll spring, ridden by the SHELVES only, with the two freezes (`scroll_frozen`) here and the flow's shape in `results`. Design + the on-screen-keyboard traps: `docs/search.md`. |
| `search/field.rs` | The **query line, its caret, its one-character instruction and the scope line under it**. No magnifier — the strip's pill above it is the mark. **There is no capsule, no rim, no rule, and — as of 2026-09-04 — no fill either.** The query IS the type, at `size::HERO` on the flat ground, full content width, and **FOCUS is carried by INK ALONE**, permanently — the design system's own `SearchField` contract. A flat `ACCENT` plate lived here for one day (issue 22, 2026-09-03, focused-but-not-editing only) and the owner rejected it on sight: "you made the search bar background white, while I wanted text to be white". It is deleted, along with the discrete-flip machinery (`fill_t`/`field_ink`/`FILL_INK_FLIP_T`) that only existed to keep an ink ramp legible against a moving plate. In its place: **two bright ink endpoints, picked by `editing` alone** (`draw`'s `ink_target`, never a second spring) — focused-not-editing crosses to `theme::FIELD_WAITING_INK`, a genuinely pure white, because that state has no caret or keyboard to help it read as focused; editing crosses to `theme::FIELD_EDITING_INK` (`TEXT_PRIMARY`, one stop dimmer, and the caret's own colour), because the blinking bar and the open keyboard already carry "you are editing this". Both ride the SAME wide `TEXT_SECONDARY`→target step — one stop (`TEXT_HEADING`) was too little from the couch (owner feedback, 2026-09-02) — and the unconditional placeholder ("Search your library") rides the same spring one step behind (`TEXT_TERTIARY` → `TEXT_SECONDARY`), unaffected by `editing`, so an empty focused field also says so. It wore `ACCENT`/`CONTROL_IDLE_FILL` as a control face until 2026-08-30, and `FIELD` survives only as the LINE BOX the run is clipped to (no plate to bound any more either). The run scrolls so the **CARET** stays on screen (`run_layout` — the TV's panel delegates `◀`/`▶` to the app, so the thing that owns the text moves the cursor), and there is no pointer cursor at all: no selection, no click-to-position. The caret is 5px wide and one CAP BAND tall, keeps the design's 8px air after the run, sits on the run's baseline, is drawn only while `editing`, and blinks in 530ms phases — intentional device feedback, with `caret_on` hiding the OFF phase and `step_blink` invalidating only the flips. One character short of `MIN_QUERY`, `one more character` follows it at BODY/tertiary with 28px of air; it stays in the FIELD whose state it explains and is consumed by the next keystroke. The **scope line** remains under the query at `SCOPE_Y`, on the field's own margin, in CAPTION/tertiary in EVERY state (one source included — this is the screen where you cannot see what you are looking at). `run_layout`/`ink_target`/`scope_text` are pure and host-tested, plus a narrow source-grep tripwire pinned against issue 22's exact deleted call shape (a regression guard, not a proof no fill can ever reappear under a different name); the registry projection they read is `with_scope`, built once per change and shared with `search/empty.rs` so the two lines cannot contradict each other. |
| `search/results.rs` | The **typed result shelves** — `card_row::strip` per type, in `search::KINDS`' fixed order, an empty type drawing nothing. Ranking is honoured INSIDE a shelf and never across them, because the server reorders its hubs per query and reordering rows per keystroke moves the row under a focus that is still typing. Tile geometry is per type (posters 250×375, episode stills 420×236, headshots a 250 circle), and every poster treatment the rest of the app has — focus pop, lifted shadow, amber resume bar, watched tick — travels here for free. A search shelf merges across sources, so its heading **cannot claim an owner**: the annotation follows FOCUS on the focused tile's caption (Continue Watching's rule), as an `Owner` fade whose swap is at the floor, since a point of ink cannot cross-fade between two different words. `stack_top` is the ONE vertical geometry — the draw reads it, the scroll rule (`reveal_shelf`) reads it, and the pointer reads rects RECORDED at draw — and the scroll policy is the shared minimal reveal, not the mock's pin, so Search is not the one screen in the app that jumps. `draw` cuts at the field's bottom edge with a real scissor. |
| `search/recents.rs` | The **empty-query state**: the user's own recent terms and the control that clears them. Rows, not a shelf (nothing here has artwork), using `table.rs`' OWN geometry on the app's ground — `ROW_H`/`SIDE`/`CONTENT_X`/`PILL_INSET` are IMPORTED, not restated, so a row-height change there moves this block with it. Written as markup rather than mounted as a `TableView` for one reason worth keeping: **these rows are the user's own words and have to stay editable in place.** Above them a section HEADER, not a heading — it names the SOURCE of the rows, so it is a full step below their labels (CAPTION, caps, tertiary, on `table::HDR_H`) — and it is placed by **cap band**, not by that widget's raw `sy + 8.0`, which is the magic y rule 3 bans. Clearing is a `widgets::Button`, so a verb never sits in the column of things you searched for. Four terms (`MAX_RECENTS`); the fifth is DROPPED, not scrolled, because a list you cannot see the end of asks to be paged and there is no paging in this product. `BLOCK_BOTTOM` is what a host test pins against the raised keyboard's edge — so the COUNT is free to follow whatever the panel actually measures. The terms persist in the session file (`recent_searches`, behind `de_soft_vec`: a corrupt entry costs that entry). |
| `search/empty.rs` | The **two states with no shelves** — nothing searched yet, and nothing found. Both are **statements, not failures**: no alert mark, no danger tint, no Retry, because nothing found is an answer the server gave and dressing it as an error tells the user something untrue about their library. Each is two parts CENTRED in the band the app owns (`band`, which moves when the keyboard comes up): the region's own name in the caps header face (`table::HDR_H`'s type role) over one statement at `size::TITLE`. Same two parts, same order, same place for both, so they read as one screen answering in one voice. The header is load-bearing — an empty recents list is still THE RECENTS LIST, and it keeps the name of the thing it is empty of. Deliberately **not** a `StatusOverlay`: that component fixes its own rungs (a `BODY` verdict over a `CAPTION` reason, no caps header at all), and bending it to a caps kicker over a bold TITLE would put every existing caller one builder call from drifting off it. |
| `jail_repair.rs` | The player failure screen’s sandbox-repair confirmation. Its `Controller` is owned by the app loop, uses the shared neutral `DecisionAlert` with Cancel selected initially, and delegates only confirmed actions to `webos::jail_repair`. Key and pointer share the failure-primary routing rule. Running and terminal repairs expose only BACK; ordinary playback failures retain quality/retry. Development display fixtures never enable the privileged operation. |
| `home.rs` / `detail.rs` / `player_hud.rs` / `info_panel.rs` / `track_menu.rs` / `chapters_panel.rs` / `library.rs` / `login.rs` / `profiles.rs` / `account_menu.rs` | **screens** — compose the above; hold their own springs + input. Should contain almost no color literals. `track_menu.rs` owns **`track_name`, the one rule for what a picker row's sub-line says**, and it is a DATA rule rather than a presentation one: `Stream.title` (what PMS parsed) first, then the CONTAINER's own name (what our demuxer read, `player::TrackNames`, published by `ff::track_names`), then — audio only — the codec+layout descriptor. The second source exists because **PMS sends no `Stream.title` at all for an MP4**: Matroska spells the tag `title` and MP4 spells it per-track `name`, and Plex's parser maps only the first, so a nine-track MP4 arrives as six rows reading `Русский` and nothing else (verified live 2026-08-22 against one server holding both containers; `/library/streams/{id}` is 501 and `checkFiles=1` adds nothing). The join is POSITION — `metadata::sub_render_ordinal` / `audio_ordinal`, the container ordinals this app already maps a row to — so `TrackNames` is dense by contract: an untagged track contributes an EMPTY string rather than being skipped, or every name after the first untagged one shifts onto its neighbour, and a wrong name is indistinguishable from a right one. Two limits worth knowing before you reach for it. It is **direct play only** (a transcode is a remux the server built), and it fixes only the files that HAVE names — an MKV whose tracks carry none is undifferentiable by any means, which is the case an ordinal fallback would cover and nothing does yet. **The simulator cannot verify any of it**: there is no demuxer off-device (the bundled FFmpeg is ARM), so the picker there draws what PMS sent. `/tmp/plxnative-tracknames[=<audio>;<subs>]` seeds the real store for a screenshot — `personbio`'s trick, and it verifies the ROW, never the FFI read. `player_hud.rs` additionally owns `busy_surface` — the single rule for which surface carries the "the pipeline is working" signal (the centred `StatusOverlay` read-out vs the transport's inline spinner beside the elapsed clock), resolved once per frame by `busy()` in `app.rs` and handed to both draws so the two can never light together; the read-out is drawn by `draw_readout` AFTER the transport and BEFORE the overlay panels, and deliberately outlives the HUD's auto-hide because its visibility is the playback state, not a timer. A **FAILED** read-out additionally takes the whole frame — `draw_hud` opens with `readout_owns_frame` and returns, so no scrim, scrubber, clocks or bottom tabs draw at all (`Player Screen.dc.html`: `hudDisplay:none` + `tabsDisplay:none` on the failed variant, because with no picture and no position a live-looking scrubber at 0:00 is the bug the read-out exists to end). Only a failure: a WORKING read-out is mid-flight and must leave the transport up, or the HUD blanks through every cold start — the host test grades both halves. It also paints its OWN opaque black ground (the mock's `background:#000`), because the video plane is only *usually* black by then and the bottom scrim that used to back its lower half is no longer drawn. **`transport_hidden()` is the INPUT half of the same rule** and `app.rs` must keep both ends: key and pointer expose only the failure’s recovery action and BACK, because a control that is not drawn must not be activatable (`ControlSlot::UpNext`'s rule). Ordinary failures may open the quality picker; a sandbox failure offers explicit repair confirmation instead. The other overlay panels are gated on it too — otherwise a blind DOWN+OK during the entry linger opened the Info card over the read-out. `home.rs` additionally owns the rotating hero's **artwork policy**: its `Backdrop` is the page GROUND
(one `AmbientWash` keyed to the current pool page, hero-scoped — the lean falls to zero as the snap
dives, so the shelves keep sitting on the flat clear their drop shadows are tuned against) plus the
two sliding ART layers, each with its own reveal spring so a backdrop that decodes late DISSOLVES in
instead of cutting; and `prefetch_hero_neighbours` (`HERO_PREFETCH`/`prefetch_order`/`prefetch_armed`),
which warms the adjacent pages' backdrop + clearLogo through `warm_tex`/`posters::logo_warm` — at most
one key per frame, only from an idle store, because the poster workers have no priority and enough
in-flight warms would stop a VISIBLE tile from being requested at all. `detail.rs` owns the **page
GROUND** for a show/movie: one `AmbientWash` (`DetailView::amb`) keyed by `amb_blur`/`amb_target` to
the item's own `UltraBlurColors` at `AmbientWash::GROUND_W` on all four corners, jumped on mount from
the catalog row and dissolved toward the LOADED item's envelope — which is the only source three of
the four ways in have (Library grid / Related tile / person page are not in the home hub catalog).
It is drawn under the backdrop art and stands in for the clear, so the below-hero rows sit on the
item's colours; there is no grey rect fading in over it any more, and nothing may re-add one.
It also owns the **hero backdrop ladder** (`hero_art_path`, pure and host-tested): a SHOW draws the
still of the episode its hero is about, an EPISODE — this page serves leaves too, reached by "Go to
Episode" on a home card's context menu or by the filmstrip's own text row — draws its OWN `thumb`
ahead of the `art` it inherits from its show, and only then does the ladder fall to the catalog row's
art and the loaded item's. There is deliberately no catalog-row twin of the episode rung: a row's
`thumb` for an episode is the SHOW's portrait poster (`pms::parse_item` prefers `grandparentThumb`),
not the still. Every full-bleed blit goes through `Rect::cover`, never a stretch to `Rect::FULL` —
an episode still is a video frame whose aspect and resolution we do not control.
Its **hero has three text blocks and each answers ONE question**, which is the rule to keep when
editing them (`Details Screen.dc.html`, 2026-08-12): the IDENTITY line carries the type/genre/rating
words AND the media chips (`draw_media_badges` — what IS this: 4K, CC/SDH/AD), the FACTS line carries
date · extent · how it plays (what you get if you press Play), and the right-hand **people column**
(`draw_people`) carries the crew credit over the cast. Two things there are load-bearing. The people
column is **bottom-anchored** on the button row (`people_bottom`) and grows UPWARD, so a wrapped cast
list never walks down into the section flow — and `people_top` is what `widgets`' hero-scrim
legibility table grades, at the tallest block the column can produce. That anchor is also why its ink
is `detail::PEOPLE_INK` (`TEXT_SECONDARY`) and not the mock's `--text-tertiary`: the move took the
column's worst case 74px up the frame, where this page's ground is far weaker than the mock's, and
tertiary there measures 2.37:1 against a stated 3:1 floor. **A contract is not a measurement** — do
not relax a floor in that table to match what a layout change did to it. And "how it plays"
(`draw_play_mode`) is a **three-state** read-out resolved by the pure `play_note`: the quiet fact, the
soft "hardware conversion needs \[PLEX PASS\]", and the HDR→SDR warning — which wins whenever both
apply, because a wrong picture outranks a slower one. Both Pass-gated states need a **proven**
missing subscription AND a real RE-ENCODE (`route::Preview::Converts`, the third of three answers —
a container-only `Remux` copies the picture, so no encoder runs and neither Pass-gated server feature
is in play); `Unknown` is silent everywhere, and the truth table is a host test. The row is measured
and drawn from one `Bit` list so it can be bounded at `FACTS_R`, the people column's left edge: the
two are level, and the fragment has no natural width ceiling.
Its **action row is a DYNAMIC control set and no index into it is a constant** (`HeroCtl`/`hero_set`/
`hero_ctls`): the Play/Resume pill, then the ↺ restart disc only while Play would actually resume,
then *Also available* only while a second source holds the item, then the **watch-state tail** — and
NOT a ⓘ *Track information* disc, which sat in that middle group until 2026-08-21 and now opens from
the About footer's Languages column instead, the block it is about (`hero_ctls`' doc argues it; the
row keeps only the controls that DO something to the item). Each
watch disc's glyph states the OUTCOME its press produces — the design system's rule for a control
that is already a circle, so the BARE `Icon::Check` / `Icon::Minus`, never the filled discs a menu
ROW carries — which means an item that is done offers only *mark unwatched*, one never started only
*mark watched*, and a **part-watched item gets BOTH**, because it is in neither state and no single
toggle can express two destinations. Which of the three it is comes from the pure `hero_watch_state`,
resolving into `widgets::PosterMark` so the app has one watch-state vocabulary (it is a second
resolver, not a call to `poster_mark`: a mark DESCRIBES while a control promises what its press
delivers, so the two diverge on a container mid-run and at the resume rule's edges). The verb a press
writes comes from the DISC (`HeroAction::watch_write`), never re-derived from the item's state. Focus
follows its CONTROL through a set change rather than its index (`hero_col`) — a disc that deletes
itself is the ordinary case here, not the exotic one.
And the episode filmstrip is **two focus rows deep** (`EpRow`), the page's second 2D block after
About: the still (plays; press-and-hold opens its context menu) and the metadata block beneath it
(opens that episode's own page). That navigation and the Related shelf's are the page's two
detail-on-detail moves, and both **report** rather than act — `take_open_request`, drained once a
frame by `app.rs`, which owns routing and the BACK trail (`trail.rs`). They used to call `open_rk`
themselves on the reasoning that the route was already `Detail` so nothing above had to be told,
which is exactly why BACK from an episode page landed on Home. The request carries the destination
rk and nothing else: where the page being LEFT was standing is a `Spot` (focus, sub-row, per-section
memory and season NUMBER) that `app.rs`'s `leaving_spot` snapshots from `detail::spot` on the press
frame for EVERY navigation off this page, not just these two, and `open_rk_at` puts it back on the
same two-stage `Pending` pump the post-playback reveal uses.
DOWN/UP pair the two exactly, LEFT/RIGHT hold the row, and the text row is deliberately NOT a
`focus_is_card` — it is a link, so it commits at once and can never be held open into the still's
menu. Its focus mark is About's idiom — literally the same call, `widgets::text_block_highlight`,
over a frame `ep_text_hl` measures to hug that episode's own content — and it fits INSIDE the band
`block_h(2)` already reserves, so nothing below the filmstrip moves when a highlight appears.
Its **season strip is a `TabStrip`**, the same component and the same springs as the shared top tab
row (one control, one motion — the directive above `widgets::TAB_PILL_H`), held per `DetailView` and
re-seated in `reset_view_state` so a new item's capsules land rather than glide in from the season
the last item was parked on. `tabs_layout` is CACHED per season set for that: it is now asked for
three times a frame (scroll target, capsule spans, draw) and this row has no FPS scene to catch the
cost — `fps:detail-transition` is a movie and returns before reaching it.
`library.rs` is the Library browse screen (shared top tab row + server-driven Sort/Filter chips + a
6-across grid of `card_row` tiles, menus built from `Popover`+`TableView`). Its toolbar is a **typed
chip list** (`Chip`), not a fixed row: the **Source chip** — `Library · <name> <handle> ▾`, the
handle a `MICRO` run at 62% of the label's own ink — leads it when the granted roster holds more
than one server and is **absent entirely** otherwise, so a chip's index is never its identity and
every activation matches on the enum (a `_` catch-all there is what once made inserting Source at 0
open the Sort menu). Its panel is the **Sources list**: 640 wide because the rows are two-line, with
two levels swapped by `TabPill::segment` pills at the panel top — *Browse*, a picker with one tick
and no words, and *On Home*, switches whose word is the only mark. The levels never mirror each
other's marks (a mark says where you are, a word says what is set); a server is a `Section` whose
header is the MACHINE and whose accessory is the PERSON; the last pinned library uses `value_dim`
and states the rule on its sub-line; an unreachable server's group takes `Section::dim` whole and
still reads `On`. The row model is the pure `source_sections`, host-tested against literal
`browse::SrcGroup`/`SrcRow` projections.
What its content region shows is the pure `readout_of` — Grid / Loading / **Failed** / Empty — the
whole decision in one host-gradeable function, over `browse`'s per-section `SecFetch` AND the
per-SOURCE one (`cur_source_state`, a projection of the same `reachable`/`sections_done` flags the
Sources list dims a group by): a source that did not answer is the same symptom one layer up, and
used to spin forever with no way out. Both of the source's terminal answers are gated on there
being NOTHING TO SHOW, which is the one rule the multi-source table changed — a source that stops
answering KEEPS the sections it had learned and can still hold the items you were looking at, so
the read-out takes the region only when the store is empty too, the same trade the page rule has
always made and kept identical at both layers on purpose. The **Failed** read-out is a
`StatusOverlay` anchored in the CONTENT REGION rather than on the panel, because the chrome above
it is still live and that is the difference between a section failing and the app failing: it names
the MACHINE that failed, gives one reason line naming the person who shared it, and offers one
*Try again* that re-kicks that one source (`browse::retry_cur_source` picks the layer — an
undiscovered table is re-fetched, a failed page only clears its back-off). Those two names are the
only identifying strings it renders and the list is CLOSED: no address, no path, no
machineIdentifier (`stats.rs`' redaction rule, for its reason — a read-out is photographed). It is
drawn beside NO GRID CONTROL — no Sort, no Filter, no count, no A–Z rail (all four act on a grid
with no items) and no alert glyph (that mark is the player's) — but it KEEPS the Source chip,
which is navigation and is how you leave a dead source, which is in turn why it spends no line on
a way out. `chips()` and `rail_drawn()` are the ONE predicate each behind a control's draw, its
pointer hit test and the focus walk, so neither can be invisible-and-clickable nor
drawn-and-unreachable; `Area::Status` is the band `sync_readout_focus` hands to the one control,
and focus LANDS on Try again because a server that did not answer often answers a second later.
`/tmp/plxnative-libfail` (`dead`/`own`/`table`/`empty`) forces that projection for a device pass —
this screen's failure is otherwise unreachable on purpose — and is compiled out of a RELEASE build
with every other trigger.
It also
owns the **deferred reload**: every path that REPLACES the item set (tab, sort, unwatched, genre, and
`browse::reset()` wiping the store from underneath — caught by an `EPOCH` watchdog on
`browse::query_gen()`) goes through `Xfade` instead of the store, as a typed `Pending` the fade's
commit frame applies. The 70 ms that buys is why the top chrome reads `view_section`/`view_unwatched`/
`view_sort_label`/`view_filter_label` rather than `browse` — a control must acknowledge its press on
the press frame while the grid is still dissolving. Do **not** close that gap by committing the query
state early and deferring only the store wipe: `maybe_spawn` scans the OLD listing for holes and
`pump` splices at `st.items[start + k]`, so a filtered page would land at unfiltered offsets. `enter()` takes an `Arrival` for the one thing it cannot infer: on `Cut` (the
`/tmp/plxnative-library` boot) it `tab_row_reveal`s, JUMPING the shared strip onto this section's
pill; on `Faded` (arriving through `nav`'s page transition, at the fade floor) it must not — a scroll
jump under a travelling capsule teleports the pills out from under it, and `tab_row_update` has been
springing toward that pill since the press frame anyway. `flush()`
in `back()` and at the top of `enter()` is load-bearing — a queued action dropped by leaving the screen
leaves the chip labelled for a listing that never happened. **All three screens that wear the shared top bar — Home, `library.rs` and `search/mod.rs` — make the profile chip a real focus stop, reached the same way on each: LEFT off the FIRST pill, RIGHT back onto it, DOWN out of the band wherever that screen's DOWN off the pills goes.** That is the only walk the geometry admits (the chip sits at the margin, the pills are centred to its right; `widgets`' own test pins the clearance), which is why one rule fits all three. Each screen owns only WHERE ITS FOCUS IS (`home::top_focus` / `library::top_focus` / `search::top_focus`, and the `Area::Chip` / `Zone::Chip` bands behind them); the chip's PRESS is answered once in `app.rs` (`key_ok`'s `TopFocus::Chip` arm and the pointer's `chip_clicked`), because unlike a pill — whose destination depends on the screen you are standing on — the chip always opens the account menu. **That popover then stands on the page it was opened FROM** (`app.rs`'s `Route::Account { over: BarHost }`, `MenuHost`'s twin): the page underneath is drawn once into `gfx::FrameCache`, its updates pause, later menu frames reuse one host quad, and a dismissal returns to the same route. `Route::Account` was a unit variant meaning "Home, plus the panel" while Home was the only screen that could press the chip, so making it pressable on three without that payload would cut to Home under the panel and strand the user there. **And all three draw the chip AFTER `draw_tab_row`** — the focused chip unfurls the profile name toward the centred strip, and under the track it is painted over by the track's own scrim; the Library and Search drew it first, which was invisible only while their chip could not open. `login.rs` is the two-column QR / short-code sign-in driven by `crate::auth`'s phase — but ONLY in `Phase::Waiting`. Its three other states (working, failed, and the post-delete `Deleted`) are a **centred `StatusOverlay`** on `Rect::FULL`, not that route: a two-column composition with a lone 26px spinner in the content column is the shape for a screen with a list on the right, and reads as broken without one. The working read-out **grows a `Try again` after `ESCAPE_AFTER_MS`** (12 s), which is the only way out of a wedged sign-in — BACK goes through `auth::cancel`, which resumes a STORED session, and a first-ever boot has none, so the key is swallowed by design and the app had to be killed. `auth::retry` bumps the auth epoch, so the wedged worker's eventual answer is discarded. `Deleted` is `StatusKind::Empty`, never `Failed`: a delete the user asked for is a completed action, not a fault; `profiles.rs` deliberately keeps the centred "who's watching" picker + PIN keypad, built on the **shared** `CardRow` with a circular `RowStyle::PROFILES` so avatars get the same springs as the poster shelves; `account_menu.rs` is the top-left profile popover (change profile / sign out) on the same `TableView` as the track menu — a popover on ANY of the three bar screens, not a second face of Home. |

Profile-chip note (2026-09-02): the chip's capsule is the band's second glass surface again, and its
round surround is present AT REST (it widens on focus; the face never fades). A 2026-09-01 correction
flattened it into a local copy of the track's face, blaming the union of the two blur regions
(728×220 → 1316×220) for Home's moving-grid frame rate; that diagnosis was measured and refuted the
next day — the regression was `fs_ambient.frag`'s dither hash — and with it fixed the resting glass
surround costs nothing the `home-hero` / `home-fold` gates can see. The record is
`docs/backdrop-blur-profiling.md`, the 2026-09-02 section.

## Gotchas that bite

- **`Label`/`Button` hold a non-owning `*const c_char`.** Keep the `CString` alive for the whole
  draw frame (bind it to a `let` in the same scope) or you'll draw freed memory. (`TextView` is the
  exception — it borrows a `&str` and builds its own `CString`s internally, so it's memory-safe.)
- **The focus ring/glow is shader-baked** (`gfx.rs` `FS_SRC`/`FS_IMG`, folded into the card
  composite pass): callers drive it only through a `focus: f32` scalar and the geometry consts
  (`theme::CARD_RING_RAD`, `consts::GLOW_PAD`). Its color cannot be tokenized — don't try, and
  don't re-add it as a literal.
- **`detail.rs` below-hero layout is a computed flow, not magic constants.** The below-hero sections
  are the children of a shared `ScrollColumn` (`impl Column for DetailView`): the container's
  `child_top(i)` stacks the *present* blocks' `block_h()` heights (via `Column::height`) from
  `CONTENT_TOP` with one `SECTION_GAP` (season tabs → episodes hug with `TAB_EP_GAP`). To resize/space
  a section, change its `block_h` (content-derived — e.g. Related tracks `REL_H`=`CARD_H`) or the gap —
  never reintroduce a hard-coded per-section Y. `ScrollColumn::draw` culls off-screen sections with
  `on_axis` and pre-translates each child painter to its origin, so each `draw_*` draws from local
  y=0; the same `child_top` feeds `scroll_target` (via `lift_target`), so draws and scroll can't drift.
- **A few things are deliberately immediate-mode** (documented in `docs/ui-system-migration.md` §D):
  home's `Backdrop` **per-element alphas** (the alphas, not the component — those alphas are springs
  now and it has a real `update`; what must never happen is routing the per-LAYER reveals through
  the cascade `p.alpha()`, which would fade the art and the wash as one object. The cascade still
  reaches the whole `Backdrop` from above, which is how `nav`'s page dip covers it),
  `player_hud`'s `SCR_H`-offset geometry
  (shared with `app.rs` pointer hit-tests), and the subtitle renderer. Leave them; wrapping them in a
  `View` breaks a load-bearing contract.
- **Clipping: prefer culling; use `Painter::clip` only for a hard-bounded panel.** A real GL scissor
  clip exists now — `Painter::clip(rect)` / `clip_clear()` (backed by `gfx::clip_set`/`clip_clear`). It
  is **global GL state**, so you MUST pair set/clear inside the same frame (see `TableView::draw`, its
  one user — a fixed panel whose overflowing list is cut cleanly at the frame). Do NOT reach for it in
  the big scroll flow: `ScrollColumn`/the shelves deliberately **cull** off-frame children by index
  (`on_axis`) instead, which avoids per-frame scissor churn and needs no clean-up. So: bounded list/panel
  → `clip`; long scrolling document → cull. (The old edge-fade-mask trick is gone — a linear fade can't
  cut a tall two-line row evenly, which read as a broken clip; scissor replaced it.)

## When you're done

Three signals, in ascending cost. **`make check`** first — the host unit suite (`cargo test --lib`,
~0.3s) includes nine UI tests: `home.rs`'s focus packing round trips, row stepping staying
inside the shelf array, and the pointer hit column matching the drawn card at every snap phase; and
`card_row.rs`'s heading-clearance behaviour driven frame by frame (the regression that the heading
must hold still while focus walks the slots beneath it). If you touch focus navigation, hit-testing,
or shelf motion, **add a test here** — that math is host-testable and these caught real bugs.
Note the asymmetry, because it tells you where to put a new test: `card_row.rs` drives a **local**
`CardRow`, so its tests are ordinary and parallel; `home.rs` keeps focus in `static mut fr`/`fc`, so
its tests must take the module's `FOCUS` mutex or they race each other rather than the code. That
lock is the cost of screen-level singleton state — hold it, don't work around it. **`xfade.rs` is
the cautionary case**: its tests were ordinary and parallel until `tick` started reporting to
`ui::idle`'s process-global flag, at which point driving a fader began mutating state *another
module's* assertions read. They all take `testlock::serial()` now. Anything you make report to the
frame gate inherits that obligation.

**If you add anything that ANIMATES, it owes two tests**, because the failure modes are opposite and
each is invisible to the other's gate. Host: it reports while running **and** goes quiet at rest —
`ui/idle.rs`'s and `ui/xfade.rs`'s test modules are the pattern, including a settled-tree case that
steps 439 springs and asserts *nothing* is requested. Device: an `fps_floor` scene proving it
still animates under the gate, and (if its screen settles) an `fps_ceiling` proving it stops. An
over-reporting animator costs the entire ~38-points-of-a-core saving while every `floor` in the
suite still passes, so the ceiling is not optional politeness — it is the only thing watching.

Then **`make`** must stay green (ARM cross-build). Then the device: there is no host *runtime*, so
nothing above draws a single pixel. For anything that moves pixels, capture the panel on the TV
(`tools/capture-screen.sh out.png DISPLAY|GRAPHIC`) and eyeball it — the token collapses and
cap-band re-centering are invisible until deployed.
